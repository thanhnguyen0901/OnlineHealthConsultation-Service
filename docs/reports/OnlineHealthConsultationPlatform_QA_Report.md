# BÁO CÁO ĐỒ ÁN MÔN HỌC

TODO: Cần insert hình logo trường/khoa nếu báo cáo yêu cầu.

TODO: Bổ sung tên trường/khoa/giảng viên nếu cần.

**Môn học:** Đảm bảo chất lượng phần mềm

---

# XÂY DỰNG VÀ KIỂM THỬ NỀN TẢNG TƯ VẤN SỨC KHỎE TRỰC TUYẾN

**Tên tiếng Anh:** Online Health Consultation Platform

---

**Nhóm thực hiện**

| STT | Họ và tên | MSSV | Vai trò |
|-----|-----------|------|---------|
| 1 | Nguyễn Việt Thanh | K223DTCN436 | Nhóm trưởng |
| 2 | Trần Long Đại | K223DTCN439 | Thành viên |
| 3 | Vũ Hải Minh | K223DTCN380 | Thành viên |

TODO: Bổ sung thời gian/địa điểm thực hiện và nộp báo cáo nếu cần.

---

## LỜI CẢM ƠN

Nhóm thực hiện đề tài xin gửi lời cảm ơn chân thành đến nhà trường và khoa đã tạo điều kiện học tập, cung cấp nền tảng kiến thức và môi trường thực hành để nhóm có cơ hội tiếp cận môn học Đảm bảo chất lượng phần mềm theo hướng gắn với một sản phẩm cụ thể.

Nhóm xin trân trọng cảm ơn giảng viên hướng dẫn đã định hướng nội dung, góp ý phương pháp phân tích yêu cầu, thiết kế kiểm thử và cách trình bày báo cáo. Những góp ý trong quá trình học tập giúp nhóm hiểu rõ hơn vai trò của đảm bảo chất lượng phần mềm, đặc biệt là mối liên hệ giữa tài liệu SRS, thiết kế hệ thống, đặc tả test case và kiểm thử tự động.

Bên cạnh đó, nhóm cũng ghi nhận sự cố gắng và tinh thần phối hợp của từng thành viên trong quá trình thực hiện đề tài. Các thành viên đã cùng nhau phân chia công việc, rà soát nội dung, bổ sung tài liệu và hoàn thiện báo cáo theo phạm vi đã thống nhất.

Mặc dù nhóm đã nỗ lực hoàn thành báo cáo, do giới hạn về thời gian và kinh nghiệm thực tế, nội dung vẫn khó tránh khỏi thiếu sót. Nhóm kính mong nhận được nhận xét và góp ý từ giảng viên để có thể điều chỉnh, hoàn thiện kiến thức và rút kinh nghiệm cho các đồ án tiếp theo.

Xin trân trọng cảm ơn.

---

## MỤC LỤC

**LỜI CẢM ƠN**

**MỤC LỤC**

**DANH SÁCH HÌNH ẢNH**

**DANH SÁCH BẢNG**

**TÓM TẮT**

**CHƯƠNG I. TỔNG QUAN**

1. Giới thiệu đề tài
   - 1.1. Mục đích của đề tài
   - 1.2. Mục tiêu
   - 1.3. Phương pháp tiến hành
2. Vai trò và nhiệm vụ của mỗi thành viên
3. Cơ sở lý thuyết

**CHƯƠNG II. PHÂN TÍCH & THIẾT KẾ HỆ THỐNG**

1. Yêu cầu từ Stakeholder
2. Danh sách use case theo SRS
3. Phân tích hệ thống theo actor
4. Định nghĩa yêu cầu chất lượng phần mềm
5. Thiết kế hệ thống

**CHƯƠNG III. TRIỂN KHAI HỆ THỐNG VÀ KIỂM THỬ**

1. Phần mềm ứng dụng
2. Cài đặt hệ thống
3. Chiến lược kiểm thử
4. Đặc tả test case theo actor/use case
5. Hiện thực automation test và ghi nhận lỗi

**CHƯƠNG IV. KẾT LUẬN**

**TÀI LIỆU THAM KHẢO**

---

## DANH SÁCH HÌNH ẢNH

| Số hình | Tên hình |
|---------|----------|
| Hình 1 | Tổng quan quy trình tư vấn sức khỏe trực tuyến |
| Hình 2 | Use case tổng quan toàn hệ thống |
| Hình 3 | Use case diagram nhóm Guest User |
| Hình 4 | Use case diagram nhóm Patient |
| Hình 5 | Use case diagram nhóm Doctor |
| Hình 6 | Use case diagram nhóm Administrator |
| Hình 7 | Activity diagram Guest User tra cứu bác sĩ |
| Hình 8 | Activity diagram Patient đăng ký/đăng nhập |
| Hình 9 | Activity diagram Patient đặt lịch tư vấn |
| Hình 10 | Activity diagram Patient gửi câu hỏi sức khỏe |
| Hình 11 | Activity diagram Doctor phản hồi câu hỏi |
| Hình 12 | Activity diagram Doctor thực hiện phiên tư vấn |
| Hình 13 | Activity diagram Administrator quản lý hệ thống |
| Hình 14 | Kiến trúc hệ thống |
| Hình 15 | ERD/database diagram |
| Hình 16 | Giao diện trang chủ |
| Hình 17 | Giao diện danh sách chuyên khoa |
| Hình 18 | Giao diện danh sách bác sĩ |
| Hình 19 | Giao diện chi tiết bác sĩ |
| Hình 20 | Giao diện đăng nhập/đăng ký |
| Hình 21 | Giao diện hồ sơ bệnh nhân |
| Hình 22 | Giao diện hồ sơ bác sĩ |
| Hình 23 | Giao diện đặt lịch tư vấn |
| Hình 24 | Giao diện gửi câu hỏi sức khỏe |
| Hình 25 | Giao diện danh sách câu hỏi của bệnh nhân |
| Hình 26 | Giao diện phản hồi câu hỏi của bác sĩ |
| Hình 27 | Giao diện lịch hẹn của bệnh nhân |
| Hình 28 | Giao diện quản lý lịch của bác sĩ |
| Hình 29 | Giao diện phiên tư vấn trực tuyến |
| Hình 30 | Giao diện kết quả tư vấn/đơn thuốc |
| Hình 31 | Giao diện đánh giá tư vấn |
| Hình 32 | Giao diện quản trị người dùng |
| Hình 33 | Giao diện quản lý chuyên khoa |
| Hình 34 | Giao diện quản lý lịch hẹn |
| Hình 35 | Giao diện dashboard thống kê |
| Hình 36 | Kết quả chạy Playwright trên terminal |
| Hình 37 | Playwright HTML report |
| Hình 38 | Screenshot minh chứng test case đặt lịch |
| Hình 39 | Screenshot minh chứng test case phản hồi câu hỏi |
| Hình 40 | Screenshot minh chứng bug nếu có |

---

## DANH SÁCH BẢNG

| Số bảng | Tên bảng |
|---------|----------|
| Bảng 1 | Phân công nhiệm vụ thành viên |
| Bảng 2 | Danh sách actor và mô tả |
| Bảng 3 | Danh sách use case của Guest User |
| Bảng 4 | Danh sách use case của Patient |
| Bảng 5 | Danh sách use case của Doctor |
| Bảng 6 | Danh sách use case của Administrator |
| Bảng 7 | Danh sách use case của External Services |
| Bảng 8 | User Story |
| Bảng 9 | Mapping use case SRS và phạm vi kiểm thử |
| Bảng 10 | Yêu cầu chất lượng phần mềm |
| Bảng 11 | API/Form theo use case |
| Bảng 12 | Từ điển cơ sở dữ liệu |
| Bảng 13 | Test case nhóm Guest User |
| Bảng 14 | Test case nhóm Patient |
| Bảng 15 | Test case nhóm Doctor |
| Bảng 16 | Test case nhóm Administrator |
| Bảng 17 | Test case nhóm External Services/Notification |
| Bảng 18 | Checklist rà soát yêu cầu |
| Bảng 19 | Checklist rà soát thiết kế |
| Bảng 20 | Checklist rà soát API |
| Bảng 21 | Checklist rà soát bảo mật |
| Bảng 22 | Bảng tổng hợp kết quả kiểm thử |
| Bảng 23 | Bảng ghi nhận lỗi |

---

## TÓM TẮT

Đề tài "Xây dựng và kiểm thử nền tảng tư vấn sức khỏe trực tuyến" được thực hiện trong khuôn khổ môn học Đảm bảo chất lượng phần mềm. Mục tiêu của đề tài là xây dựng một ứng dụng web hỗ trợ người dùng tiếp cận dịch vụ tư vấn sức khỏe từ xa, đồng thời áp dụng các hoạt động đảm bảo chất lượng phần mềm từ giai đoạn phân tích yêu cầu đến thiết kế test case và định hướng kiểm thử tự động.

Phạm vi MVP của hệ thống được xác định theo SRS của Online Health Consultation Platform. Hệ thống phục vụ bốn actor chính gồm Guest User, Patient, Doctor và Administrator. Bên cạnh các actor trực tiếp, hệ thống còn tương tác với các hệ thống bên ngoài gồm Notification Service, Video Communication Service và File Storage Service để hỗ trợ thông báo, phiên tư vấn trực tuyến và lưu trữ tệp liên quan đến hồ sơ hoặc nội dung tư vấn.

Về chức năng, hệ thống bao gồm các nhóm nghiệp vụ chính: public access/discovery, authentication/authorization, patient profile, doctor profile, specialty management, doctor discovery, health question, appointment, consultation session, consultation result/prescription, rating, notification, admin management và reporting/dashboard. Các nhóm chức năng này phản ánh đầy đủ phạm vi sử dụng của nền tảng, từ việc người dùng khách tra cứu thông tin bác sĩ, bệnh nhân đăng ký tài khoản và đặt lịch tư vấn, bác sĩ phản hồi câu hỏi và thực hiện phiên tư vấn, đến việc quản trị viên quản lý người dùng, chuyên khoa, lịch hẹn, nội dung và số liệu vận hành.

Báo cáo không chỉ tập trung vào một vài luồng minh họa, mà tổ chức nội dung phân tích và kiểm thử theo đầy đủ các nhóm use case chính trong SRS. Một số luồng E2E quan trọng như tra cứu bác sĩ, đặt lịch tư vấn, gửi và phản hồi câu hỏi sức khỏe, tham gia phiên tư vấn trực tuyến có thể được chọn để minh họa automation test bằng Playwright. Tuy nhiên, phần phân tích yêu cầu, mapping use case và đặc tả test case vẫn cần bao phủ các nhóm actor và chức năng chính của hệ thống.

Định hướng đảm bảo chất lượng của đề tài gồm: phân tích yêu cầu, thiết kế use case, thiết kế UI/API/database, thiết kế test case, xây dựng kiểm thử tự động bằng Playwright cho các luồng phù hợp, kết hợp checklist rà soát bảo mật, phân quyền và quyền riêng tư dữ liệu sức khỏe. Báo cáo không ghi kết quả kiểm thử thực tế khi chưa có quá trình thực thi và minh chứng tương ứng; các kết quả, lỗi phát hiện và hình ảnh minh chứng sẽ được bổ sung sau khi nhóm hoàn tất hoạt động kiểm thử.

Về công nghệ, frontend sử dụng React 18, TypeScript, Vite, React Router v6, Redux Toolkit, Redux Saga, Axios, PrimeReact, Tailwind CSS, Formik, Yup, i18next và Recharts. Backend được trình bày theo hướng Node.js với NestJS, TypeScript, Prisma ORM và PostgreSQL; hệ thống sử dụng JWT, RBAC và kiểm soát quyền sở hữu dữ liệu cho xác thực và phân quyền. Công cụ kiểm thử chính là Playwright cho E2E automation test; tùy phạm vi triển khai, nhóm có thể bổ sung Postman/API testing và Jest/unit test cho các tầng phù hợp.

# CHƯƠNG I. TỔNG QUAN

## 1. Giới thiệu đề tài

Trong những năm gần đây, chuyển đổi số trong lĩnh vực y tế trở thành một nhu cầu rõ rệt khi người dân ngày càng quan tâm đến việc tiếp cận thông tin sức khỏe nhanh chóng, thuận tiện và an toàn. Bên cạnh hình thức khám trực tiếp tại cơ sở y tế, các nền tảng tư vấn sức khỏe trực tuyến giúp rút ngắn khoảng cách giữa bệnh nhân và bác sĩ, hỗ trợ người dùng đặt câu hỏi, đặt lịch và nhận tư vấn ban đầu mà không phụ thuộc hoàn toàn vào vị trí địa lý.

Đề tài "Xây dựng và kiểm thử nền tảng tư vấn sức khỏe trực tuyến" được thực hiện nhằm xây dựng một hệ thống web hỗ trợ các nghiệp vụ cốt lõi của quá trình tư vấn sức khỏe từ xa. Hệ thống cho phép người dùng xem thông tin nền tảng, tra cứu chuyên khoa, tìm kiếm bác sĩ, xem hồ sơ công khai của bác sĩ, gửi câu hỏi sức khỏe, đặt lịch tư vấn, tham gia phiên tư vấn trực tuyến, nhận phản hồi, xem kết quả tư vấn hoặc đơn thuốc và đánh giá chất lượng tư vấn sau khi hoàn tất.

Đối với bác sĩ, hệ thống hỗ trợ quản lý hồ sơ chuyên môn, theo dõi lịch tư vấn, phản hồi câu hỏi sức khỏe, tham gia phiên tư vấn và ghi nhận kết quả tư vấn cho bệnh nhân. Đối với quản trị viên, hệ thống cung cấp các chức năng quản lý người dùng, bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung cần kiểm duyệt và dashboard thống kê để phục vụ vận hành nền tảng.

Do hệ thống xử lý thông tin cá nhân và dữ liệu sức khỏe, đảm bảo chất lượng phần mềm là nội dung trọng tâm của đề tài. Các yếu tố cần được quan tâm gồm bảo mật, quyền riêng tư, phân quyền theo vai trò, quyền sở hữu dữ liệu, validation form/API, kiểm soát trạng thái lịch hẹn, thông báo lỗi rõ ràng và tính đúng đắn của các luồng nghiệp vụ. Việc kiểm thử không chỉ nhằm xác nhận giao diện hoạt động đúng, mà còn nhằm đánh giá hệ thống có bảo vệ dữ liệu và thực thi quy tắc nghiệp vụ đúng như SRS hay không.

TODO: Cần insert hình tổng quan quy trình tư vấn sức khỏe trực tuyến.

### 1.1. Mục đích của đề tài

Mục đích của đề tài là xây dựng nền tảng web tư vấn sức khỏe trực tuyến theo phạm vi MVP đã xác định trong SRS. Nền tảng cần đáp ứng các nhu cầu cơ bản của Guest User, Patient, Doctor và Administrator, đồng thời có khả năng tích hợp với các dịch vụ hỗ trợ như Notification Service, Video Communication Service và File Storage Service.

Thông qua đề tài, nhóm vận dụng kiến thức của môn Đảm bảo chất lượng phần mềm vào một sản phẩm có nhiều yêu cầu nghiệp vụ và yêu cầu chất lượng rõ ràng. Các nội dung như phân tích yêu cầu, thiết kế use case, thiết kế dữ liệu, thiết kế API, đặc tả test case, kiểm thử phân quyền và kiểm thử luồng nghiệp vụ được trình bày như một chuỗi liên kết từ yêu cầu đến đánh giá chất lượng.

Báo cáo hướng đến việc thể hiện mối quan hệ giữa yêu cầu, thiết kế, triển khai và kiểm thử. Mỗi chức năng chính cần có cơ sở yêu cầu, cách thiết kế phù hợp, điều kiện kiểm thử và tiêu chí đánh giá rõ ràng. Đây là cơ sở để nhóm đánh giá hệ thống thông qua các test case chức năng, test case phân quyền, test case dữ liệu và test case theo luồng nghiệp vụ.

### 1.2. Mục tiêu

Các mục tiêu chính của đề tài gồm:

- Phân tích SRS và xác định đầy đủ actor, external service và các nhóm use case chính của hệ thống.
- Xây dựng user story và use case theo từng actor: Guest User, Patient, Doctor và Administrator.
- Thiết kế wireframe/UI, API, database và architecture phù hợp với phạm vi MVP.
- Triển khai hoặc mô tả các chức năng MVP chính, gồm public discovery, authentication/authorization, hồ sơ bệnh nhân, hồ sơ bác sĩ, chuyên khoa, tìm kiếm bác sĩ, hỏi đáp sức khỏe, đặt lịch, phiên tư vấn, kết quả tư vấn/đơn thuốc, đánh giá, thông báo, quản trị và báo cáo.
- Thiết kế test case theo từng nhóm actor/use case, bao phủ cả luồng hợp lệ, luồng lỗi, dữ liệu không hợp lệ và kiểm soát quyền truy cập.
- Áp dụng các kỹ thuật kiểm thử như Black-box Testing, Equivalence Partitioning, Boundary Value Analysis, Decision Table, API testing và E2E testing.
- Định hướng kiểm thử tự động bằng Playwright cho một số luồng E2E quan trọng khi giao diện và dữ liệu kiểm thử đã sẵn sàng.
- Rà soát các yêu cầu bảo mật, phân quyền và privacy, đặc biệt đối với dữ liệu sức khỏe, token, mật khẩu, lịch hẹn, nội dung tư vấn và đơn thuốc.
- Tổng hợp kết quả kiểm thử, ghi nhận lỗi và đề xuất cải tiến sau khi có quá trình thực thi kiểm thử thực tế.

### 1.3. Phương pháp tiến hành

**1. Khảo sát và phân tích yêu cầu**

Nhóm bắt đầu bằng việc đọc và phân tích SRS của Online Health Consultation Platform để xác định phạm vi MVP, actor, external service, yêu cầu chức năng và yêu cầu phi chức năng. Kết quả phân tích được dùng để lập danh sách use case, nhóm chức năng chính và các quy tắc nghiệp vụ cần kiểm thử.

**2. Nghiên cứu công nghệ**

Nhóm nghiên cứu các công nghệ phù hợp với hệ thống web tư vấn sức khỏe trực tuyến, bao gồm frontend, backend, database, xác thực, phân quyền, realtime communication, API documentation và công cụ kiểm thử. Việc lựa chọn công nghệ cần bảo đảm hệ thống có khả năng mở rộng ở mức MVP, dễ bảo trì và thuận lợi cho kiểm thử.

**3. Phân tích và thiết kế hệ thống**

Từ yêu cầu đã thu thập, nhóm xây dựng user story, use case, mô tả actor, thiết kế wireframe/UI, API, database và kiến trúc hệ thống. Các thiết kế được trình bày theo hướng có thể kiểm thử, nghĩa là mỗi luồng nghiệp vụ cần có tiền điều kiện, dữ liệu đầu vào, kết quả mong đợi và các trường hợp ngoại lệ rõ ràng.

**4. Triển khai chức năng**

Các chức năng được triển khai hoặc mô tả theo nhóm nghiệp vụ chính trong SRS, bao gồm truy cập công khai, xác thực, hồ sơ người dùng, tìm kiếm bác sĩ, hỏi đáp sức khỏe, đặt lịch, tư vấn trực tuyến, kết quả tư vấn, đơn thuốc, đánh giá, thông báo, quản trị và dashboard. Trong quá trình triển khai, các quy tắc phân quyền, validation và kiểm soát trạng thái nghiệp vụ cần được đặt làm tiêu chí quan trọng.

**5. Thiết kế kiểm thử**

Nhóm xây dựng test case theo từng actor và use case, bao gồm test case chức năng, test case dữ liệu, test case phân quyền, test case trạng thái và test case liên quan đến external service. Các kỹ thuật như phân vùng tương đương, giá trị biên và bảng quyết định được áp dụng cho các trường hợp có nhiều điều kiện nghiệp vụ.

**6. Hiện thực kiểm thử tự động**

Một số luồng E2E quan trọng có thể được lựa chọn để minh họa automation test bằng Playwright, chẳng hạn tra cứu bác sĩ, đăng nhập, đặt lịch tư vấn, gửi câu hỏi sức khỏe và bác sĩ phản hồi câu hỏi. Kiểm thử tự động được định hướng như phần hỗ trợ cho quá trình đảm bảo chất lượng, không thay thế toàn bộ kiểm thử thủ công và kiểm thử API.

**7. Ghi nhận kết quả và đánh giá**

Sau khi thực thi kiểm thử, nhóm tổng hợp kết quả, ghi nhận lỗi, phân loại mức độ ảnh hưởng và đề xuất hướng khắc phục. Báo cáo chỉ ghi nhận kết quả kiểm thử khi có quá trình chạy test và minh chứng tương ứng; các phần chưa thực thi được trình bày như kế hoạch hoặc phạm vi cần bổ sung.

## 2. Vai trò và nhiệm vụ của mỗi thành viên

| STT | Họ tên | MSSV | Vai trò | Nhiệm vụ |
|---|---|---|---|---|
| 1 | Nguyễn Việt Thanh | K223DTCN436 | Nhóm trưởng | Phân tích SRS, tổng hợp phạm vi MVP, thiết kế cấu trúc báo cáo, định hướng kiến trúc, API/database, test strategy, phân công công việc, tích hợp nội dung từ các thành viên và rà soát cuối. |
| 2 | Trần Long Đại | K223DTCN439 | Thành viên | Phân tích các luồng Patient gồm đăng ký/đăng nhập, quản lý hồ sơ, tìm kiếm bác sĩ, đặt lịch, gửi câu hỏi, xem phản hồi, theo dõi lịch sử tư vấn và đánh giá; hỗ trợ thiết kế test case cho nhóm Patient. |
| 3 | Vũ Hải Minh | K223DTCN380 | Thành viên | Phân tích các luồng Doctor/Admin gồm hồ sơ bác sĩ, phản hồi câu hỏi, quản lý lịch, tư vấn trực tuyến, đơn thuốc, quản trị hệ thống, dashboard và phân quyền; hỗ trợ test case Doctor/Admin/security. |

## 3. Cơ sở lý thuyết

### 3.1. Quy trình nghiệp vụ của hệ thống tư vấn sức khỏe trực tuyến

Hệ thống tư vấn sức khỏe trực tuyến được tổ chức xoay quanh quá trình người dùng tiếp cận thông tin, tạo tài khoản, lựa chọn bác sĩ, trao đổi vấn đề sức khỏe, đặt lịch tư vấn, tham gia phiên tư vấn và nhận kết quả sau tư vấn. Các quy trình nghiệp vụ chính bao gồm:

- **Quy trình Guest User tra cứu thông tin và chuyển đổi sang đăng ký/đăng nhập:** Guest User xem trang chủ, danh sách chuyên khoa, danh sách bác sĩ và hồ sơ công khai của bác sĩ. Khi chọn thao tác cần xác thực như đặt lịch hoặc gửi câu hỏi, hệ thống chuyển người dùng sang đăng ký/đăng nhập.
- **Quy trình Patient đăng ký/đăng nhập và quản lý hồ sơ:** Patient tạo tài khoản, đăng nhập và cập nhật hồ sơ cá nhân, thông tin liên hệ, ngày sinh, giới tính và thông tin sức khỏe cần thiết cho quá trình tư vấn.
- **Quy trình Patient tìm kiếm bác sĩ và đặt lịch tư vấn:** Patient tìm bác sĩ theo chuyên khoa hoặc từ khóa, xem hồ sơ bác sĩ, chọn khung giờ phù hợp, nhập lý do tư vấn và gửi yêu cầu đặt lịch. Hệ thống kiểm tra dữ liệu, quyền truy cập và tình trạng trùng lịch trước khi tạo lịch hẹn.
- **Quy trình Patient gửi câu hỏi sức khỏe và xem phản hồi:** Patient nhập nội dung câu hỏi, có thể chọn bác sĩ phù hợp nếu luồng nghiệp vụ cho phép, gửi câu hỏi và theo dõi trạng thái. Khi bác sĩ phản hồi, Patient được xem nội dung phản hồi trong phạm vi dữ liệu của chính mình.
- **Quy trình Doctor phản hồi câu hỏi:** Doctor xem danh sách câu hỏi được phân công hoặc thuộc phạm vi phụ trách, nhập nội dung phản hồi, gửi câu trả lời và cập nhật trạng thái xử lý của câu hỏi.
- **Quy trình Doctor quản lý lịch và thực hiện tư vấn:** Doctor xem lịch hẹn, xác nhận hoặc theo dõi trạng thái lịch, tham gia phiên tư vấn trực tuyến với Patient đúng lịch và đúng phạm vi phụ trách.
- **Quy trình ghi nhận kết quả tư vấn và đơn thuốc:** Sau phiên tư vấn, Doctor ghi nhận kết luận, hướng dẫn điều trị hoặc đơn thuốc điện tử cơ bản. Patient chỉ được xem kết quả tư vấn và đơn thuốc gắn với lịch hẹn của chính mình.
- **Quy trình Patient đánh giá tư vấn:** Sau khi lịch hẹn hoàn tất, Patient có thể gửi đánh giá và nhận xét về chất lượng tư vấn. Hệ thống không cho đánh giá khi phiên tư vấn chưa hoàn tất.
- **Quy trình Admin quản lý hệ thống:** Administrator quản lý người dùng, bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung cần kiểm duyệt và dashboard thống kê.
- **Quy trình Notification/Video/File Storage hỗ trợ hệ thống:** Notification Service hỗ trợ gửi thông báo và nhắc lịch; Video Communication Service hỗ trợ phiên tư vấn trực tuyến hoặc video mock ở MVP; File Storage Service hỗ trợ lưu trữ tệp liên quan đến hồ sơ, tài liệu tư vấn hoặc minh chứng y tế nếu phạm vi triển khai có sử dụng.

TODO: Cần insert hình luồng nghiệp vụ tổng quan toàn hệ thống.

### 3.2. Các quy định/quy tắc quản lý/quy trình tại hệ thống

Các quy tắc quản lý là cơ sở để thiết kế hệ thống và xây dựng test case. Đối với nền tảng tư vấn sức khỏe trực tuyến, những quy tắc quan trọng gồm:

- Guest User chỉ được xem dữ liệu public như trang chủ, danh sách chuyên khoa, danh sách bác sĩ và hồ sơ công khai của bác sĩ.
- Patient chỉ được xem và thao tác dữ liệu của chính mình, gồm hồ sơ cá nhân, câu hỏi, lịch hẹn, kết quả tư vấn, đơn thuốc và đánh giá.
- Doctor chỉ được xem dữ liệu được phân công hoặc thuộc phạm vi phụ trách, gồm câu hỏi, lịch hẹn, phiên tư vấn và kết quả tư vấn liên quan.
- Administrator quản lý hệ thống theo quyền được cấp; các thao tác nhạy cảm cần được kiểm soát và ghi nhận để phục vụ truy vết.
- Hệ thống không cho đặt lịch trùng hoặc chồng lấn khung giờ của bác sĩ và bệnh nhân.
- Chỉ Patient và Doctor liên quan đến appointment hợp lệ mới được tham gia phiên tư vấn tương ứng.
- Patient chỉ được đánh giá sau khi phiên tư vấn hoặc lịch hẹn đã hoàn tất.
- Dữ liệu sức khỏe, lịch sử tư vấn, đơn thuốc, thông tin cá nhân và nội dung câu hỏi cần được bảo mật.
- Password, token, secret và thông tin xác thực không được hiển thị hoặc ghi log dưới dạng có thể lộ dữ liệu nhạy cảm.
- Form và API phải có validation đối với dữ liệu bắt buộc, định dạng email, độ dài nội dung, ngày giờ đặt lịch và các trường nghiệp vụ quan trọng.
- Thông báo lỗi cần rõ ràng cho người dùng nhưng không làm lộ thông tin nội bộ của hệ thống.
- Notification có thể thất bại mà không được làm sai nghiệp vụ chính nếu thiết kế cho phép lưu nghiệp vụ trước và xử lý thông báo sau.
- Video có thể fallback sang chat trong phạm vi MVP nếu video không khả dụng, nhưng hệ thống vẫn phải bảo đảm đúng quyền truy cập phiên tư vấn.

### 3.3. Đối tượng sử dụng

**Guest User**

Guest User là người dùng chưa đăng nhập vào hệ thống. Nhóm này có thể xem thông tin nền tảng, danh sách chuyên khoa, danh sách bác sĩ và hồ sơ công khai của bác sĩ. Guest User không được đặt lịch tư vấn, gửi câu hỏi sức khỏe hoặc truy cập dữ liệu cá nhân của người dùng khác. Khi thực hiện thao tác cần xác thực, hệ thống cần chuyển người dùng đến màn hình đăng ký hoặc đăng nhập.

**Patient**

Patient là bệnh nhân hoặc người dùng đã đăng ký tài khoản để sử dụng dịch vụ tư vấn sức khỏe. Patient có thể quản lý hồ sơ cá nhân, tìm kiếm bác sĩ, gửi câu hỏi sức khỏe, đặt lịch, tham gia phiên tư vấn, xem phản hồi, xem kết quả tư vấn, xem đơn thuốc và đánh giá chất lượng tư vấn. Patient chỉ được truy cập dữ liệu thuộc quyền sở hữu của chính mình.

**Doctor**

Doctor là người cung cấp tư vấn chuyên môn trên nền tảng. Doctor có thể quản lý hồ sơ bác sĩ, theo dõi lịch hẹn, phản hồi câu hỏi sức khỏe, tham gia phiên tư vấn trực tuyến, ghi nhận kết quả tư vấn và lập đơn thuốc điện tử cơ bản. Doctor chỉ được truy cập dữ liệu nằm trong phạm vi phụ trách hoặc được hệ thống phân công.

**Administrator**

Administrator là người quản trị và vận hành hệ thống. Vai trò này có thể quản lý người dùng, bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung tư vấn, đánh giá và dashboard thống kê. Vì Administrator có quyền rộng, hệ thống cần phân quyền rõ ràng và kiểm soát chặt các thao tác nhạy cảm.

**Notification Service**

Notification Service là hệ thống bên ngoài hoặc thành phần hỗ trợ gửi thông báo cho người dùng. Dịch vụ này có thể được dùng cho xác nhận lịch hẹn, nhắc lịch tư vấn, thông báo khi có phản hồi câu hỏi hoặc thông báo thay đổi trạng thái. Nếu gửi thông báo thất bại, hệ thống cần ghi nhận trạng thái phù hợp và không làm sai dữ liệu nghiệp vụ chính.

**Video Communication Service**

Video Communication Service hỗ trợ phiên tư vấn trực tuyến giữa Patient và Doctor. Trong phạm vi MVP, dịch vụ này có thể được tích hợp cơ bản hoặc mô phỏng, tùy điều kiện triển khai. Khi video không khả dụng, hệ thống có thể fallback sang chat để bảo đảm phiên tư vấn vẫn có kênh trao đổi.

**File Storage Service**

File Storage Service hỗ trợ lưu trữ tệp liên quan đến hồ sơ, tài liệu y tế, hình ảnh hoặc tài liệu đính kèm phục vụ tư vấn. Các tệp này có thể chứa thông tin nhạy cảm nên cần được kiểm soát quyền truy cập. Người dùng chỉ được xem hoặc tải tệp trong phạm vi được cấp quyền.

### 3.4. Công nghệ hỗ trợ xây dựng phần mềm

**Frontend**

Frontend sử dụng React 18, TypeScript và Vite để xây dựng giao diện web. React Router v6 được dùng cho điều hướng giữa các khu vực public, auth, patient, doctor và admin. Redux Toolkit và Redux Saga hỗ trợ quản lý trạng thái và xử lý side effect; Axios dùng để giao tiếp API. PrimeReact và Tailwind CSS hỗ trợ xây dựng giao diện; Formik và Yup hỗ trợ form và validation; i18next hỗ trợ đa ngôn ngữ; Recharts hỗ trợ biểu đồ thống kê và dashboard.

**Backend**

Backend là Node.js API service. Báo cáo hiện trình bày backend theo hướng NestJS với TypeScript, tổ chức module theo nghiệp vụ như auth, user profile, specialty, discovery, question, appointment, consultation, prescription, rating, notification, admin và reporting. Prisma ORM được sử dụng để làm việc với cơ sở dữ liệu quan hệ.

**Database**

Cơ sở dữ liệu sử dụng PostgreSQL, phù hợp với các quan hệ nghiệp vụ như người dùng, hồ sơ bệnh nhân, hồ sơ bác sĩ, chuyên khoa, câu hỏi, phản hồi, lịch hẹn, phiên tư vấn, đơn thuốc, đánh giá, thông báo và audit log. Các ràng buộc dữ liệu cần hỗ trợ tính nhất quán của lịch hẹn, quyền sở hữu dữ liệu và trạng thái nghiệp vụ.

**Authentication/Authorization**

Hệ thống sử dụng JWT cho xác thực phiên đăng nhập. Phân quyền được tổ chức theo role-based access control kết hợp ownership để kiểm soát người dùng chỉ được thao tác dữ liệu thuộc phạm vi của mình. Đây là nội dung quan trọng trong kiểm thử bảo mật và kiểm thử phân quyền.

**Realtime/communication**

Phiên tư vấn trực tuyến có thể sử dụng WebSocket/Socket.IO cho trao đổi thời gian thực. Với phạm vi MVP, video communication có thể được tích hợp cơ bản hoặc mô phỏng; khi video không khả dụng, hệ thống có thể chuyển sang chat nếu thiết kế cho phép.

**Testing**

Công cụ kiểm thử chính cho E2E automation là Playwright. Ngoài ra, nhóm có thể sử dụng Postman hoặc công cụ API testing tương đương để kiểm thử endpoint, request/response, validation và phân quyền. Jest có thể được sử dụng cho unit test hoặc integration test ở các thành phần phù hợp.

**Tools**

Các công cụ hỗ trợ phát triển và quản lý gồm Git cho quản lý phiên bản, Docker cho môi trường chạy dịch vụ phụ trợ, Swagger/OpenAPI cho tài liệu API và hỗ trợ kiểm thử API. Các công cụ này giúp quá trình phát triển, kiểm thử và bàn giao hệ thống có tính nhất quán hơn.

# CHƯƠNG II. PHÂN TÍCH & THIẾT KẾ HỆ THỐNG

## 1. Yêu cầu từ Stakeholder

Trong phạm vi SRS của Online Health Consultation Platform, hệ thống phục vụ nhiều nhóm stakeholder với nhu cầu và phạm vi thao tác khác nhau. Guest User cần truy cập được các thông tin công khai của nền tảng, xem danh sách chuyên khoa, tìm kiếm bác sĩ và xem hồ sơ công khai của bác sĩ trước khi quyết định đăng ký tài khoản hoặc sử dụng dịch vụ tư vấn.

Patient cần các chức năng phục vụ quá trình nhận tư vấn sức khỏe trực tuyến, gồm đăng ký, đăng nhập, đăng xuất, quản lý hồ sơ sức khỏe cá nhân, tìm kiếm bác sĩ, đặt lịch tư vấn, gửi câu hỏi sức khỏe, tham gia phiên tư vấn, xem lịch sử tư vấn, xem kết quả tư vấn/đơn thuốc, đánh giá chất lượng tư vấn và nhận thông báo liên quan đến lịch hẹn hoặc phản hồi mới.

Doctor cần quản lý hồ sơ chuyên môn, cập nhật thông tin chuyên khoa và lịch làm việc, xem các câu hỏi sức khỏe được phân công hoặc có thể xử lý, phản hồi câu hỏi của bệnh nhân, quản lý lịch tư vấn, tham gia tư vấn trực tuyến, ghi nhận kết quả tư vấn và cấp đơn thuốc điện tử cơ bản sau phiên tư vấn.

Administrator cần quản lý tập trung các dữ liệu vận hành của hệ thống, bao gồm tài khoản bác sĩ, tài khoản bệnh nhân, chuyên khoa, lịch hẹn, nội dung tư vấn, phản hồi, đánh giá và dashboard thống kê. Administrator cũng cần có công cụ kiểm duyệt nội dung và theo dõi hoạt động hệ thống để bảo đảm nền tảng vận hành đúng quy định.

Operator hoặc người vận hành hệ thống quan tâm đến các yêu cầu chất lượng như bảo mật, log, audit, backup, monitoring, xử lý lỗi và khả năng phục hồi khi có sự cố. Ngoài các actor trực tiếp, hệ thống còn tương tác với External Services gồm Notification Service, Video Communication Service và File Storage Service để hỗ trợ gửi thông báo, thiết lập phiên tư vấn trực tuyến và lưu trữ tệp tải lên nếu có.

### 1.1. Khảo sát nhu cầu người dùng

Việc khảo sát trong báo cáo được thực hiện theo hướng phân tích nhu cầu của các nhóm người dùng mục tiêu dựa trên phạm vi SRS. Kết quả tổng hợp cho thấy người dùng cần một giao diện dễ hiểu, thao tác rõ ràng, hạn chế bước không cần thiết và có phản hồi phù hợp khi dữ liệu nhập vào không hợp lệ.

Với Guest User và Patient, nhu cầu nổi bật là tìm bác sĩ nhanh theo chuyên khoa hoặc từ khóa, xem thông tin bác sĩ đủ rõ để ra quyết định, đặt lịch tư vấn theo khung giờ còn trống và tránh tình trạng trùng lịch. Patient cũng cần quản lý hồ sơ sức khỏe, gửi câu hỏi, xem phản hồi, theo dõi lịch sử tư vấn, nhận đơn thuốc và đánh giá chất lượng tư vấn trong một môi trường bảo mật.

Với Doctor, hệ thống cần bảo đảm bác sĩ chỉ nhìn thấy đúng câu hỏi, lịch hẹn và phiên tư vấn thuộc phạm vi phụ trách. Giao diện dành cho Doctor cần hỗ trợ xem lịch, phản hồi câu hỏi, bắt đầu phiên tư vấn, trao đổi qua chat hoặc video, ghi nhận kết quả tư vấn và cấp đơn thuốc một cách mạch lạc.

Với Administrator, nhu cầu chính là quản trị tập trung tài khoản, bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung cần kiểm duyệt và dashboard thống kê. Với người vận hành hệ thống, các yêu cầu như bảo mật dữ liệu sức khỏe, log, audit, backup, monitoring và thông báo lỗi rõ ràng là cơ sở quan trọng để duy trì hệ thống ổn định.

Ngoài ra, hệ thống cần có cơ chế thông báo nhắc lịch rõ ràng cho bệnh nhân và bác sĩ. Phiên tư vấn trực tuyến cần ổn định; trong phạm vi MVP, nếu video không khả dụng, hệ thống có thể fallback sang chat để bảo đảm người dùng vẫn có kênh trao đổi.

### 1.2. User Story

| ID | User Story | Actor | Ưu tiên |
|---|---|---|---|
| US-01 | Là Guest User, tôi muốn xem trang chủ để nắm được mục đích và chức năng chính của nền tảng. | Guest User | Cao |
| US-02 | Là Guest User, tôi muốn xem danh sách chuyên khoa để lựa chọn nhóm bác sĩ phù hợp với nhu cầu sức khỏe. | Guest User | Cao |
| US-03 | Là Guest User, tôi muốn tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa để nhanh chóng tìm được bác sĩ phù hợp. | Guest User | Cao |
| US-04 | Là Guest User, tôi muốn xem chi tiết hồ sơ công khai của bác sĩ để tham khảo chuyên môn, kinh nghiệm và thông tin liên quan. | Guest User | Cao |
| US-05 | Là Guest User, khi chọn đặt lịch hoặc gửi câu hỏi, tôi muốn được chuyển đến đăng nhập/đăng ký để tiếp tục thao tác với vai trò Patient. | Guest User | Cao |
| US-06 | Là Patient, tôi muốn đăng ký tài khoản để sử dụng các chức năng tư vấn sức khỏe trực tuyến. | Patient | Cao |
| US-07 | Là Patient, tôi muốn đăng nhập và đăng xuất để bảo vệ phiên sử dụng cá nhân. | Patient | Cao |
| US-08 | Là Patient, tôi muốn quản lý hồ sơ sức khỏe cá nhân để bác sĩ có thêm thông tin khi tư vấn. | Patient | Cao |
| US-09 | Là Patient, tôi muốn tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa để lựa chọn bác sĩ phù hợp. | Patient | Cao |
| US-10 | Là Patient, tôi muốn đặt lịch tư vấn với bác sĩ theo khung giờ còn trống để được tư vấn đúng thời điểm. | Patient | Cao |
| US-11 | Là Patient, tôi muốn xem danh sách lịch hẹn sắp tới để chủ động theo dõi kế hoạch tư vấn. | Patient | Cao |
| US-12 | Là Patient, tôi muốn hủy lịch hẹn khi điều kiện nghiệp vụ cho phép để tránh giữ khung giờ không sử dụng. | Patient | Trung bình |
| US-13 | Là Patient, tôi muốn gửi câu hỏi sức khỏe để nhận phản hồi chuyên môn từ bác sĩ. | Patient | Cao |
| US-14 | Là Patient, tôi muốn xem phản hồi của bác sĩ đối với câu hỏi đã gửi để theo dõi nội dung tư vấn. | Patient | Cao |
| US-15 | Là Patient, tôi muốn tham gia phiên tư vấn trực tuyến đúng lịch hẹn để trao đổi trực tiếp với bác sĩ. | Patient | Cao |
| US-16 | Là Patient, tôi muốn xem kết quả tư vấn và đơn thuốc để thực hiện theo hướng dẫn sau phiên tư vấn. | Patient | Cao |
| US-17 | Là Patient, tôi muốn đánh giá chất lượng tư vấn sau khi hoàn tất phiên tư vấn để phản hồi trải nghiệm sử dụng. | Patient | Trung bình |
| US-18 | Là Patient, tôi muốn nhận thông báo nhắc lịch và thông báo khi có phản hồi mới để không bỏ lỡ thông tin quan trọng. | Patient | Cao |
| US-19 | Là Doctor, tôi muốn đăng nhập để truy cập khu vực làm việc dành cho bác sĩ. | Doctor | Cao |
| US-20 | Là Doctor, tôi muốn quản lý hồ sơ chuyên môn để cập nhật thông tin chuyên khoa, kinh nghiệm và lịch làm việc. | Doctor | Cao |
| US-21 | Là Doctor, tôi muốn xem các câu hỏi được phân công hoặc có thể xử lý để phản hồi đúng phạm vi chuyên môn. | Doctor | Cao |
| US-22 | Là Doctor, tôi muốn phản hồi câu hỏi của bệnh nhân để hỗ trợ tư vấn sức khỏe. | Doctor | Cao |
| US-23 | Là Doctor, tôi muốn quản lý lịch tư vấn và xem các lịch hẹn đã được đặt để chuẩn bị cho phiên tư vấn. | Doctor | Cao |
| US-24 | Là Doctor, tôi muốn bắt đầu phiên tư vấn và trao đổi qua chat hoặc video để thực hiện tư vấn trực tuyến. | Doctor | Cao |
| US-25 | Là Doctor, tôi muốn ghi kết quả tư vấn để lưu lại kết luận và hướng dẫn sau phiên tư vấn. | Doctor | Cao |
| US-26 | Là Doctor, tôi muốn cấp đơn thuốc điện tử cơ bản để bệnh nhân có thông tin điều trị sau tư vấn. | Doctor | Cao |
| US-27 | Là Administrator, tôi muốn quản lý tài khoản bác sĩ để kiểm soát thông tin và trạng thái hoạt động của bác sĩ. | Administrator | Cao |
| US-28 | Là Administrator, tôi muốn quản lý tài khoản bệnh nhân để hỗ trợ vận hành và xử lý các vấn đề tài khoản. | Administrator | Cao |
| US-29 | Là Administrator, tôi muốn quản lý chuyên khoa để bảo đảm danh mục chuyên khoa chính xác và dễ tra cứu. | Administrator | Cao |
| US-30 | Là Administrator, tôi muốn quản lý lịch hẹn để giám sát trạng thái tư vấn trên hệ thống. | Administrator | Cao |
| US-31 | Là Administrator, tôi muốn kiểm duyệt nội dung tư vấn, phản hồi hoặc đánh giá khi cần để bảo đảm chất lượng thông tin. | Administrator | Trung bình |
| US-32 | Là Administrator, tôi muốn xem dashboard thống kê để theo dõi hoạt động người dùng, bác sĩ, lịch hẹn và phiên tư vấn. | Administrator | Cao |
| US-33 | Là hệ thống, tôi muốn gửi email/SMS nhắc lịch để Patient và Doctor nhận được thông tin đúng thời điểm. | System/External | Cao |
| US-34 | Là hệ thống, tôi muốn khởi tạo video/chat session để Patient và Doctor có kênh trao đổi trong phiên tư vấn. | System/External | Cao |
| US-35 | Là hệ thống, tôi muốn lưu trữ và truy xuất file upload nếu có để hỗ trợ hồ sơ và tài liệu tư vấn. | System/External | Trung bình |

## 2. Danh sách use case theo SRS

### 2.1. Use case của Guest User

| Mã UC | Tên use case | Mô tả ngắn | Mức ưu tiên | Có kiểm thử trong báo cáo? |
|---|---|---|---|---|
| UC-G-01 | Xem trang chủ hệ thống | Xem thông tin giới thiệu nền tảng và các lối vào chức năng công khai. | Cao | Có |
| UC-G-02 | Xem danh sách chuyên khoa | Xem các chuyên khoa đang được công khai trên hệ thống. | Cao | Có |
| UC-G-03 | Tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa | Tìm bác sĩ dựa trên chuyên khoa, tên hoặc từ khóa liên quan. | Cao | Có |
| UC-G-04 | Xem chi tiết hồ sơ công khai của bác sĩ | Xem thông tin công khai của bác sĩ như chuyên khoa, kinh nghiệm và mô tả tư vấn. | Cao | Có |
| UC-G-05 | Xem danh sách bác sĩ nổi bật hoặc bác sĩ đang hoạt động | Xem danh sách bác sĩ được phép hiển thị công khai. | Trung bình | Có |
| UC-G-06 | Chuyển đến đăng ký/đăng nhập khi muốn đặt lịch hoặc gửi câu hỏi | Khi Guest chọn thao tác được bảo vệ, hệ thống chuyển đến luồng xác thực. | Cao | Có |

TODO: Cần insert hình Use case diagram nhóm Guest User.

### 2.2. Use case của Patient

| Mã UC | Tên use case | Mô tả ngắn | Mức ưu tiên | Có kiểm thử trong báo cáo? |
|---|---|---|---|---|
| UC-P-01 | Đăng ký tài khoản | Tạo tài khoản bệnh nhân để sử dụng chức năng tư vấn. | Cao | Có |
| UC-P-02 | Đăng nhập | Xác thực Patient để truy cập chức năng được bảo vệ. | Cao | Có |
| UC-P-03 | Đăng xuất | Kết thúc phiên sử dụng của Patient. | Cao | Có |
| UC-P-04 | Quản lý hồ sơ sức khỏe cá nhân | Cập nhật thông tin cá nhân và dữ liệu sức khỏe cần thiết. | Cao | Có |
| UC-P-05 | Tìm kiếm bác sĩ theo chuyên khoa | Tìm bác sĩ phù hợp với nhu cầu tư vấn. | Cao | Có |
| UC-P-06 | Xem chi tiết bác sĩ | Xem hồ sơ bác sĩ trước khi đặt lịch hoặc gửi câu hỏi. | Cao | Có |
| UC-P-07 | Gửi câu hỏi sức khỏe | Gửi câu hỏi để bác sĩ phản hồi. | Cao | Có |
| UC-P-08 | Đặt lịch hẹn tư vấn | Chọn bác sĩ, thời gian và gửi yêu cầu đặt lịch. | Cao | Có |
| UC-P-09 | Xem danh sách lịch hẹn sắp tới | Theo dõi các lịch hẹn của chính Patient. | Cao | Có |
| UC-P-10 | Tham gia phiên tư vấn | Tham gia chat/video với bác sĩ theo lịch hẹn hợp lệ. | Cao | Có |
| UC-P-11 | Xem phản hồi của bác sĩ | Xem nội dung bác sĩ đã trả lời cho câu hỏi sức khỏe. | Cao | Có |
| UC-P-12 | Xem lịch sử tư vấn | Xem các phiên tư vấn đã diễn ra của chính Patient. | Cao | Có |
| UC-P-13 | Xem đơn thuốc và tóm tắt tư vấn | Xem kết quả tư vấn, hướng dẫn và đơn thuốc sau phiên tư vấn. | Cao | Có |
| UC-P-14 | Đánh giá chất lượng tư vấn | Gửi đánh giá sau khi tư vấn hoàn tất. | Trung bình | Có |
| UC-P-15 | Nhận nhắc lịch và thông báo | Nhận thông báo nhắc lịch, xác nhận lịch hoặc phản hồi mới. | Cao | Có |

TODO: Cần insert hình Use case diagram nhóm Patient.

### 2.3. Use case của Doctor

| Mã UC | Tên use case | Mô tả ngắn | Mức ưu tiên | Có kiểm thử trong báo cáo? |
|---|---|---|---|---|
| UC-D-01 | Đăng nhập | Xác thực Doctor để truy cập khu vực làm việc. | Cao | Có |
| UC-D-02 | Quản lý hồ sơ bác sĩ | Cập nhật hồ sơ chuyên môn, chuyên khoa, kinh nghiệm và lịch làm việc. | Cao | Có |
| UC-D-03 | Xem các câu hỏi sức khỏe được phân công hoặc có thể xử lý | Xem danh sách câu hỏi thuộc phạm vi phụ trách. | Cao | Có |
| UC-D-04 | Phản hồi câu hỏi của bệnh nhân | Nhập và gửi phản hồi cho câu hỏi sức khỏe. | Cao | Có |
| UC-D-05 | Quản lý lịch tư vấn | Theo dõi và quản lý lịch tư vấn của Doctor. | Cao | Có |
| UC-D-06 | Xem các lịch hẹn đã được đặt | Xem danh sách lịch hẹn liên quan đến Doctor. | Cao | Có |
| UC-D-07 | Bắt đầu phiên tư vấn | Khởi tạo hoặc bắt đầu phiên tư vấn theo appointment hợp lệ. | Cao | Có |
| UC-D-08 | Thực hiện tư vấn qua chat hoặc video | Trao đổi với Patient qua kênh tư vấn trực tuyến. | Cao | Có |
| UC-D-09 | Ghi nhận kết quả tư vấn | Lưu kết luận, tóm tắt và hướng dẫn sau phiên tư vấn. | Cao | Có |
| UC-D-10 | Cấp đơn thuốc điện tử cơ bản | Tạo đơn thuốc gắn với phiên tư vấn. | Cao | Có |
| UC-D-11 | Xem lịch sử tư vấn của bệnh nhân | Xem thông tin cần thiết trong phạm vi phiên tư vấn được phép. | Trung bình | Có |

TODO: Cần insert hình Use case diagram nhóm Doctor.

### 2.4. Use case của Administrator

| Mã UC | Tên use case | Mô tả ngắn | Mức ưu tiên | Có kiểm thử trong báo cáo? |
|---|---|---|---|---|
| UC-A-01 | Đăng nhập | Xác thực Administrator để truy cập khu vực quản trị. | Cao | Có |
| UC-A-02 | Quản lý tài khoản bác sĩ | Tạo, cập nhật, duyệt hoặc khóa tài khoản bác sĩ theo quyền. | Cao | Có |
| UC-A-03 | Quản lý tài khoản bệnh nhân | Theo dõi và quản lý tài khoản Patient khi cần. | Cao | Có |
| UC-A-04 | Quản lý chuyên khoa | Tạo, cập nhật, bật/tắt hoặc quản lý danh mục chuyên khoa. | Cao | Có |
| UC-A-05 | Quản lý lịch hẹn | Theo dõi và xử lý lịch hẹn trên toàn hệ thống theo quyền. | Cao | Có |
| UC-A-06 | Kiểm duyệt nội dung tư vấn và phản hồi | Kiểm duyệt câu hỏi, phản hồi, đánh giá hoặc nội dung nhạy cảm khi cần. | Trung bình | Có |
| UC-A-07 | Xem dashboard thống kê hệ thống | Xem số liệu người dùng, lịch hẹn, phiên tư vấn và hoạt động nền tảng. | Cao | Có |
| UC-A-08 | Theo dõi người dùng đang hoạt động và số lượng phiên tư vấn | Theo dõi hoạt động vận hành ở mức tổng quan. | Trung bình | Có |

TODO: Cần insert hình Use case diagram nhóm Administrator.

### 2.5. Use case của External Services

| Mã UC | Tên use case | Mô tả ngắn | Mức ưu tiên | Có kiểm thử trong báo cáo? |
|---|---|---|---|---|
| UC-E-01 | Gửi email nhắc lịch | Gửi email xác nhận hoặc nhắc lịch cho Patient/Doctor. | Cao | Có |
| UC-E-02 | Gửi SMS nhắc lịch | Gửi SMS nhắc lịch nếu kênh SMS được cấu hình. | Trung bình | Có |
| UC-E-03 | Thiết lập phiên tư vấn video | Khởi tạo hoặc hỗ trợ phiên tư vấn video giữa Patient và Doctor. | Cao | Có |
| UC-E-04 | Lưu trữ và truy xuất tệp tải lên | Lưu và truy xuất tệp liên quan đến hồ sơ hoặc tư vấn nếu có. | Trung bình | Có |

TODO: Cần insert hình Use case diagram External Services nếu cần.

### 2.6. Phạm vi phân tích và kiểm thử trong báo cáo

Báo cáo trình bày đầy đủ danh sách use case chính theo SRS cho Guest User, Patient, Doctor, Administrator và External Services. Các test case được thiết kế theo từng nhóm actor/use case để bảo đảm phạm vi kiểm thử không chỉ tập trung vào một vài luồng minh họa, mà vẫn bao phủ các nhóm chức năng quan trọng của hệ thống.

Do giới hạn thời gian, automation test bằng Playwright sẽ ưu tiên một số luồng E2E đại diện, gồm Guest tra cứu bác sĩ, Patient đặt lịch, Patient gửi câu hỏi, Doctor phản hồi câu hỏi, Doctor thực hiện tư vấn và Admin quản lý/chuyên khoa/dashboard nếu giao diện sẵn sàng. Các use case còn lại vẫn được đưa vào phạm vi phân tích và thiết kế test case thủ công hoặc API test tùy mức độ phù hợp.

TODO: Cần insert hình Use case tổng quan toàn hệ thống.

## 3. Phân tích hệ thống

Phần này phân tích các nhóm use case chính theo SRS để làm cơ sở thiết kế test case ở Chương III. Mỗi nhóm được mô tả ở mức nghiệp vụ, không đi quá sâu vào từng use case nhỏ nhưng vẫn nêu rõ mục tiêu, actor, tiền điều kiện, luồng chính, luồng phụ và hậu điều kiện.

### 3.1. Nhóm use case Guest User - Public Discovery

**Mục tiêu**

Nhóm use case này bao phủ UC-G-01 đến UC-G-06, cho phép Guest User truy cập thông tin công khai, xem trang chủ, danh sách chuyên khoa, danh sách bác sĩ, bác sĩ nổi bật hoặc đang hoạt động, xem chi tiết hồ sơ công khai của bác sĩ và được chuyển sang đăng nhập/đăng ký khi muốn thực hiện thao tác được bảo vệ.

**Actor**

- Actor chính: Guest User.
- Actor phụ: System, Authentication/Authorization.

**Tiền điều kiện**

- Hệ thống có dữ liệu public về chuyên khoa và bác sĩ.
- Guest User chưa đăng nhập hoặc không có phiên xác thực hợp lệ.
- Chỉ dữ liệu được phép công khai mới được hiển thị.

**Luồng chính**

1. Guest User truy cập trang chủ hệ thống.
2. Hệ thống hiển thị thông tin nền tảng, chuyên khoa nổi bật và lối vào danh sách bác sĩ.
3. Guest User xem danh sách chuyên khoa hoặc danh sách bác sĩ đang hoạt động.
4. Guest User tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa.
5. Hệ thống trả về danh sách bác sĩ phù hợp, chỉ bao gồm bác sĩ đủ điều kiện hiển thị công khai.
6. Guest User chọn một bác sĩ để xem chi tiết hồ sơ công khai.
7. Khi Guest User chọn đặt lịch hoặc gửi câu hỏi, hệ thống chuyển đến đăng nhập/đăng ký.

**Luồng phụ**

- Nếu không có bác sĩ phù hợp, hệ thống hiển thị trạng thái không có kết quả.
- Nếu bác sĩ inactive hoặc chưa được duyệt, hệ thống không hiển thị trong khu vực public.
- Nếu Guest User chỉ xem thông tin công khai, hệ thống không yêu cầu đăng nhập.

**Hậu điều kiện**

- Guest User xem được dữ liệu public hợp lệ.
- Không có dữ liệu sức khỏe, lịch hẹn hoặc thông tin riêng tư bị lộ.
- Các thao tác đặt lịch và gửi câu hỏi luôn yêu cầu xác thực.

TODO: Cần insert Activity diagram Guest User tra cứu bác sĩ và chuyển đổi đăng nhập/đăng ký.

### 3.2. Nhóm use case Authentication & Account Access

**Mục tiêu**

Nhóm use case này bao phủ đăng ký tài khoản Patient, đăng nhập/đăng xuất của Patient, Doctor, Administrator và phân quyền sau đăng nhập. Mục tiêu là bảo đảm mỗi người dùng truy cập đúng khu vực chức năng theo vai trò.

**Actor**

- Actor chính: Guest User, Patient, Doctor, Administrator.
- Actor phụ: Authentication/Authorization.

**Tiền điều kiện**

- Hệ thống có form đăng ký/đăng nhập.
- Doctor và Administrator đã có tài khoản hợp lệ theo quy trình quản trị.
- Chính sách role-based access control được áp dụng cho các khu vực chức năng.

**Luồng chính**

1. Guest User chọn đăng ký tài khoản Patient.
2. Hệ thống kiểm tra dữ liệu đăng ký như email, mật khẩu và thông tin bắt buộc.
3. Patient đăng nhập bằng thông tin hợp lệ.
4. Doctor hoặc Administrator đăng nhập bằng tài khoản được cấp.
5. Hệ thống xác thực thông tin đăng nhập và tạo phiên truy cập hợp lệ.
6. Hệ thống xác định vai trò của người dùng.
7. Người dùng được chuyển đến khu vực phù hợp: Patient portal, Doctor workspace hoặc Admin dashboard.
8. Người dùng chọn đăng xuất, hệ thống kết thúc phiên sử dụng.

**Luồng phụ**

- Nếu email, mật khẩu hoặc dữ liệu đăng ký không hợp lệ, hệ thống hiển thị lỗi validation.
- Nếu thông tin đăng nhập sai, hệ thống từ chối đăng nhập và không tạo phiên truy cập.
- Nếu người dùng truy cập chức năng ngoài vai trò, hệ thống từ chối quyền truy cập.
- Nếu phiên đăng nhập hết hạn, hệ thống yêu cầu đăng nhập lại.

**Hậu điều kiện**

- Tài khoản Patient hợp lệ được tạo khi đăng ký thành công.
- Người dùng đăng nhập được phân quyền đúng vai trò.
- Phiên đăng xuất kết thúc truy cập vào các chức năng được bảo vệ.

TODO: Cần insert Activity diagram đăng ký/đăng nhập.

### 3.3. Nhóm use case Patient Profile & Doctor Profile

**Mục tiêu**

Nhóm use case này bao phủ UC-P-04 và UC-D-02, cho phép Patient quản lý hồ sơ sức khỏe cá nhân và Doctor quản lý hồ sơ chuyên môn. Dữ liệu hồ sơ sức khỏe được xem là dữ liệu nhạy cảm, cần kiểm soát quyền truy cập chặt chẽ.

**Actor**

- Actor chính: Patient, Doctor.
- Actor phụ: Administrator nếu hệ thống hỗ trợ duyệt hoặc quản lý hồ sơ bác sĩ.

**Tiền điều kiện**

- Patient hoặc Doctor đã đăng nhập.
- Người dùng chỉ thao tác trên hồ sơ thuộc phạm vi quyền của mình.
- Các trường dữ liệu bắt buộc và định dạng hợp lệ được xác định trước.

**Luồng chính**

1. Patient truy cập màn hình hồ sơ sức khỏe cá nhân.
2. Patient cập nhật thông tin cá nhân, liên hệ, ngày sinh, giới tính và thông tin sức khỏe cần thiết.
3. Hệ thống kiểm tra dữ liệu nhập và lưu hồ sơ cho đúng Patient.
4. Doctor truy cập màn hình hồ sơ chuyên môn.
5. Doctor cập nhật thông tin chuyên khoa, kinh nghiệm, mô tả tư vấn và lịch làm việc nếu có.
6. Administrator có thể xem, quản lý hoặc duyệt hồ sơ bác sĩ nếu quy trình hệ thống yêu cầu.

**Luồng phụ**

- Nếu dữ liệu thiếu hoặc sai định dạng, hệ thống hiển thị lỗi validation.
- Nếu Patient cố truy cập hồ sơ của Patient khác, hệ thống từ chối.
- Nếu Doctor cập nhật thông tin không hợp lệ hoặc chưa được duyệt, hệ thống hiển thị trạng thái phù hợp.

**Hậu điều kiện**

- Hồ sơ Patient hoặc Doctor được cập nhật đúng phạm vi quyền.
- Dữ liệu sức khỏe không bị hiển thị cho người không có quyền.
- Hồ sơ Doctor sẵn sàng phục vụ tra cứu nếu đủ điều kiện public.

TODO: Cần insert Activity diagram quản lý hồ sơ nếu cần.

### 3.4. Nhóm use case Specialty & Doctor Discovery

**Mục tiêu**

Nhóm use case này bao phủ UC-G-02, UC-G-03, UC-G-04, UC-P-05, UC-P-06 và UC-A-04. Hệ thống cho phép Guest/Patient tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa, đồng thời cho phép Administrator quản lý danh mục chuyên khoa.

**Actor**

- Actor chính: Guest User, Patient, Administrator.
- Actor phụ: Doctor.

**Tiền điều kiện**

- Hệ thống có danh mục chuyên khoa.
- Bác sĩ có hồ sơ chuyên môn và trạng thái hoạt động phù hợp.
- Administrator đã đăng nhập khi quản lý chuyên khoa.

**Luồng chính**

1. Guest User hoặc Patient xem danh sách chuyên khoa.
2. Người dùng chọn chuyên khoa hoặc nhập từ khóa tìm kiếm.
3. Hệ thống lọc danh sách bác sĩ theo điều kiện tìm kiếm.
4. Hệ thống chỉ hiển thị bác sĩ active/approved.
5. Người dùng xem chi tiết hồ sơ bác sĩ.
6. Administrator tạo, cập nhật, bật/tắt hoặc quản lý danh mục chuyên khoa.

**Luồng phụ**

- Nếu chuyên khoa không còn hoạt động, hệ thống không dùng chuyên khoa đó để hiển thị public.
- Nếu không có bác sĩ phù hợp, hệ thống hiển thị danh sách rỗng.
- Nếu Administrator nhập dữ liệu chuyên khoa trùng hoặc không hợp lệ, hệ thống trả lỗi validation.

**Hậu điều kiện**

- Người dùng tìm được bác sĩ phù hợp theo dữ liệu public.
- Danh mục chuyên khoa được quản lý tập trung.
- Dữ liệu bác sĩ hiển thị đúng trạng thái và phạm vi công khai.

TODO: Cần insert Activity diagram tìm kiếm bác sĩ theo chuyên khoa.

### 3.5. Nhóm use case Health Question

**Mục tiêu**

Nhóm use case này bao phủ UC-P-07, UC-P-11, UC-D-03, UC-D-04 và UC-A-06. Patient gửi câu hỏi sức khỏe, Doctor xem và phản hồi câu hỏi thuộc phạm vi xử lý, Administrator kiểm duyệt nội dung nếu hệ thống hỗ trợ.

**Actor**

- Actor chính: Patient, Doctor.
- Actor phụ: Administrator, Notification Service.

**Tiền điều kiện**

- Patient đã đăng nhập để gửi câu hỏi.
- Doctor đã đăng nhập để xem và phản hồi câu hỏi.
- Câu hỏi có trạng thái cho phép phản hồi.
- Quyền truy cập theo vai trò và phạm vi dữ liệu được áp dụng.

**Luồng chính**

1. Patient mở form gửi câu hỏi sức khỏe.
2. Patient nhập tiêu đề, nội dung và có thể chọn bác sĩ nếu hệ thống cho phép.
3. Hệ thống kiểm tra dữ liệu câu hỏi và lưu với trạng thái chờ xử lý.
4. Doctor xem danh sách câu hỏi được phân công hoặc có thể xử lý.
5. Doctor chọn câu hỏi và nhập nội dung phản hồi.
6. Hệ thống kiểm tra nội dung phản hồi.
7. Hệ thống lưu phản hồi, cập nhật trạng thái câu hỏi và thông báo cho Patient.
8. Patient xem phản hồi trong danh sách câu hỏi của chính mình.
9. Administrator kiểm duyệt nội dung khi phát sinh yêu cầu kiểm duyệt.

**Luồng phụ**

- Guest User không được gửi câu hỏi.
- Patient không được xem câu hỏi của Patient khác.
- Doctor không được phản hồi câu hỏi ngoài phạm vi được phân công hoặc được phép xử lý.
- Nếu nội dung câu hỏi hoặc phản hồi rỗng, hệ thống trả lỗi validation.
- Nếu câu hỏi đã đóng hoặc đã xử lý xong, hệ thống có thể từ chối phản hồi tiếp theo.

**Hậu điều kiện**

- Câu hỏi và phản hồi được lưu đúng trạng thái.
- Patient xem được phản hồi thuộc về mình.
- Doctor chỉ xử lý dữ liệu trong phạm vi được phép.
- Nội dung sức khỏe được bảo vệ như dữ liệu nhạy cảm.

TODO: Cần insert Activity diagram Patient gửi câu hỏi và Doctor phản hồi.

### 3.6. Nhóm use case Appointment Management

**Mục tiêu**

Nhóm use case này bao phủ UC-P-08, UC-P-09, UC-D-05, UC-D-06, UC-A-05 và UC-P-15. Patient đặt lịch và xem lịch hẹn, Doctor quản lý lịch tư vấn, Administrator giám sát lịch hẹn, hệ thống gửi nhắc lịch qua Notification Service.

**Actor**

- Actor chính: Patient, Doctor, Administrator.
- Actor phụ: Notification Service.

**Tiền điều kiện**

- Patient đã đăng nhập.
- Bác sĩ được chọn đang hoạt động và có thể nhận lịch.
- Slot tư vấn chưa bị trùng theo quy tắc hệ thống.
- Notification Service sẵn sàng hoặc có cơ chế ghi nhận lỗi gửi thông báo.

**Luồng chính**

1. Patient chọn bác sĩ và khung giờ tư vấn.
2. Patient nhập lý do tư vấn hoặc thông tin cần thiết.
3. Hệ thống kiểm tra dữ liệu đặt lịch và quyền Patient.
4. Hệ thống kiểm tra slot của bác sĩ và Patient để tránh trùng lịch.
5. Hệ thống tạo lịch hẹn với trạng thái phù hợp.
6. Patient xem danh sách lịch hẹn sắp tới.
7. Doctor xem các lịch hẹn đã được đặt và quản lý lịch tư vấn.
8. Administrator xem hoặc quản lý lịch hẹn theo quyền.
9. Notification Service gửi xác nhận hoặc nhắc lịch trước giờ hẹn.

**Luồng phụ**

- Nếu slot đã có lịch, hệ thống từ chối tạo lịch hẹn.
- Nếu dữ liệu đặt lịch thiếu hoặc sai định dạng, hệ thống trả lỗi validation.
- Nếu Patient hủy lịch trong điều kiện được phép, hệ thống cập nhật trạng thái lịch.
- Nếu Notification Service gửi thất bại, nghiệp vụ lịch hẹn không bị làm sai; trạng thái gửi có thể được ghi nhận để xử lý sau.

**Hậu điều kiện**

- Lịch hẹn hợp lệ được tạo và gắn với đúng Patient/Doctor.
- Không tạo lịch hẹn trùng slot.
- Patient, Doctor và Administrator chỉ xem lịch theo phạm vi quyền.
- Thông báo lịch hẹn được gửi hoặc ghi nhận trạng thái xử lý.

TODO: Cần insert Activity diagram đặt lịch hẹn tư vấn.

### 3.7. Nhóm use case Consultation Session

**Mục tiêu**

Nhóm use case này bao phủ UC-P-10, UC-D-07, UC-D-08 và UC-E-03. Hệ thống cho phép Doctor bắt đầu phiên tư vấn, Patient tham gia phiên tư vấn, trao đổi qua chat/video và fallback sang chat khi video không khả dụng.

**Actor**

- Actor chính: Patient, Doctor.
- Actor phụ: Video Communication Service, System.

**Tiền điều kiện**

- Appointment hợp lệ và ở trạng thái cho phép tư vấn.
- Patient và Doctor liên quan đến appointment đã đăng nhập.
- Hệ thống có cấu hình kênh tư vấn chat/video theo phạm vi MVP.

**Luồng chính**

1. Doctor mở lịch hẹn và bắt đầu phiên tư vấn.
2. Hệ thống kiểm tra appointment, trạng thái lịch hẹn và quyền của Doctor.
3. Hệ thống khởi tạo session tư vấn.
4. Patient tham gia session theo lịch hẹn của chính mình.
5. Hệ thống kiểm tra quyền của Patient đối với appointment.
6. Doctor và Patient trao đổi qua chat hoặc video.
7. Nếu dùng video, hệ thống thiết lập phiên thông qua Video Communication Service hoặc video mock.
8. Khi kết thúc tư vấn, hệ thống cập nhật trạng thái session theo quy tắc nghiệp vụ.

**Luồng phụ**

- Nếu người tham gia không thuộc appointment, hệ thống từ chối join.
- Nếu appointment chưa đến thời gian cho phép hoặc không ở trạng thái hợp lệ, hệ thống không cho bắt đầu/join session.
- Nếu video lỗi hoặc không khả dụng, hệ thống fallback sang chat nếu thiết kế cho phép.
- Nếu kết nối gián đoạn, hệ thống cần hiển thị trạng thái lỗi rõ ràng.

**Hậu điều kiện**

- Phiên tư vấn chỉ được truy cập bởi đúng Patient và Doctor liên quan.
- Nội dung trao đổi được gắn với appointment/session hợp lệ.
- Trạng thái phiên tư vấn được cập nhật để phục vụ kết quả tư vấn và lịch sử.

TODO: Cần insert Activity diagram phiên tư vấn trực tuyến.

### 3.8. Nhóm use case Consultation Result & Prescription

**Mục tiêu**

Nhóm use case này bao phủ UC-D-09, UC-D-10, UC-P-12, UC-P-13 và UC-D-11. Doctor ghi nhận kết quả tư vấn, tạo đơn thuốc điện tử cơ bản; Patient xem lịch sử, tóm tắt tư vấn và đơn thuốc của mình; Doctor xem lịch sử tư vấn phù hợp quyền.

**Actor**

- Actor chính: Doctor, Patient.
- Actor phụ: System.

**Tiền điều kiện**

- Phiên tư vấn hoặc appointment hợp lệ đã diễn ra.
- Doctor phụ trách có quyền ghi kết quả tư vấn.
- Patient chỉ truy cập kết quả thuộc lịch hẹn của chính mình.

**Luồng chính**

1. Doctor mở phiên tư vấn hoặc lịch hẹn đã thực hiện.
2. Doctor nhập summary/kết luận tư vấn.
3. Doctor nhập đơn thuốc cơ bản nếu cần.
4. Hệ thống kiểm tra dữ liệu summary và đơn thuốc.
5. Hệ thống lưu kết quả tư vấn và đơn thuốc.
6. Patient xem lịch sử tư vấn, tóm tắt và đơn thuốc của chính mình.
7. Doctor xem lịch sử tư vấn của bệnh nhân trong phạm vi được phép.

**Luồng phụ**

- Nếu summary hoặc đơn thuốc thiếu thông tin bắt buộc, hệ thống trả lỗi validation.
- Nếu Doctor không phụ trách appointment, hệ thống từ chối ghi kết quả.
- Nếu Patient truy cập kết quả của người khác, hệ thống từ chối.
- Nếu appointment chưa hoàn tất hoặc chưa có kết quả, hệ thống hiển thị trạng thái phù hợp.

**Hậu điều kiện**

- Kết quả tư vấn và đơn thuốc được lưu đúng appointment.
- Patient xem được dữ liệu của chính mình.
- Doctor chỉ xem lịch sử tư vấn trong phạm vi quyền.
- Dữ liệu tư vấn và đơn thuốc được bảo vệ như dữ liệu sức khỏe.

TODO: Cần insert Activity diagram ghi nhận kết quả tư vấn và đơn thuốc.

### 3.9. Nhóm use case Rating & Feedback

**Mục tiêu**

Nhóm use case này bao phủ UC-P-14 và phần kiểm duyệt rating/comment của Administrator nếu hệ thống hỗ trợ. Patient đánh giá chất lượng tư vấn sau khi phiên tư vấn hoàn tất.

**Actor**

- Actor chính: Patient.
- Actor phụ: Administrator, Doctor.

**Tiền điều kiện**

- Patient đã đăng nhập.
- Consultation hoặc appointment đã hoàn tất.
- Patient là chủ sở hữu lịch hẹn cần đánh giá.

**Luồng chính**

1. Patient mở lịch sử tư vấn đã hoàn tất.
2. Patient chọn chức năng đánh giá.
3. Patient nhập điểm đánh giá và nhận xét.
4. Hệ thống kiểm tra điều kiện được đánh giá và dữ liệu nhập.
5. Hệ thống lưu rating/comment.
6. Administrator có thể xem, ẩn hoặc kiểm duyệt rating/comment nếu có quy trình kiểm duyệt.

**Luồng phụ**

- Nếu consultation chưa completed, hệ thống không cho đánh giá.
- Nếu hệ thống có rule không cho đánh giá trùng, lần đánh giá lặp lại bị từ chối hoặc chuyển sang cập nhật đánh giá cũ.
- Nếu nhận xét chứa nội dung cần kiểm duyệt, hệ thống có thể đưa vào trạng thái chờ duyệt.

**Hậu điều kiện**

- Rating được gắn với đúng Patient, Doctor và consultation/appointment.
- Đánh giá chỉ được tạo khi thỏa điều kiện nghiệp vụ.
- Nội dung đánh giá có thể được quản trị theo quy định nền tảng.

TODO: Cần insert Activity diagram đánh giá tư vấn nếu cần.

### 3.10. Nhóm use case Notification

**Mục tiêu**

Nhóm use case này bao phủ UC-P-15, UC-E-01 và UC-E-02. Hệ thống gửi thông báo khi tạo lịch, nhắc lịch trước giờ hẹn và thông báo khi bác sĩ phản hồi câu hỏi.

**Actor**

- Actor chính: Patient, Doctor.
- Actor phụ: Notification Service, System.

**Tiền điều kiện**

- Có sự kiện nghiệp vụ cần thông báo như tạo lịch, nhắc lịch hoặc có phản hồi câu hỏi.
- Người nhận có thông tin liên hệ hợp lệ nếu kênh email/SMS được sử dụng.
- Hệ thống có cơ chế ghi nhận trạng thái gửi nếu được thiết kế.

**Luồng chính**

1. Hệ thống phát sinh sự kiện cần thông báo.
2. Hệ thống tạo nội dung thông báo phù hợp với loại sự kiện.
3. Notification Service gửi email hoặc SMS cho người nhận.
4. Hệ thống ghi nhận trạng thái gửi nếu có notification log.
5. Người dùng nhận thông báo và truy cập lại hệ thống để xem chi tiết.

**Luồng phụ**

- Nếu gửi email/SMS thất bại, hệ thống ghi nhận trạng thái lỗi và có thể retry theo thiết kế.
- Nếu người nhận thiếu email/số điện thoại, hệ thống không gửi qua kênh tương ứng.
- Lỗi notification không được làm sai dữ liệu nghiệp vụ chính như lịch hẹn hoặc phản hồi câu hỏi.

**Hậu điều kiện**

- Thông báo được gửi hoặc trạng thái lỗi được ghi nhận.
- Nghiệp vụ chính vẫn giữ trạng thái đúng.
- Có dữ liệu phục vụ kiểm tra trạng thái gửi thông báo nếu hệ thống hỗ trợ log.

TODO: Cần insert Activity diagram notification flow nếu cần.

### 3.11. Nhóm use case Administration & Reporting

**Mục tiêu**

Nhóm use case này bao phủ UC-A-01 đến UC-A-08. Administrator đăng nhập, quản lý bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, kiểm duyệt nội dung, xem dashboard thống kê, theo dõi người dùng hoạt động và số lượng phiên tư vấn.

**Actor**

- Actor chính: Administrator.
- Actor phụ: Patient, Doctor, System.

**Tiền điều kiện**

- Administrator có tài khoản hợp lệ và đăng nhập thành công.
- Các chức năng quản trị được bảo vệ bằng phân quyền.
- Dữ liệu người dùng, chuyên khoa, lịch hẹn và phiên tư vấn có sẵn để quản lý hoặc thống kê.

**Luồng chính**

1. Administrator đăng nhập vào khu vực quản trị.
2. Administrator quản lý tài khoản bác sĩ, gồm xem, cập nhật trạng thái hoặc duyệt hồ sơ nếu hệ thống hỗ trợ.
3. Administrator quản lý tài khoản bệnh nhân khi cần hỗ trợ vận hành.
4. Administrator tạo, cập nhật hoặc quản lý chuyên khoa.
5. Administrator xem và quản lý lịch hẹn trên hệ thống.
6. Administrator kiểm duyệt nội dung tư vấn, phản hồi hoặc đánh giá khi có yêu cầu.
7. Administrator xem dashboard thống kê hoạt động.
8. Administrator theo dõi người dùng đang hoạt động và số lượng phiên tư vấn ở mức tổng quan.

**Luồng phụ**

- Nếu tài khoản không có vai trò Administrator, hệ thống từ chối truy cập khu vực quản trị.
- Nếu dữ liệu quản trị không hợp lệ, hệ thống trả lỗi validation.
- Nếu thao tác quản trị ảnh hưởng dữ liệu nhạy cảm, hệ thống cần kiểm soát quyền và ghi nhận audit nếu có.
- Nếu dashboard không có dữ liệu trong khoảng lọc, hệ thống hiển thị trạng thái rỗng.

**Hậu điều kiện**

- Dữ liệu quản trị được cập nhật đúng quyền.
- Nội dung cần kiểm duyệt được xử lý theo quy trình.
- Dashboard phản ánh dữ liệu hệ thống ở mức thống kê.
- Các thao tác quản trị nhạy cảm có cơ sở để kiểm tra phân quyền và audit.

TODO: Cần insert Activity diagram Administrator quản lý hệ thống.

TODO: Cần insert Activity diagram dashboard/reporting nếu cần.

## 4. Định nghĩa các yêu cầu chất lượng phần mềm

### 4.1. Mục tiêu

Việc định nghĩa yêu cầu chất lượng phần mềm nhằm bảo đảm nền tảng tư vấn sức khỏe trực tuyến hoạt động đúng theo SRS, an toàn, bảo mật, dễ sử dụng và có thể kiểm thử. Các yêu cầu chất lượng được xác định từ góc nhìn nghiệp vụ, vận hành và phát triển để làm cơ sở thiết kế test case ở Chương III.

Do hệ thống xử lý dữ liệu sức khỏe, hồ sơ cá nhân, lịch hẹn, phiên tư vấn và đơn thuốc, báo cáo đặc biệt chú trọng các yêu cầu về bảo mật, quyền riêng tư, phân quyền theo vai trò, quyền sở hữu dữ liệu, kiểm soát trạng thái lịch hẹn và tính đúng đắn của phiên tư vấn trực tuyến. Những yêu cầu này cần được mô tả theo hướng có thể kiểm thử, có điều kiện đầu vào rõ ràng và có kết quả mong đợi cụ thể.

### 4.2. Yêu cầu từ môi trường nghiệp vụ

| Nhóm chức năng | Req-ID | Nội dung yêu cầu | Stakeholder |
|---|---|---|---|
| Public Access & Doctor Discovery | QA-PA-01 | Hệ thống phải cho phép Guest User xem trang chủ, danh sách chuyên khoa, danh sách bác sĩ và hồ sơ công khai của bác sĩ mà không cần đăng nhập. | Guest User |
| Public Access & Doctor Discovery | QA-PA-02 | Hệ thống chỉ hiển thị bác sĩ active/approved trong khu vực public. | Guest User, Patient, Administrator |
| Public Access & Doctor Discovery | QA-PA-03 | Hệ thống phải cho phép tìm kiếm bác sĩ theo chuyên khoa hoặc từ khóa và hiển thị trạng thái không có kết quả khi không tìm thấy dữ liệu phù hợp. | Guest User, Patient |
| Authentication & Authorization | QA-AU-01 | Hệ thống phải yêu cầu Patient đăng nhập trước khi đặt lịch, gửi câu hỏi, tham gia tư vấn, xem kết quả tư vấn hoặc đánh giá. | Patient |
| Authentication & Authorization | QA-AU-02 | Hệ thống phải xác định đúng vai trò sau đăng nhập và chuyển người dùng đến khu vực phù hợp với Patient, Doctor hoặc Administrator. | Patient, Doctor, Administrator |
| Authentication & Authorization | QA-AU-03 | Hệ thống phải từ chối truy cập khi người dùng không có quyền thực hiện chức năng hoặc truy cập dữ liệu ngoài phạm vi. | Tất cả actor |
| Patient Profile | QA-PP-01 | Hệ thống phải cho phép Patient tạo và cập nhật hồ sơ sức khỏe cá nhân sau khi đăng nhập. | Patient |
| Patient Profile | QA-PP-02 | Hệ thống chỉ cho phép Patient xem và cập nhật hồ sơ của chính mình. | Patient |
| Patient Profile | QA-PP-03 | Hệ thống phải kiểm tra dữ liệu hồ sơ như ngày sinh, giới tính, số điện thoại và thông tin bắt buộc trước khi lưu. | Patient |
| Doctor Profile | QA-DP-01 | Hệ thống phải cho phép Doctor cập nhật hồ sơ chuyên môn, chuyên khoa, kinh nghiệm và lịch làm việc nếu có. | Doctor |
| Doctor Profile | QA-DP-02 | Hồ sơ Doctor chỉ được hiển thị công khai khi đáp ứng điều kiện hoạt động/được duyệt theo quy tắc hệ thống. | Doctor, Administrator |
| Doctor Profile | QA-DP-03 | Administrator phải có khả năng quản lý trạng thái hồ sơ bác sĩ nếu quy trình duyệt bác sĩ được áp dụng. | Administrator |
| Specialty Management | QA-SM-01 | Administrator phải tạo, cập nhật và quản lý danh mục chuyên khoa. | Administrator |
| Specialty Management | QA-SM-02 | Hệ thống không được sử dụng chuyên khoa inactive để hiển thị hoặc lọc dữ liệu public nếu chuyên khoa đó đã bị tắt. | Guest User, Patient, Administrator |
| Specialty Management | QA-SM-03 | Hệ thống phải kiểm tra dữ liệu chuyên khoa để tránh tên rỗng, trùng hoặc không hợp lệ theo quy tắc nghiệp vụ. | Administrator |
| Health Question | QA-HQ-01 | Hệ thống chỉ cho phép Patient đã đăng nhập gửi câu hỏi sức khỏe. | Patient |
| Health Question | QA-HQ-02 | Hệ thống chỉ cho phép Patient xem câu hỏi và phản hồi thuộc về chính mình. | Patient |
| Health Question | QA-HQ-03 | Hệ thống chỉ cho phép Doctor xem và phản hồi câu hỏi thuộc phạm vi được phân công hoặc có thể xử lý. | Doctor |
| Health Question | QA-HQ-04 | Khi Doctor phản hồi câu hỏi hợp lệ, hệ thống phải lưu phản hồi và cập nhật trạng thái câu hỏi theo đúng quy tắc. | Doctor, Patient |
| Appointment | QA-AP-01 | Hệ thống phải cho phép Patient đặt lịch với bác sĩ khi slot còn trống và dữ liệu đặt lịch hợp lệ. | Patient |
| Appointment | QA-AP-02 | Hệ thống phải ngăn đặt trùng lịch hoặc chồng lấn khung giờ của Patient và Doctor. | Patient, Doctor |
| Appointment | QA-AP-03 | Patient chỉ được xem, hủy hoặc thao tác lịch hẹn của chính mình theo điều kiện nghiệp vụ. | Patient |
| Appointment | QA-AP-04 | Doctor và Administrator phải xem/quản lý lịch hẹn theo đúng phạm vi quyền. | Doctor, Administrator |
| Consultation Session | QA-CS-01 | Hệ thống chỉ cho đúng Patient và Doctor của appointment hợp lệ tham gia consultation session. | Patient, Doctor |
| Consultation Session | QA-CS-02 | Hệ thống phải kiểm tra trạng thái appointment trước khi cho phép bắt đầu hoặc tham gia phiên tư vấn. | Patient, Doctor |
| Consultation Session | QA-CS-03 | Hệ thống phải hỗ trợ trao đổi qua chat/video theo phạm vi MVP và fallback sang chat khi video không khả dụng nếu thiết kế cho phép. | Patient, Doctor |
| Consultation Result & Prescription | QA-CR-01 | Doctor phải ghi nhận được tóm tắt/kết quả tư vấn cho appointment thuộc phạm vi phụ trách. | Doctor |
| Consultation Result & Prescription | QA-CR-02 | Doctor phải tạo được đơn thuốc điện tử cơ bản với dữ liệu hợp lệ nếu cần. | Doctor |
| Consultation Result & Prescription | QA-CR-03 | Patient chỉ được xem kết quả tư vấn và prescription của chính mình. | Patient |
| Consultation Result & Prescription | QA-CR-04 | Doctor chỉ được xem lịch sử tư vấn của Patient trong phạm vi được phép. | Doctor |
| Rating & Feedback | QA-RF-01 | Hệ thống chỉ cho phép Patient đánh giá sau khi consultation hoặc appointment đã completed. | Patient |
| Rating & Feedback | QA-RF-02 | Hệ thống phải kiểm soát đánh giá trùng nếu quy tắc nghiệp vụ không cho phép đánh giá nhiều lần. | Patient |
| Rating & Feedback | QA-RF-03 | Administrator có thể xem, ẩn hoặc kiểm duyệt rating/comment nếu hệ thống hỗ trợ kiểm duyệt. | Administrator |
| Notification | QA-NT-01 | Hệ thống phải gửi thông báo xác nhận hoặc nhắc lịch nếu notification được cấu hình. | Patient, Doctor |
| Notification | QA-NT-02 | Hệ thống phải gửi thông báo khi bác sĩ phản hồi câu hỏi sức khỏe nếu kênh thông báo được bật. | Patient |
| Notification | QA-NT-03 | Lỗi gửi notification không được làm sai trạng thái nghiệp vụ chính như lịch hẹn hoặc phản hồi câu hỏi. | Operator, Patient, Doctor |
| Administration | QA-AD-01 | Administrator phải quản lý được tài khoản bác sĩ và bệnh nhân theo quyền. | Administrator |
| Administration | QA-AD-02 | Administrator phải quản lý được chuyên khoa, lịch hẹn và nội dung cần kiểm duyệt theo quyền. | Administrator |
| Administration | QA-AD-03 | Các thao tác quản trị nhạy cảm phải được kiểm soát quyền và có cơ sở ghi nhận audit nếu hệ thống hỗ trợ. | Administrator, Operator |
| Reporting/Dashboard | QA-RP-01 | Administrator phải xem được dashboard thống kê hoạt động hệ thống theo dữ liệu hiện có. | Administrator |
| Reporting/Dashboard | QA-RP-02 | Dashboard phải hỗ trợ hiển thị số liệu người dùng, bác sĩ, lịch hẹn, phiên tư vấn hoặc trạng thái hệ thống theo phạm vi MVP. | Administrator |
| Reporting/Dashboard | QA-RP-03 | Khi không có dữ liệu trong phạm vi lọc, hệ thống phải hiển thị trạng thái rỗng rõ ràng. | Administrator |

### 4.3. Yêu cầu từ môi trường vận hành

- **Bảo mật:** Các chức năng được bảo vệ phải yêu cầu xác thực hợp lệ. Hệ thống không được trả về mật khẩu, token bí mật hoặc thông tin xác thực nhạy cảm trong response.
- **Quyền riêng tư dữ liệu sức khỏe:** Hồ sơ sức khỏe, câu hỏi, phản hồi, lịch sử tư vấn, kết quả tư vấn và đơn thuốc chỉ được hiển thị cho đúng người có quyền.
- **Phân quyền và ownership:** Guest chỉ xem dữ liệu public; Patient chỉ thao tác dữ liệu của chính mình; Doctor chỉ thao tác dữ liệu được phân công/phụ trách; Administrator thao tác theo quyền quản trị.
- **Validation dữ liệu đầu vào:** Các form/API đăng ký, đăng nhập, hồ sơ, chuyên khoa, câu hỏi, đặt lịch, kết quả tư vấn, đơn thuốc và đánh giá phải kiểm tra dữ liệu bắt buộc, định dạng và trạng thái nghiệp vụ.
- **Xử lý lỗi nhất quán:** Lỗi validation, xác thực, phân quyền, không tìm thấy dữ liệu, trùng lịch và lỗi dịch vụ ngoài cần có thông báo rõ ràng, không làm lộ chi tiết nội bộ nhạy cảm.
- **Hiệu năng MVP:** Các API thường dùng như public discovery, đăng nhập, danh sách lịch hẹn, danh sách câu hỏi và dashboard cần phản hồi trong ngưỡng chấp nhận của phạm vi MVP.
- **Tính sẵn sàng:** Hệ thống cần duy trì các luồng chính như đăng nhập, đặt lịch, gửi câu hỏi và tham gia tư vấn ở trạng thái ổn định trong điều kiện vận hành thông thường.
- **Fallback chat/video:** Nếu video không khả dụng trong phiên tư vấn, hệ thống có thể chuyển sang chat để bảo đảm người dùng vẫn có kênh trao đổi.
- **Logging/audit:** Hệ thống cần có log phục vụ vận hành và audit cho các thao tác nhạy cảm như quản trị, truy cập dữ liệu sức khỏe, cập nhật kết quả tư vấn hoặc đơn thuốc nếu được triển khai.
- **Backup/khôi phục nếu có:** Dữ liệu nghiệp vụ quan trọng như tài khoản, hồ sơ, lịch hẹn, kết quả tư vấn và đơn thuốc cần có phương án sao lưu/khôi phục theo môi trường triển khai.
- **Responsive usability:** Giao diện cần dễ sử dụng trên desktop và mobile, đặc biệt với các luồng public discovery, đặt lịch, gửi câu hỏi, tư vấn trực tuyến và dashboard quản trị.

### 4.4. Yêu cầu từ môi trường phát triển

- **Frontend React module hóa:** Frontend cần tổ chức theo module hoặc feature như auth, patient, doctor, admin, discovery, appointment, question và consultation để dễ bảo trì và kiểm thử.
- **Backend Node.js/NestJS:** Backend được trình bày theo hướng Node.js/NestJS, tổ chức theo module nghiệp vụ rõ ràng; nếu môi trường triển khai dùng Express thì vẫn cần bảo đảm cấu trúc route/service/middleware có trách nhiệm rõ ràng.
- **REST API rõ ràng:** API cần có endpoint, phương thức, request body, response và mã lỗi rõ ràng, hỗ trợ traceability từ use case đến test case.
- **Database quan hệ:** Database quan hệ cần thể hiện được các quan hệ chính giữa user, profile, specialty, doctor, question, appointment, consultation, prescription, rating, notification và audit.
- **API documentation:** Hệ thống cần có tài liệu API bằng Swagger/OpenAPI hoặc tài liệu tương đương để hỗ trợ phát triển, tích hợp và kiểm thử API.
- **Cấu hình môi trường:** Các thông tin như database URL, JWT secret, email/SMS configuration, video configuration và file storage configuration phải được quản lý bằng biến môi trường hoặc cơ chế cấu hình an toàn.
- **Testability:** Thiết kế cần cho phép kiểm thử unit, integration, API và E2E; các nghiệp vụ quan trọng phải có input/output và rule đủ rõ để tạo test case.
- **Automation testing readiness:** Giao diện và API cần có định danh ổn định, dữ liệu seed phù hợp và môi trường chạy test tách biệt để hỗ trợ Playwright E2E khi triển khai.
- **CI/CD readiness:** Dự án cần sẵn sàng cho quy trình build, lint, type-check, unit test, API test và E2E test trong pipeline nếu có.
- **Coding convention và maintainability:** Code cần tuân thủ quy ước đặt tên, tách trách nhiệm rõ ràng, hạn chế trùng lặp, có validation ở biên hệ thống và có xử lý lỗi thống nhất.

### 4.5. Mapping yêu cầu chất lượng và kỹ thuật kiểm thử

| Yêu cầu chất lượng | Kỹ thuật kiểm thử phù hợp | Ví dụ test case |
|---|---|---|
| Public access | Black-box/API test | Guest gọi API danh sách bác sĩ public không cần token. |
| Doctor public visibility | Black-box/API test | Bác sĩ inactive/unapproved không xuất hiện trong danh sách public. |
| Authentication | Equivalence Partitioning | Đăng nhập với email/mật khẩu hợp lệ và không hợp lệ. |
| Authorization | Decision Table | Patient không xem được dữ liệu của Patient khác. |
| Role-based redirect | E2E/manual test | Patient, Doctor, Administrator đăng nhập và được chuyển đúng khu vực. |
| Patient profile ownership | Security/negative test | Patient A không cập nhật được hồ sơ Patient B. |
| Specialty management | API/manual test | Admin tạo chuyên khoa với tên hợp lệ; tên rỗng bị từ chối. |
| Health question access | Decision Table | Doctor chỉ xem/trả lời câu hỏi thuộc phạm vi được phép. |
| Appointment conflict | Boundary/Decision Table | Đặt lịch trùng slot bác sĩ hoặc Patient bị từ chối. |
| Appointment validation | Equivalence Partitioning | Thiếu doctorId, scheduledAt hoặc reason thì hệ thống trả lỗi validation. |
| Consultation session access | Security/negative test | Người không thuộc appointment không join được session. |
| Video fallback | E2E/API integration test | Khi video không khả dụng, session chuyển sang chat nếu thiết kế cho phép. |
| Prescription privacy | Security/negative test | Patient chỉ xem được prescription của chính mình. |
| Rating rule | Decision Table | Patient chỉ đánh giá sau khi consultation completed. |
| Notification | Integration/API test | Tạo lịch hẹn sinh notification nhắc lịch nếu kênh thông báo được cấu hình. |
| Input validation | Equivalence Partitioning | Email sai định dạng hoặc nội dung câu hỏi rỗng bị từ chối. |
| Privacy | Security/negative test | Response không chứa passwordHash, token bí mật hoặc dữ liệu sức khỏe ngoài quyền. |
| Error handling | Black-box/API test | Lỗi validation, unauthorized, forbidden và conflict có response nhất quán. |
| Performance | Performance test | API discovery phản hồi dưới ngưỡng MVP ở mức tải phù hợp. |
| UI responsive | E2E/manual test | Kiểm tra luồng tìm bác sĩ, đặt lịch và dashboard trên desktop/mobile. |

## 5. Thiết kế hệ thống

### 5.1. Kiến trúc hệ thống

Hệ thống được thiết kế theo kiến trúc nhiều lớp, trong đó frontend React đảm nhiệm phần giao diện, backend Node.js/NestJS xử lý nghiệp vụ và API, cơ sở dữ liệu quan hệ lưu trữ dữ liệu chính. Cách tổ chức này phù hợp với phạm vi MVP của nền tảng tư vấn sức khỏe trực tuyến, đồng thời giúp tách biệt trách nhiệm giữa giao diện, nghiệp vụ, dữ liệu và tích hợp dịch vụ ngoài.

**Presentation Layer - React frontend**

Presentation Layer là lớp giao diện người dùng, phục vụ Guest User, Patient, Doctor và Administrator. Lớp này chịu trách nhiệm hiển thị trang public, form đăng ký/đăng nhập, hồ sơ, tìm kiếm bác sĩ, đặt lịch, câu hỏi sức khỏe, phiên tư vấn, đơn thuốc, đánh giá và dashboard. Frontend sử dụng React 18, TypeScript, Vite, React Router v6, Redux Toolkit, Redux Saga, Axios, PrimeReact, Tailwind CSS, Formik, Yup, i18next và Recharts.

**Business Logic/API Layer - Node.js backend**

Business Logic/API Layer là backend Node.js/NestJS, tiếp nhận request từ frontend, kiểm tra xác thực, phân quyền, validation và thực thi nghiệp vụ. Lớp này bao gồm các module chính như authentication, profile, specialty, discovery, question, appointment, consultation, prescription, rating, notification, admin và reporting. Backend cung cấp REST API và có thể hỗ trợ WebSocket/Socket.IO cho phiên tư vấn chat/video.

**Data Access Layer - Relational Database**

Data Access Layer sử dụng PostgreSQL thông qua Prisma ORM. Cơ sở dữ liệu lưu trữ tài khoản, hồ sơ bệnh nhân, hồ sơ bác sĩ, chuyên khoa, câu hỏi, phản hồi, lịch hẹn, phiên tư vấn, tin nhắn, kết quả tư vấn, đơn thuốc, đánh giá, thông báo, audit log, outbox event và file metadata. Vì hệ thống xử lý dữ liệu sức khỏe, lớp dữ liệu cần bảo đảm ràng buộc quan hệ, ownership và tính nhất quán trạng thái nghiệp vụ.

**External Integration Layer**

External Integration Layer kết nối các dịch vụ bên ngoài hoặc thành phần hỗ trợ gồm Notification Service, Video Communication Service và File Storage Service. Notification Service phục vụ gửi email/SMS xác nhận hoặc nhắc lịch. Video Communication Service phục vụ phiên tư vấn video hoặc video mock trong phạm vi MVP. File Storage Service phục vụ lưu trữ và truy xuất tệp tải lên như tài liệu y tế, hình ảnh hoặc tệp đính kèm liên quan đến tư vấn.

**Cross-cutting concerns**

Các mối quan tâm xuyên suốt gồm authentication, authorization, validation, logging, audit và error handling. Authentication sử dụng JWT để xác thực phiên đăng nhập. Authorization kết hợp role-based access control và ownership để bảo đảm người dùng chỉ thao tác đúng phạm vi dữ liệu. Validation được áp dụng cho form/API. Logging và audit hỗ trợ vận hành, truy vết hành động nhạy cảm. Error handling cần nhất quán, rõ ràng và không làm lộ dữ liệu nội bộ.

TODO: Cần insert hình kiến trúc hệ thống.

### 5.2. Thiết kế giao diện/Form theo nhóm chức năng

#### FO-001: Trang chủ public

- **Use case sử dụng:** UC-G-01, UC-G-05.
- **Actor:** Guest User, Patient.
- **Mô tả xử lý:** Hiển thị thông tin tổng quan nền tảng, các chuyên khoa nổi bật, bác sĩ nổi bật hoặc đang hoạt động và lối vào tìm kiếm bác sĩ, đăng nhập, đăng ký.

TODO: Cần insert hình giao diện trang chủ public.

#### FO-002: Danh sách chuyên khoa

- **Use case sử dụng:** UC-G-02, UC-P-05.
- **Actor:** Guest User, Patient.
- **Mô tả xử lý:** Hiển thị danh sách chuyên khoa đang hoạt động. Người dùng chọn chuyên khoa để lọc danh sách bác sĩ tương ứng.

TODO: Cần insert hình giao diện danh sách chuyên khoa.

#### FO-003: Danh sách bác sĩ

- **Use case sử dụng:** UC-G-03, UC-G-05, UC-P-05.
- **Actor:** Guest User, Patient.
- **Mô tả xử lý:** Hiển thị danh sách bác sĩ theo chuyên khoa hoặc từ khóa. Giao diện cần hỗ trợ tìm kiếm, lọc và trạng thái không có kết quả.

TODO: Cần insert hình giao diện danh sách bác sĩ.

#### FO-004: Chi tiết bác sĩ

- **Use case sử dụng:** UC-G-04, UC-P-06.
- **Actor:** Guest User, Patient.
- **Mô tả xử lý:** Hiển thị hồ sơ công khai của bác sĩ, gồm thông tin chuyên môn, chuyên khoa, kinh nghiệm và thao tác đặt lịch/gửi câu hỏi. Nếu Guest chọn thao tác được bảo vệ, hệ thống chuyển sang đăng nhập/đăng ký.

TODO: Cần insert hình giao diện chi tiết bác sĩ.

#### FO-005: Đăng ký/đăng nhập

- **Use case sử dụng:** UC-P-01, UC-P-02, UC-P-03, UC-D-01, UC-A-01.
- **Actor:** Guest User, Patient, Doctor, Administrator.
- **Mô tả xử lý:** Cho phép Patient đăng ký tài khoản, các actor đăng nhập/đăng xuất. Sau đăng nhập, hệ thống điều hướng theo vai trò.

TODO: Cần insert hình giao diện đăng ký/đăng nhập.

#### FO-006: Hồ sơ bệnh nhân

- **Use case sử dụng:** UC-P-04.
- **Actor:** Patient.
- **Mô tả xử lý:** Patient xem và cập nhật hồ sơ sức khỏe cá nhân. Form cần validation dữ liệu bắt buộc, ngày sinh, giới tính, số điện thoại và thông tin sức khỏe.

TODO: Cần insert hình giao diện hồ sơ bệnh nhân.

#### FO-007: Hồ sơ bác sĩ

- **Use case sử dụng:** UC-D-02.
- **Actor:** Doctor, Administrator.
- **Mô tả xử lý:** Doctor cập nhật hồ sơ chuyên môn, chuyên khoa, kinh nghiệm, mô tả tư vấn và lịch làm việc. Administrator có thể quản lý hoặc duyệt hồ sơ nếu quy trình hệ thống hỗ trợ.

TODO: Cần insert hình giao diện hồ sơ bác sĩ.

#### FO-008: Đặt lịch tư vấn

- **Use case sử dụng:** UC-P-08.
- **Actor:** Patient.
- **Mô tả xử lý:** Patient chọn bác sĩ, ngày giờ, thời lượng và lý do tư vấn. Hệ thống kiểm tra slot, quyền Patient và dữ liệu đầu vào trước khi tạo lịch hẹn.

TODO: Cần insert hình giao diện đặt lịch tư vấn.

#### FO-009: Gửi câu hỏi sức khỏe

- **Use case sử dụng:** UC-P-07.
- **Actor:** Patient.
- **Mô tả xử lý:** Patient nhập tiêu đề, nội dung câu hỏi và chọn bác sĩ nếu luồng nghiệp vụ cho phép. Nội dung câu hỏi là dữ liệu sức khỏe nhạy cảm.

TODO: Cần insert hình giao diện gửi câu hỏi sức khỏe.

#### FO-010: Danh sách câu hỏi/phản hồi

- **Use case sử dụng:** UC-P-11, UC-D-03, UC-D-04.
- **Actor:** Patient, Doctor.
- **Mô tả xử lý:** Patient xem câu hỏi và phản hồi của chính mình. Doctor xem câu hỏi được phân công hoặc có thể xử lý, nhập phản hồi và cập nhật trạng thái câu hỏi.

TODO: Cần insert hình giao diện danh sách câu hỏi/phản hồi.

#### FO-011: Lịch hẹn của bệnh nhân

- **Use case sử dụng:** UC-P-09, UC-P-15.
- **Actor:** Patient.
- **Mô tả xử lý:** Patient xem lịch hẹn sắp tới, trạng thái lịch, thông tin bác sĩ và thao tác hủy lịch nếu điều kiện nghiệp vụ cho phép.

TODO: Cần insert hình giao diện lịch hẹn của bệnh nhân.

#### FO-012: Lịch tư vấn của bác sĩ

- **Use case sử dụng:** UC-D-05, UC-D-06.
- **Actor:** Doctor.
- **Mô tả xử lý:** Doctor xem lịch hẹn được đặt, quản lý trạng thái lịch và chuẩn bị bắt đầu phiên tư vấn.

TODO: Cần insert hình giao diện lịch tư vấn của bác sĩ.

#### FO-013: Phiên tư vấn chat/video

- **Use case sử dụng:** UC-P-10, UC-D-07, UC-D-08.
- **Actor:** Patient, Doctor.
- **Mô tả xử lý:** Patient và Doctor tham gia phiên tư vấn gắn với appointment hợp lệ. Giao diện hỗ trợ chat/video theo MVP và fallback chat khi video không khả dụng.

TODO: Cần insert hình giao diện phiên tư vấn chat/video.

#### FO-014: Kết quả tư vấn/đơn thuốc

- **Use case sử dụng:** UC-D-09, UC-D-10, UC-P-12, UC-P-13, UC-D-11.
- **Actor:** Doctor, Patient.
- **Mô tả xử lý:** Doctor nhập summary, kết luận và đơn thuốc cơ bản. Patient xem kết quả tư vấn và đơn thuốc của chính mình.

TODO: Cần insert hình giao diện kết quả tư vấn/đơn thuốc.

#### FO-015: Đánh giá tư vấn

- **Use case sử dụng:** UC-P-14.
- **Actor:** Patient, Administrator.
- **Mô tả xử lý:** Patient đánh giá sau khi consultation completed. Administrator có thể xem hoặc kiểm duyệt đánh giá nếu hệ thống hỗ trợ.

TODO: Cần insert hình giao diện đánh giá tư vấn.

#### FO-016: Quản lý bác sĩ/bệnh nhân

- **Use case sử dụng:** UC-A-02, UC-A-03.
- **Actor:** Administrator.
- **Mô tả xử lý:** Administrator xem, lọc, cập nhật trạng thái hoặc quản lý tài khoản bác sĩ và bệnh nhân theo quyền.

TODO: Cần insert hình giao diện quản lý bác sĩ/bệnh nhân.

#### FO-017: Quản lý chuyên khoa

- **Use case sử dụng:** UC-A-04.
- **Actor:** Administrator.
- **Mô tả xử lý:** Administrator tạo, cập nhật, bật/tắt và quản lý danh mục chuyên khoa.

TODO: Cần insert hình giao diện quản lý chuyên khoa.

#### FO-018: Quản lý lịch hẹn

- **Use case sử dụng:** UC-A-05.
- **Actor:** Administrator.
- **Mô tả xử lý:** Administrator xem danh sách lịch hẹn, lọc theo trạng thái, bác sĩ, bệnh nhân hoặc thời gian và xử lý trạng thái theo quyền.

TODO: Cần insert hình giao diện quản lý lịch hẹn.

#### FO-019: Kiểm duyệt nội dung

- **Use case sử dụng:** UC-A-06.
- **Actor:** Administrator.
- **Mô tả xử lý:** Administrator xem, duyệt, ẩn hoặc xử lý nội dung câu hỏi, phản hồi, rating/comment nếu có quy trình kiểm duyệt.

TODO: Cần insert hình giao diện kiểm duyệt nội dung.

#### FO-020: Dashboard thống kê

- **Use case sử dụng:** UC-A-07, UC-A-08.
- **Actor:** Administrator.
- **Mô tả xử lý:** Hiển thị số liệu tổng quan về người dùng, bác sĩ, bệnh nhân, lịch hẹn, phiên tư vấn, câu hỏi, trạng thái hệ thống và biểu đồ thống kê theo phạm vi MVP.

TODO: Cần insert hình giao diện dashboard thống kê.

### 5.3. Thiết kế API theo nhóm chức năng

| Nhóm API | Endpoint | Method | Actor | Input chính | Output chính | Ghi chú |
|---|---|---|---|---|---|---|
| Public API | `/public/home` | GET | Guest, Patient | Không yêu cầu token | Thông tin trang chủ public | Theo thiết kế API trong báo cáo |
| Public API | `/public/specialties` | GET | Guest, Patient | Bộ lọc nếu có | Danh sách chuyên khoa public | Theo thiết kế API trong báo cáo |
| Public API | `/public/doctors` | GET | Guest, Patient | `keyword`, `specialtyId`, `page`, `limit` | Danh sách bác sĩ public | Theo thiết kế API trong báo cáo |
| Public API | `/public/doctors/:doctorId` | GET | Guest, Patient | `doctorId` | Hồ sơ công khai bác sĩ | Theo thiết kế API trong báo cáo |
| Auth API | `/auth/register` | POST | Guest | Email, mật khẩu, thông tin đăng ký | Tài khoản Patient được tạo | Theo thiết kế MVP |
| Auth API | `/auth/login` | POST | Patient, Doctor, Admin | Email, mật khẩu | Token và thông tin vai trò | Theo thiết kế MVP |
| Auth API | `/auth/logout` | POST | User đã đăng nhập | Token hiện tại | Kết thúc phiên | Dự kiến theo thiết kế MVP |
| Auth API | `/auth/refresh` | POST | User đã đăng nhập | Refresh token nếu có | Access token mới | Dự kiến theo thiết kế MVP |
| Auth API | `/auth/forgot-password` | POST | Guest/User | Email | Yêu cầu khôi phục mật khẩu | Dự kiến theo thiết kế MVP |
| Profile API | `/patients/me` | GET/PATCH | Patient | Token, dữ liệu hồ sơ | Hồ sơ Patient | Theo thiết kế MVP |
| Profile API | `/doctors/me` | GET/PATCH | Doctor | Token, dữ liệu chuyên môn | Hồ sơ Doctor | Theo thiết kế MVP |
| Profile API | `/doctors/:id` | GET | Guest, Patient, Admin | `id` | Hồ sơ bác sĩ theo phạm vi quyền | Public/private cần tách dữ liệu trả về |
| Specialty API | `/specialties` | GET | Guest, Patient, Doctor | Bộ lọc nếu có | Danh sách chuyên khoa | Theo thiết kế MVP |
| Specialty API | `/admin/specialties` | POST/PATCH/DELETE | Administrator | Dữ liệu chuyên khoa | Chuyên khoa được tạo/cập nhật | Dự kiến theo route admin |
| Question API | `/questions` | POST | Patient | `title`, `content`, `doctorId` nếu có | Câu hỏi được tạo | Theo thiết kế MVP |
| Question API | `/questions/mine` | GET | Patient | Token | Câu hỏi của Patient | Theo thiết kế MVP |
| Question API | `/questions/assigned` | GET | Doctor | Token | Câu hỏi được phân công/có thể xử lý | Theo thiết kế MVP |
| Question API | `/questions/:id/answers` | POST | Doctor | `id`, `content` | Phản hồi được tạo | Theo thiết kế MVP |
| Question API | `/admin/questions/:id/moderation` | PATCH | Administrator | `id`, trạng thái moderation | Nội dung được kiểm duyệt | Dự kiến theo thiết kế MVP |
| Appointment API | `/appointments` | POST | Patient | `doctorId`, `scheduledAt`, `durationMinutes`, `reason` | Appointment được tạo | Theo thiết kế MVP |
| Appointment API | `/appointments/mine` | GET | Patient | Token | Lịch hẹn của Patient | Theo thiết kế MVP |
| Appointment API | `/appointments/doctor/me` | GET | Doctor | Token | Lịch hẹn của Doctor | Theo thiết kế MVP |
| Appointment API | `/appointments/:id/cancel` | PATCH | Patient/Admin | `id`, lý do nếu có | Appointment bị hủy nếu hợp lệ | Theo thiết kế MVP |
| Appointment API | `/appointments/:id/confirm` | PATCH | Doctor/Admin | `id` | Appointment được xác nhận | Dự kiến theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/start` | POST | Doctor | `appointmentId`, channel nếu có | Session được khởi tạo | Theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/join` | POST | Patient, Doctor | `appointmentId` | Thông tin join session | Theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/messages` | GET/POST | Patient, Doctor | `appointmentId`, message nếu POST | Danh sách/tin nhắn mới | Theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/summary` | PATCH | Doctor | `summary` | Summary được lưu | Theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/end` | PATCH | Doctor | `appointmentId` | Session kết thúc | Theo thiết kế MVP |
| Consultation API | `/consultations/:appointmentId/prescriptions` | POST/GET | Doctor, Patient | Prescription data nếu POST | Đơn thuốc | Dự kiến theo thiết kế MVP |
| Consultation API | `/consultations/mine` | GET | Patient | Token | Lịch sử tư vấn của Patient | Theo thiết kế MVP |
| Consultation API | `/consultations/doctor/me` | GET | Doctor | Token | Lịch sử tư vấn của Doctor | Theo thiết kế MVP |
| Rating API | `/ratings` | POST | Patient | `appointmentId`, score, comment | Rating được tạo | Theo thiết kế MVP |
| Rating API | `/ratings/mine` | GET | Patient | Token | Rating của Patient | Dự kiến theo thiết kế MVP |
| Rating API | `/ratings/doctor/:id` | GET | Guest, Patient, Admin | Doctor id | Rating public/tổng hợp | Dự kiến theo thiết kế MVP |
| Notification API | `/notifications/mine` | GET | Patient, Doctor | Token | Danh sách thông báo | Theo thiết kế MVP |
| Notification API | `/notifications/:id/read` | PATCH | Patient, Doctor | Notification id | Thông báo đã đọc | Dự kiến theo thiết kế MVP |
| Admin API | `/admin/users` | GET/PATCH | Administrator | Bộ lọc, dữ liệu cập nhật | Danh sách/cập nhật user | Theo thiết kế MVP |
| Admin API | `/admin/doctors` | GET/PATCH | Administrator | Bộ lọc, trạng thái doctor | Danh sách/cập nhật doctor | Theo thiết kế MVP |
| Admin API | `/admin/patients` | GET/PATCH | Administrator | Bộ lọc, trạng thái patient | Danh sách/cập nhật patient | Theo thiết kế MVP |
| Admin API | `/admin/appointments` | GET/PATCH | Administrator | Bộ lọc, trạng thái lịch | Danh sách/cập nhật lịch hẹn | Theo thiết kế MVP |
| Admin API | `/admin/reports` | GET | Administrator | Khoảng thời gian, bộ lọc | Báo cáo hệ thống | Theo thiết kế MVP |
| Admin API | `/admin/dashboard` | GET | Administrator | Khoảng thời gian nếu có | Số liệu dashboard | Theo thiết kế MVP |
| External/File API | `/files/upload` | POST | Patient, Doctor, Admin | File, metadata | FileAttachment metadata | Dự kiến theo thiết kế MVP |
| External/File API | `/files/:id` | GET | Người có quyền | File id | File hoặc URL tải file | Dự kiến theo thiết kế MVP |

### 5.4. Cơ sở dữ liệu

Cơ sở dữ liệu của hệ thống sử dụng PostgreSQL, được truy cập thông qua Prisma ORM. Thiết kế dữ liệu hướng đến việc lưu trữ đầy đủ các nhóm nghiệp vụ theo SRS, đồng thời bảo đảm quan hệ giữa người dùng, hồ sơ, chuyên khoa, câu hỏi, lịch hẹn, phiên tư vấn, đơn thuốc, đánh giá, thông báo và audit.

**Identity & Security**

Nhóm bảng này gồm `User`, `AuditLog` và các bảng hoặc trường liên quan đến xác thực/phân quyền. `User` lưu tài khoản và vai trò; `AuditLog` ghi nhận các hành động nhạy cảm như quản trị, truy cập/cập nhật dữ liệu sức khỏe hoặc thay đổi trạng thái nghiệp vụ.

**Profile & Specialty**

Nhóm này gồm `PatientProfile`, `DoctorProfile`, `Specialty` và `DoctorSpecialty`. PatientProfile lưu dữ liệu cá nhân và thông tin sức khỏe. DoctorProfile lưu thông tin chuyên môn. Specialty và DoctorSpecialty quản lý danh mục chuyên khoa và quan hệ bác sĩ - chuyên khoa.

**Question & Answer**

Nhóm này gồm `Question` và `Answer`. Question lưu câu hỏi sức khỏe của Patient, có thể gắn với Doctor hoặc chuyên khoa. Answer lưu phản hồi của Doctor. Nội dung câu hỏi và phản hồi là dữ liệu sức khỏe nhạy cảm, cần kiểm soát quyền truy cập.

**Appointment & Consultation**

Nhóm này gồm `Appointment`, `ConsultationSession` và `ConsultationMessage`. Appointment lưu lịch hẹn giữa Patient và Doctor. ConsultationSession lưu thông tin phiên tư vấn. ConsultationMessage lưu trao đổi chat trong phiên tư vấn nếu có.

**Prescription & Rating**

Nhóm này gồm `Prescription`, `PrescriptionItem` và `Rating`. Prescription lưu đơn thuốc gắn với phiên tư vấn hoặc appointment. PrescriptionItem lưu từng dòng thuốc. Rating lưu đánh giá của Patient sau khi tư vấn hoàn tất.

**Notification & Audit**

Nhóm này gồm `NotificationLog`, `OutboxEvent` và `AuditLog`. NotificationLog lưu trạng thái thông báo. OutboxEvent hỗ trợ xử lý sự kiện bất đồng bộ nếu hệ thống áp dụng outbox. AuditLog hỗ trợ truy vết hành động nhạy cảm.

**File Storage**

Nhóm này gồm `FileAttachment`, lưu metadata của file tải lên như tên file, mime type, kích thước, đường dẫn/khóa lưu trữ, người upload và liên kết nghiệp vụ. File có thể chứa thông tin y tế nên metadata và quyền truy cập cần được bảo vệ.

**Reporting**

Reporting có thể được xây dựng từ các bảng nghiệp vụ hiện có như users, appointments, consultation sessions, questions, ratings và notification logs. Nếu cần tối ưu dashboard, hệ thống có thể bổ sung bảng tổng hợp hoặc materialized view trong giai đoạn mở rộng.

TODO: Cần insert hình ERD/database diagram.

### 5.5. Từ điển cơ sở dữ liệu

**Bảng User**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh người dùng. |
| 2 | email | String | Unique, not null | Email đăng nhập. |
| 3 | passwordHash | String | Not null, sensitive | Hash mật khẩu, không trả về response. |
| 4 | fullName | String | Not null | Họ tên người dùng. |
| 5 | role | Enum/String | Not null | PATIENT, DOCTOR, ADMIN. |
| 6 | status | Enum/String | Default active | Trạng thái tài khoản. |
| 7 | createdAt | DateTime | Not null | Ngày tạo. |

**Bảng PatientProfile**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh hồ sơ bệnh nhân. |
| 2 | userId | UUID/String | Foreign key, unique | Liên kết User. |
| 3 | dateOfBirth | Date | Nullable | Ngày sinh. |
| 4 | gender | Enum/String | Nullable | Giới tính. |
| 5 | phone | String | Nullable | Số điện thoại. |
| 6 | address | String | Nullable | Địa chỉ. |
| 7 | medicalHistory | Text/Json | Sensitive | Tiền sử bệnh, dữ liệu sức khỏe nhạy cảm. |

**Bảng DoctorProfile**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh hồ sơ bác sĩ. |
| 2 | userId | UUID/String | Foreign key, unique | Liên kết User. |
| 3 | bio | Text | Nullable | Mô tả chuyên môn. |
| 4 | experienceYears | Integer | Default 0 | Số năm kinh nghiệm. |
| 5 | consultationFee | Decimal | Nullable | Phí tư vấn nếu có. |
| 6 | status | Enum/String | Not null | ACTIVE, INACTIVE, PENDING_APPROVAL. |
| 7 | schedule | Json | Nullable | Lịch làm việc. |

**Bảng Specialty**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh chuyên khoa. |
| 2 | name | String | Unique, not null | Tên chuyên khoa. |
| 3 | description | Text | Nullable | Mô tả chuyên khoa. |
| 4 | isActive | Boolean | Default true | Trạng thái hiển thị. |
| 5 | createdAt | DateTime | Not null | Ngày tạo. |

**Bảng DoctorSpecialty**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | doctorId | UUID/String | Foreign key | Liên kết DoctorProfile. |
| 2 | specialtyId | UUID/String | Foreign key | Liên kết Specialty. |
| 3 | isPrimary | Boolean | Default false | Chuyên khoa chính nếu có. |

**Bảng Question**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh câu hỏi. |
| 2 | patientId | UUID/String | Foreign key | Patient tạo câu hỏi. |
| 3 | doctorId | UUID/String | Nullable foreign key | Doctor được gán nếu có. |
| 4 | title | String | Not null | Tiêu đề câu hỏi. |
| 5 | content | Text | Not null, sensitive | Nội dung sức khỏe nhạy cảm. |
| 6 | status | Enum/String | Not null | PENDING, ANSWERED, CLOSED, MODERATED. |
| 7 | createdAt | DateTime | Not null | Ngày tạo câu hỏi. |

**Bảng Answer**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh phản hồi. |
| 2 | questionId | UUID/String | Foreign key | Câu hỏi được trả lời. |
| 3 | doctorId | UUID/String | Foreign key | Doctor phản hồi. |
| 4 | content | Text | Not null, sensitive | Nội dung phản hồi chuyên môn. |
| 5 | isApproved | Boolean | Default false | Trạng thái duyệt nếu có. |
| 6 | createdAt | DateTime | Not null | Ngày phản hồi. |

**Bảng Appointment**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh lịch hẹn. |
| 2 | patientId | UUID/String | Foreign key | Patient đặt lịch. |
| 3 | doctorId | UUID/String | Foreign key | Doctor tư vấn. |
| 4 | scheduledAt | DateTime | Not null | Thời điểm tư vấn. |
| 5 | durationMinutes | Integer | Not null/default | Thời lượng tư vấn. |
| 6 | reason | Text | Sensitive | Lý do tư vấn. |
| 7 | status | Enum/String | Not null | PENDING_CONFIRMATION, CONFIRMED, COMPLETED, CANCELLED. |
| 8 | createdAt | DateTime | Not null | Ngày tạo lịch. |

**Bảng ConsultationSession**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh phiên tư vấn. |
| 2 | appointmentId | UUID/String | Foreign key, unique | Lịch hẹn tương ứng. |
| 3 | startedAt | DateTime | Nullable | Thời điểm bắt đầu. |
| 4 | endedAt | DateTime | Nullable | Thời điểm kết thúc. |
| 5 | status | Enum/String | Not null | CREATED, IN_PROGRESS, COMPLETED. |
| 6 | channel | Enum/String | Nullable | CHAT hoặc VIDEO. |
| 7 | summary | Text | Sensitive | Tóm tắt/kết quả tư vấn. |

**Bảng ConsultationMessage**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh tin nhắn. |
| 2 | sessionId | UUID/String | Foreign key | Phiên tư vấn. |
| 3 | senderId | UUID/String | Foreign key | Người gửi. |
| 4 | content | Text | Not null, sensitive | Nội dung trao đổi trong phiên tư vấn. |
| 5 | createdAt | DateTime | Not null | Thời điểm gửi. |

**Bảng Prescription**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh đơn thuốc. |
| 2 | consultationSessionId | UUID/String | Foreign key | Phiên tư vấn liên quan. |
| 3 | doctorId | UUID/String | Foreign key | Doctor kê đơn. |
| 4 | patientId | UUID/String | Foreign key | Patient nhận đơn. |
| 5 | notes | Text | Sensitive | Ghi chú đơn thuốc. |
| 6 | createdAt | DateTime | Not null | Ngày tạo. |

**Bảng PrescriptionItem**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh dòng thuốc. |
| 2 | prescriptionId | UUID/String | Foreign key | Đơn thuốc cha. |
| 3 | medicineName | String | Not null, sensitive | Tên thuốc. |
| 4 | dosage | String | Nullable | Liều dùng. |
| 5 | frequency | String | Nullable | Tần suất dùng. |
| 6 | duration | String | Nullable | Thời lượng dùng. |
| 7 | instruction | Text | Nullable | Hướng dẫn sử dụng. |

**Bảng Rating**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh đánh giá. |
| 2 | appointmentId | UUID/String | Foreign key | Lịch hẹn được đánh giá. |
| 3 | patientId | UUID/String | Foreign key | Patient đánh giá. |
| 4 | doctorId | UUID/String | Foreign key | Doctor được đánh giá. |
| 5 | score | Integer | 1-5 | Điểm đánh giá. |
| 6 | comment | Text | Nullable | Nhận xét, có thể cần kiểm duyệt. |
| 7 | createdAt | DateTime | Not null | Ngày đánh giá. |

**Bảng NotificationLog**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh thông báo. |
| 2 | userId | UUID/String | Foreign key | Người nhận. |
| 3 | type | Enum/String | Not null | APPOINTMENT, QUESTION, SYSTEM. |
| 4 | channel | Enum/String | Not null | EMAIL, SMS, IN_APP. |
| 5 | content | Text/Json | Nullable | Nội dung thông báo. |
| 6 | status | Enum/String | Not null | PENDING, SENT, FAILED, READ. |
| 7 | sentAt | DateTime | Nullable | Thời điểm gửi. |

**Bảng AuditLog**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh audit. |
| 2 | actorId | UUID/String | Nullable foreign key | Người thực hiện. |
| 3 | action | String | Not null | Hành động được ghi nhận. |
| 4 | entityType | String | Not null | Loại dữ liệu bị tác động. |
| 5 | entityId | UUID/String | Nullable | Định danh dữ liệu bị tác động. |
| 6 | metadata | Json | Sensitive | Audit metadata, cần sanitize dữ liệu nhạy cảm. |
| 7 | createdAt | DateTime | Not null | Thời điểm ghi nhận. |

**Bảng FileAttachment**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh tệp. |
| 2 | uploadedById | UUID/String | Foreign key | Người upload. |
| 3 | ownerType | String | Nullable | Loại nghiệp vụ liên quan. |
| 4 | ownerId | UUID/String | Nullable | ID nghiệp vụ liên quan. |
| 5 | fileName | String | Not null | Tên file gốc. |
| 6 | mimeType | String | Not null | Kiểu file. |
| 7 | size | Integer | Not null | Kích thước file. |
| 8 | storageKey | String | Not null, sensitive | Khóa/đường dẫn lưu trữ. |
| 9 | createdAt | DateTime | Not null | Ngày upload. |

**Bảng OutboxEvent**

| ID | Attribute | Type | Constraint | Note |
|---|---|---|---|---|
| 1 | id | UUID/String | Primary key | Định danh sự kiện. |
| 2 | eventType | String | Not null | Loại sự kiện nghiệp vụ. |
| 3 | payload | Json | Not null | Dữ liệu sự kiện, cần tránh chứa dữ liệu nhạy cảm không cần thiết. |
| 4 | status | Enum/String | Not null | PENDING, PROCESSED, FAILED. |
| 5 | retryCount | Integer | Default 0 | Số lần retry. |
| 6 | createdAt | DateTime | Not null | Thời điểm tạo. |
| 7 | processedAt | DateTime | Nullable | Thời điểm xử lý xong. |

# CHƯƠNG III. TRIỂN KHAI HỆ THỐNG

## 1. Phần mềm ứng dụng

Hệ thống được xây dựng theo mô hình ứng dụng web gồm frontend, backend và cơ sở dữ liệu quan hệ. Frontend đảm nhiệm giao diện thao tác cho người dùng, backend cung cấp các API nghiệp vụ và xử lý phân quyền, còn database lưu trữ thông tin tài khoản, hồ sơ, lịch hẹn, câu hỏi, phiên tư vấn, đơn thuốc, đánh giá và log nghiệp vụ.

Phạm vi MVP của hệ thống tập trung vào nền tảng tư vấn sức khỏe trực tuyến cho bốn nhóm người dùng chính: Guest User, Patient, Doctor và Administrator. Guest User có thể tra cứu thông tin công khai, chuyên khoa và bác sĩ. Patient có thể đăng ký, đăng nhập, quản lý hồ sơ, gửi câu hỏi, đặt lịch, tham gia tư vấn, xem kết quả tư vấn, đơn thuốc và đánh giá chất lượng dịch vụ. Doctor có thể quản lý hồ sơ chuyên môn, lịch tư vấn, phản hồi câu hỏi, thực hiện phiên tư vấn và ghi nhận kết quả. Administrator quản lý người dùng, bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung và dashboard thống kê.

Ngoài các chức năng nội bộ, hệ thống còn có lớp tích hợp với các dịch vụ bên ngoài ở mức MVP hoặc định hướng mở rộng, gồm Notification Service để gửi thông báo, Video Communication Service để hỗ trợ tư vấn trực tuyến và File Storage Service để lưu trữ tệp tải lên khi có nhu cầu. Các chức năng này cần được thiết kế theo hướng có thể kiểm thử, không làm sai lệch nghiệp vụ chính khi dịch vụ phụ trợ gặp lỗi.

### 1.1. Giao diện chính của phần mềm

**Trang chủ**

TODO: Cần insert hình giao diện trang chủ.

Trang chủ là điểm truy cập đầu tiên của Guest User và người dùng đã đăng nhập. Giao diện trình bày thông tin tổng quan về nền tảng, các chuyên khoa nổi bật, bác sĩ tiêu biểu và các lối đi nhanh đến tra cứu bác sĩ, đăng nhập hoặc đăng ký. Màn hình này cần rõ ràng, dễ dùng và không yêu cầu đăng nhập đối với nội dung công khai.

**Trang danh sách chuyên khoa**

TODO: Cần insert hình giao diện danh sách chuyên khoa.

Trang danh sách chuyên khoa cho phép người dùng xem các chuyên khoa đang được hệ thống công bố. Từ màn hình này, người dùng có thể chọn một chuyên khoa để tiếp tục tra cứu bác sĩ phù hợp. Đây là giao diện phục vụ luồng Guest User tra cứu bác sĩ và cũng hỗ trợ Patient khi chuẩn bị đặt lịch tư vấn.

**Trang danh sách bác sĩ**

TODO: Cần insert hình giao diện danh sách bác sĩ.

Trang danh sách bác sĩ hiển thị các bác sĩ đã được duyệt và còn hoạt động theo phạm vi công khai. Người dùng có thể tìm kiếm, lọc theo chuyên khoa hoặc xem thông tin tóm tắt của bác sĩ. Khi người dùng khách chọn hành động cần xác thực như đặt lịch hoặc gửi câu hỏi, hệ thống cần chuyển sang màn hình đăng nhập/đăng ký.

**Trang chi tiết bác sĩ**

TODO: Cần insert hình giao diện chi tiết bác sĩ.

Trang chi tiết bác sĩ cung cấp thông tin hồ sơ công khai như họ tên, chuyên khoa, học hàm/học vị hoặc mô tả chuyên môn nếu có. Màn hình này cần hạn chế hiển thị thông tin nội bộ và dữ liệu nhạy cảm. Các hành động đặt lịch tư vấn hoặc gửi câu hỏi được kiểm soát theo trạng thái đăng nhập và vai trò của người dùng.

**Trang đăng ký**

TODO: Cần insert hình giao diện đăng ký.

Trang đăng ký cho phép người dùng tạo tài khoản Patient để sử dụng các chức năng cần xác thực. Form cần kiểm tra dữ liệu đầu vào như họ tên, email, số điện thoại, mật khẩu và xác nhận mật khẩu. Sau khi đăng ký thành công, người dùng có thể đăng nhập và tiếp tục hoàn thiện hồ sơ sức khỏe cá nhân.

**Trang đăng nhập**

TODO: Cần insert hình giao diện đăng nhập.

Trang đăng nhập phục vụ Patient, Doctor và Administrator. Sau khi xác thực thành công, hệ thống điều hướng người dùng đến khu vực phù hợp với vai trò. Khi đăng nhập thất bại, thông báo lỗi cần rõ ràng nhưng không làm lộ thông tin nhạy cảm về tài khoản.

**Trang hồ sơ bệnh nhân**

TODO: Cần insert hình giao diện hồ sơ bệnh nhân.

Trang hồ sơ bệnh nhân cho phép Patient cập nhật thông tin cá nhân và thông tin sức khỏe cần thiết cho quá trình tư vấn. Các dữ liệu như tiền sử bệnh, dị ứng, triệu chứng hoặc ghi chú sức khỏe phải được xem là dữ liệu nhạy cảm. Patient chỉ được xem và chỉnh sửa hồ sơ của chính mình.

**Trang hồ sơ bác sĩ**

TODO: Cần insert hình giao diện hồ sơ bác sĩ.

Trang hồ sơ bác sĩ cho phép Doctor cập nhật thông tin chuyên môn, kinh nghiệm, mô tả dịch vụ tư vấn và lịch làm việc nếu hệ thống hỗ trợ. Các thông tin công khai cần được trình bày nhất quán với trang chi tiết bác sĩ. Những thay đổi quan trọng có thể cần Administrator kiểm duyệt tùy theo quy trình vận hành.

**Trang đặt lịch tư vấn**

TODO: Cần insert hình giao diện đặt lịch tư vấn.

Trang đặt lịch tư vấn cho phép Patient chọn bác sĩ, thời gian, kênh tư vấn và nhập lý do tư vấn nếu cần. Hệ thống cần hỗ trợ tạo lịch hẹn, xem lịch của bệnh nhân, bác sĩ xác nhận và cập nhật trạng thái lịch hẹn theo quy trình nghiệp vụ. Màn hình cần kiểm tra trùng lịch, trạng thái bác sĩ và quyền truy cập trước khi ghi nhận lịch hẹn.

**Trang danh sách lịch hẹn của bệnh nhân**

TODO: Cần insert hình giao diện danh sách lịch hẹn của bệnh nhân.

Trang danh sách lịch hẹn của bệnh nhân hiển thị các cuộc hẹn sắp tới và lịch sử lịch hẹn thuộc Patient đang đăng nhập. Người dùng có thể xem thời gian tư vấn, bác sĩ phụ trách, trạng thái lịch và thao tác hủy lịch nếu còn trong điều kiện cho phép. Giao diện này không được để bệnh nhân truy cập lịch của người khác.

**Trang quản lý lịch tư vấn của bác sĩ**

TODO: Cần insert hình giao diện quản lý lịch tư vấn của bác sĩ.

Trang quản lý lịch tư vấn của bác sĩ hiển thị các lịch hẹn được gán cho Doctor. Bác sĩ có thể xem thông tin lịch, xác nhận hoặc chuẩn bị bắt đầu phiên tư vấn theo trạng thái nghiệp vụ. Màn hình cần hỗ trợ lọc theo ngày, trạng thái và bảo đảm bác sĩ chỉ thấy lịch thuộc phạm vi của mình.

**Trang gửi câu hỏi sức khỏe**

TODO: Cần insert hình giao diện gửi câu hỏi sức khỏe.

Trang gửi câu hỏi sức khỏe cho phép Patient nhập nội dung câu hỏi và gửi đến hệ thống hoặc bác sĩ phù hợp theo luồng thiết kế. Nội dung câu hỏi là dữ liệu liên quan đến sức khỏe nên cần được bảo vệ theo nguyên tắc privacy. Màn hình phải có kiểm tra dữ liệu đầu vào, thông báo gửi thành công/thất bại và không cho Guest User gửi câu hỏi khi chưa đăng nhập.

**Trang danh sách câu hỏi của bệnh nhân**

TODO: Cần insert hình giao diện danh sách câu hỏi của bệnh nhân.

Trang danh sách câu hỏi của bệnh nhân cho phép Patient xem lại các câu hỏi đã gửi, trạng thái xử lý và phản hồi của bác sĩ nếu có. Màn hình hỗ trợ theo dõi lịch sử trao đổi sức khỏe, đồng thời cần bảo vệ nội dung câu hỏi như dữ liệu riêng tư. Patient chỉ được xem câu hỏi thuộc tài khoản của mình.

**Trang danh sách câu hỏi cần phản hồi của bác sĩ**

TODO: Cần insert hình giao diện danh sách câu hỏi cần phản hồi của bác sĩ.

Trang này giúp Doctor xem các câu hỏi được phân công hoặc thuộc phạm vi có thể xử lý. Danh sách cần thể hiện thông tin tóm tắt, trạng thái câu hỏi và thời điểm gửi. Hệ thống không cho bác sĩ xem các câu hỏi ngoài phạm vi phân quyền.

**Trang phản hồi câu hỏi**

TODO: Cần insert hình giao diện phản hồi câu hỏi.

Trang phản hồi câu hỏi cho phép Doctor đọc nội dung câu hỏi, nhập câu trả lời và gửi phản hồi cho Patient. Nội dung phản hồi cần được lưu kèm thông tin bác sĩ, thời điểm phản hồi và trạng thái xử lý. Nếu có quy trình kiểm duyệt, phản hồi cần chuyển qua trạng thái chờ duyệt trước khi hiển thị công khai hoặc gửi đến bệnh nhân.

**Trang phiên tư vấn trực tuyến chat/video**

TODO: Cần insert hình giao diện phiên tư vấn trực tuyến chat/video.

Trang phiên tư vấn trực tuyến là nơi Patient và Doctor tham gia tư vấn theo lịch hẹn hợp lệ. Hệ thống có thể hỗ trợ chat, video hoặc fallback sang chat trong phạm vi MVP khi video không khả dụng. Chỉ đúng Patient và Doctor của lịch hẹn mới được tham gia phiên tư vấn.

**Trang kết quả tư vấn**

TODO: Cần insert hình giao diện kết quả tư vấn.

Trang kết quả tư vấn hiển thị tóm tắt nội dung tư vấn, nhận định của bác sĩ và hướng dẫn sau tư vấn nếu có. Doctor là người ghi nhận kết quả, còn Patient chỉ được xem kết quả của chính mình. Đây là dữ liệu sức khỏe cần kiểm soát quyền truy cập nghiêm ngặt.

**Trang đơn thuốc điện tử**

TODO: Cần insert hình giao diện đơn thuốc điện tử.

Trang đơn thuốc điện tử cho phép Patient xem thông tin thuốc, liều dùng và hướng dẫn sử dụng do Doctor ghi nhận sau phiên tư vấn. Doctor có thể tạo đơn thuốc cơ bản trong phạm vi MVP. Thông tin đơn thuốc phải gắn với phiên tư vấn và không được hiển thị cho người không có quyền.

**Trang đánh giá chất lượng tư vấn**

TODO: Cần insert hình giao diện đánh giá chất lượng tư vấn.

Trang đánh giá chất lượng tư vấn cho phép Patient đánh giá sau khi phiên tư vấn hoàn tất. Giao diện có thể gồm điểm đánh giá, nhận xét và thông tin bác sĩ được đánh giá. Hệ thống cần ngăn đánh giá khi phiên tư vấn chưa hoàn tất hoặc đánh giá trùng nếu quy trình có quy định.

**Trang thông báo**

TODO: Cần insert hình giao diện thông báo.

Trang thông báo hiển thị các thông tin nhắc lịch, phản hồi câu hỏi, thay đổi trạng thái lịch hẹn hoặc thông báo hệ thống. Patient và Doctor cần nhận đúng thông báo liên quan đến mình. Trạng thái đã đọc/chưa đọc giúp người dùng theo dõi các cập nhật quan trọng.

**Trang quản lý tài khoản bác sĩ**

TODO: Cần insert hình giao diện quản lý tài khoản bác sĩ.

Trang quản lý tài khoản bác sĩ dành cho Administrator tạo, cập nhật, khóa/mở khóa hoặc duyệt thông tin bác sĩ. Giao diện cần thể hiện trạng thái hoạt động, chuyên khoa và thông tin liên hệ cần thiết. Các thao tác quản trị phải được phân quyền và ghi nhận log phù hợp.

**Trang quản lý tài khoản bệnh nhân**

TODO: Cần insert hình giao diện quản lý tài khoản bệnh nhân.

Trang quản lý tài khoản bệnh nhân cho phép Administrator tra cứu, cập nhật trạng thái hoặc hỗ trợ xử lý tài khoản bệnh nhân. Giao diện cần tránh hiển thị dữ liệu sức khỏe chi tiết nếu không cần thiết cho mục đích quản trị. Các hành động nhạy cảm như khóa tài khoản cần có xác nhận.

**Trang quản lý chuyên khoa**

TODO: Cần insert hình giao diện quản lý chuyên khoa.

Trang quản lý chuyên khoa cho phép Administrator thêm, sửa, ẩn hoặc kích hoạt chuyên khoa. Dữ liệu chuyên khoa được dùng trong luồng tra cứu bác sĩ và đặt lịch tư vấn. Vì vậy, thay đổi chuyên khoa cần đảm bảo không làm sai lệch liên kết với hồ sơ bác sĩ.

**Trang quản lý lịch hẹn**

TODO: Cần insert hình giao diện quản lý lịch hẹn.

Trang quản lý lịch hẹn cho phép Administrator theo dõi các lịch tư vấn trên toàn hệ thống. Giao diện hỗ trợ lọc theo bác sĩ, bệnh nhân, thời gian và trạng thái. Vai trò quản trị có thể can thiệp một số trạng thái lịch hẹn theo quy trình vận hành, nhưng vẫn cần bảo đảm audit và quyền riêng tư dữ liệu.

**Trang kiểm duyệt nội dung**

TODO: Cần insert hình giao diện kiểm duyệt nội dung.

Trang kiểm duyệt nội dung phục vụ việc rà soát câu hỏi, phản hồi, đánh giá hoặc nội dung nhạy cảm nếu hệ thống áp dụng quy trình kiểm duyệt. Administrator có thể duyệt, ẩn hoặc yêu cầu chỉnh sửa nội dung theo chính sách vận hành. Chức năng này giúp giảm rủi ro thông tin sai lệch và bảo vệ trải nghiệm người dùng.

**Trang dashboard thống kê**

TODO: Cần insert hình giao diện dashboard thống kê.

Trang dashboard thống kê cung cấp số liệu tổng quan về người dùng, bác sĩ, lịch hẹn, phiên tư vấn, câu hỏi và mức độ hoạt động của hệ thống. Các biểu đồ giúp Administrator theo dõi vận hành và phát hiện bất thường. Số liệu hiển thị trong báo cáo chỉ nên được cập nhật khi có dữ liệu thực tế hoặc môi trường demo được kiểm soát.

## 2. Cài đặt hệ thống

Phần cài đặt hệ thống mô tả cách chuẩn bị môi trường để chạy backend, frontend, database và các dịch vụ phụ trợ ở phạm vi phát triển hoặc MVP. Các giá trị cấu hình nhạy cảm như mật khẩu database, JWT secret, API key email/SMS hoặc khóa dịch vụ lưu trữ không được ghi trực tiếp trong báo cáo. Khi triển khai thực tế, các giá trị này cần được quản lý qua biến môi trường hoặc công cụ quản lý secret phù hợp.

### 2.1. Backend

Backend được xây dựng trên nền tảng Node.js, sử dụng NestJS và TypeScript để tổ chức các module nghiệp vụ. Các nhóm chức năng chính gồm xác thực, phân quyền, hồ sơ người dùng, chuyên khoa, bác sĩ, câu hỏi sức khỏe, lịch hẹn, phiên tư vấn, đơn thuốc, đánh giá, thông báo và quản trị.

Yêu cầu môi trường:

- Node.js phiên bản phù hợp với dự án, ưu tiên Node.js 18 trở lên.
- npm để cài đặt thư viện.
- PostgreSQL hoặc kết nối database tương ứng với cấu hình môi trường.
- Docker/Docker Compose nếu chạy database bằng container.
- File `.env` chứa cấu hình môi trường, không ghi secret thật vào báo cáo.

Các bước cài đặt backend:

1. Cài đặt thư viện:

```bash
npm install
```

2. Tạo file cấu hình môi trường từ file mẫu:

```bash
cp .env.example .env
```

Các biến cấu hình thường gồm `NODE_ENV`, `PORT`, `DATABASE_URL`, `JWT_SECRET`, `JWT_REFRESH_SECRET`, thời hạn token, cấu hình CORS và cấu hình dịch vụ thông báo/video nếu có. Giá trị thật cần được nhóm tự cấu hình trong môi trường chạy, không đưa vào báo cáo.

3. Khởi động database local nếu sử dụng Docker:

```bash
docker compose up -d
```

4. Sinh Prisma Client:

```bash
npm run prisma:generate
```

5. Áp dụng migration cho database:

```bash
npm run prisma:migrate:deploy
```

Trong môi trường phát triển, nhóm có thể dùng lệnh migration dev nếu cần tạo migration mới. Khi triển khai môi trường ổn định, chỉ nên áp dụng các migration đã được rà soát.

6. Seed dữ liệu mẫu nếu cần cho môi trường phát triển hoặc demo:

```bash
npm run prisma:seed
```

7. Khởi động backend ở chế độ phát triển:

```bash
npm run start:dev
```

8. Truy cập tài liệu Swagger/OpenAPI nếu đã bật cấu hình:

```text
http://localhost:<PORT>/api/docs
```

Cổng backend phụ thuộc cấu hình `PORT` trong môi trường chạy.

TODO: Cần bổ sung cổng backend chính thức nếu nhóm chốt cấu hình triển khai.

### 2.2. Frontend

Frontend được xây dựng bằng React 18, TypeScript và Vite. Giao diện sử dụng React Router v6 cho điều hướng, Redux Toolkit và Redux Saga cho quản lý trạng thái, Axios cho gọi API, PrimeReact kết hợp Tailwind CSS cho giao diện, Formik và Yup cho form/validation, i18next cho đa ngôn ngữ và Recharts cho dashboard.

Yêu cầu môi trường:

- Node.js phiên bản phù hợp với frontend, ưu tiên Node.js 18 trở lên.
- npm để cài đặt thư viện.
- Backend đã chạy và cung cấp API base URL.
- File cấu hình môi trường frontend, ví dụ `.env`, `.env.local` hoặc tên tương ứng theo dự án.

Các bước cài đặt frontend:

1. Cài đặt thư viện:

```bash
npm install
```

2. Cấu hình API base URL để frontend gọi backend:

```text
VITE_API_BASE_URL=http://localhost:<BACKEND_PORT>/api
```

Tên biến môi trường có thể thay đổi theo cấu hình frontend thực tế, nhưng cần bảo đảm tất cả request đều gọi đúng backend API và không hard-code URL production trong mã nguồn.

3. Khởi động frontend ở chế độ phát triển:

```bash
npm run dev
```

Với Vite, cổng phát triển thường dùng là `5173`. Nếu cổng này đã được sử dụng, Vite có thể tự chuyển sang cổng khác hoặc nhóm cấu hình lại theo nhu cầu.

### 2.3. Database

Cơ sở dữ liệu của hệ thống sử dụng PostgreSQL kết hợp Prisma ORM. Database lưu trữ dữ liệu tài khoản, hồ sơ bệnh nhân, hồ sơ bác sĩ, chuyên khoa, câu hỏi, phản hồi, lịch hẹn, phiên tư vấn, đơn thuốc, đánh giá, thông báo, file đính kèm và audit log. Các trường dữ liệu sức khỏe và thông tin định danh cần được kiểm soát quyền truy cập và không ghi ra log không cần thiết.

Quy trình cài đặt database local:

1. Khởi động PostgreSQL:

```bash
docker compose up -d
```

2. Kiểm tra trạng thái database nếu cần:

```bash
docker compose ps
```

3. Sinh Prisma Client:

```bash
npm run prisma:generate
```

4. Áp dụng migration:

```bash
npm run prisma:migrate:deploy
```

5. Seed dữ liệu mẫu nếu cần:

```bash
npm run prisma:seed
```

Trong môi trường production, không sử dụng lệnh reset database và không ghi mật khẩu thật trong tài liệu. Migration cần được kiểm tra trước khi áp dụng, đặc biệt với các bảng chứa dữ liệu sức khỏe, đơn thuốc và audit log.

TODO: Cần insert hình ERD/database diagram.

### 2.4. External Services

**Notification Service**

Notification Service hỗ trợ gửi thông báo nhắc lịch, thông báo khi bác sĩ phản hồi câu hỏi và thông báo thay đổi trạng thái lịch hẹn. Trong phạm vi MVP, chức năng này có thể triển khai ở mức email/SMS mock, log nội bộ hoặc provider cấu hình thử nghiệm. Nếu chưa tích hợp provider production, báo cáo chỉ ghi nhận đây là cấu phần MVP/định hướng mở rộng, không khẳng định đã gửi email/SMS thật.

**Video Communication Service**

Video Communication Service hỗ trợ phiên tư vấn trực tuyến giữa Patient và Doctor. Trong phạm vi MVP, hệ thống có thể dùng video mock, liên kết phòng họp, hoặc tích hợp cơ bản với dịch vụ video. Khi video không khả dụng, hệ thống cần có phương án fallback sang chat để không làm gián đoạn toàn bộ luồng tư vấn.

**File Storage Service**

File Storage Service phục vụ upload và truy xuất tệp như ảnh đại diện, tài liệu y tế hoặc file đính kèm nếu chức năng này được bật. Trong phạm vi MVP, file có thể được lưu cục bộ hoặc qua dịch vụ lưu trữ cấu hình riêng. Metadata của file cần được lưu trong database, còn nội dung file và đường dẫn lưu trữ phải được bảo vệ theo quyền truy cập.

## 3. Chiến lược kiểm thử

Chiến lược kiểm thử được xây dựng nhằm bảo đảm hệ thống tư vấn sức khỏe trực tuyến đáp ứng đúng yêu cầu SRS, đặc biệt ở các luồng có liên quan đến dữ liệu sức khỏe, phân quyền người dùng, lịch hẹn và phiên tư vấn trực tuyến. Phần này trình bày phạm vi, loại kiểm thử, kỹ thuật thiết kế test case, công cụ, môi trường và nguyên tắc ghi nhận kết quả trước khi đi vào đặc tả test case chi tiết.

TODO: Cần insert hình tổng quan chiến lược kiểm thử nếu cần.

### 3.1. Mục tiêu kiểm thử

Mục tiêu kiểm thử của hệ thống gồm:

- Xác nhận hệ thống đáp ứng các yêu cầu chức năng đã mô tả trong SRS.
- Xác nhận cơ chế xác thực và phân quyền hoạt động đúng theo từng vai trò: Guest User, Patient, Doctor và Administrator.
- Xác nhận dữ liệu sức khỏe, hồ sơ cá nhân, câu hỏi, kết quả tư vấn và đơn thuốc được bảo vệ đúng phạm vi truy cập.
- Xác nhận các luồng nghiệp vụ chính như tra cứu bác sĩ, đặt lịch, gửi câu hỏi, phản hồi câu hỏi, tư vấn trực tuyến, ghi đơn thuốc và đánh giá hoạt động đúng.
- Xác nhận thông báo nhắc lịch, thông báo phản hồi và dashboard quản trị hoạt động đúng nếu các chức năng này được cấu hình trong phạm vi MVP.
- Phát hiện lỗi về validation, trạng thái nghiệp vụ, điều hướng giao diện, API contract, dữ liệu trả về và xử lý ngoại lệ.

### 3.2. Phạm vi kiểm thử

| Nhóm chức năng | Actor | Phạm vi kiểm thử | Mức ưu tiên |
|---|---|---|---|
| Public access/discovery | Guest User, Patient | Xem trang chủ, danh sách chuyên khoa, danh sách bác sĩ, chi tiết bác sĩ, tìm kiếm và lọc bác sĩ. | Cao |
| Auth | Patient, Doctor, Administrator | Đăng ký, đăng nhập, đăng xuất, refresh token nếu có, điều hướng theo vai trò và xử lý thông tin đăng nhập sai. | Cao |
| Patient profile | Patient | Xem, cập nhật hồ sơ cá nhân và hồ sơ sức khỏe; kiểm tra quyền sở hữu dữ liệu. | Cao |
| Doctor profile | Doctor, Administrator | Xem/cập nhật hồ sơ chuyên môn, trạng thái bác sĩ, thông tin hiển thị công khai. | Cao |
| Specialty | Guest User, Patient, Administrator | Xem chuyên khoa public, lọc bác sĩ theo chuyên khoa, thêm/sửa/ẩn chuyên khoa ở khu vực quản trị. | Trung bình |
| Health question | Patient, Doctor, Administrator | Patient gửi câu hỏi, Doctor xem câu hỏi được phân công và phản hồi, Admin kiểm duyệt nếu có. | Cao |
| Appointment | Patient, Doctor, Administrator | Đặt lịch, xem lịch, hủy/xác nhận lịch nếu được phép, kiểm tra trùng lịch và trạng thái lịch hẹn. | Cao |
| Consultation | Patient, Doctor | Doctor bắt đầu phiên, Patient tham gia, chat/video, kiểm tra đúng người thuộc appointment và time-window. | Cao |
| Prescription | Doctor, Patient | Doctor ghi kết quả và đơn thuốc; Patient xem tóm tắt tư vấn và đơn thuốc của chính mình. | Cao |
| Rating | Patient, Administrator | Patient đánh giá sau khi tư vấn hoàn tất; kiểm tra chống đánh giá sai điều kiện hoặc trùng lặp nếu có. | Trung bình |
| Notification | Patient, Doctor, System | Thông báo tạo lịch, nhắc lịch, thông báo phản hồi câu hỏi và trạng thái đã đọc/chưa đọc. | Trung bình |
| Admin management | Administrator | Quản lý bác sĩ, bệnh nhân, chuyên khoa, lịch hẹn, nội dung và trạng thái tài khoản. | Cao |
| Reporting/dashboard | Administrator | Xem số liệu tổng quan, biểu đồ thống kê, lọc dữ liệu dashboard nếu có. | Trung bình |
| External integrations | System, External Services | Notification Service, Video Communication Service, File Storage Service; kiểm tra fallback khi dịch vụ phụ trợ chưa khả dụng. | Trung bình |

### 3.3. Loại kiểm thử áp dụng

Các loại kiểm thử được áp dụng hoặc định hướng áp dụng cho hệ thống gồm:

- Functional testing: kiểm tra từng chức năng theo yêu cầu SRS và expected result trong test case.
- UI testing: kiểm tra giao diện, form, điều hướng, thông báo lỗi, trạng thái nút và khả năng hiển thị dữ liệu.
- API testing: kiểm tra request/response, status code, validation, dữ liệu trả về và lỗi nghiệp vụ ở các endpoint.
- E2E testing: kiểm tra luồng hoàn chỉnh từ giao diện đến backend, ví dụ Guest tra cứu bác sĩ, Patient đặt lịch, Doctor phản hồi câu hỏi.
- Authorization testing: kiểm tra quyền truy cập theo vai trò và quyền sở hữu dữ liệu.
- Data privacy testing: kiểm tra response, UI và log không làm lộ password hash, token, dữ liệu sức khỏe hoặc dữ liệu của người dùng khác.
- Negative testing: kiểm tra trường hợp nhập sai, thiếu dữ liệu, truy cập sai quyền, appointment không hợp lệ hoặc trạng thái không cho phép.
- Regression testing: kiểm tra lại các chức năng quan trọng sau khi sửa lỗi hoặc cập nhật chức năng.
- Smoke testing: kiểm tra nhanh các luồng tối thiểu sau khi deploy hoặc sau khi khởi động môi trường.
- Unit/integration testing: kiểm tra logic service, validation, guard, lớp truy xuất dữ liệu và tích hợp module nếu có điều kiện triển khai.
- Performance testing: định hướng kiểm tra hiệu năng các API tra cứu, đặt lịch, dashboard ở mức tải phù hợp với MVP.
- Security testing: định hướng rà soát xác thực, phân quyền, dữ liệu nhạy cảm, lỗi validation, CORS và thông tin lỗi trả về.

### 3.4. Kỹ thuật thiết kế test case

Các kỹ thuật thiết kế test case được sử dụng gồm:

- Black-box testing: kiểm tra hệ thống theo input, output và hành vi mong đợi mà không phụ thuộc vào chi tiết cài đặt.
- Equivalence Partitioning: chia dữ liệu đầu vào thành các lớp tương đương hợp lệ và không hợp lệ, ví dụ email hợp lệ/sai định dạng, khung giờ còn trống/đã đặt.
- Boundary Value Analysis: kiểm tra giá trị biên như độ dài mật khẩu, độ dài tiêu đề câu hỏi, thời gian sớm/muộn khi tham gia tư vấn.
- Decision Table: thiết kế test cho các điều kiện kết hợp như role, trạng thái appointment, người sở hữu dữ liệu và hành động được phép.
- Cause-Effect Graph: áp dụng khi một kết quả phụ thuộc nhiều điều kiện, ví dụ cho phép tham gia phiên tư vấn khi đúng người, đúng lịch, đúng trạng thái và đúng thời gian.
- State transition testing: kiểm tra chuyển trạng thái của appointment, question và consultation session, ví dụ `PENDING` sang `ANSWERED`, `CONFIRMED` sang `COMPLETED`.
- Role-based access matrix testing: kiểm tra mỗi vai trò chỉ được truy cập đúng chức năng và dữ liệu theo ma trận phân quyền.

### 3.5. Công cụ kiểm thử

| Công cụ | Mục đích | Ghi chú |
|---|---|---|
| Playwright | Kiểm thử E2E trên giao diện web. | Ưu tiên các luồng đại diện như tra cứu bác sĩ, đặt lịch, gửi câu hỏi, phản hồi câu hỏi và tư vấn trực tuyến. |
| Postman | Kiểm thử API thủ công hoặc theo collection. | Phù hợp để kiểm tra auth, validation, phân quyền và response dữ liệu. |
| Jest | Kiểm thử unit/integration nếu có. | Dùng cho service, guard, validation và logic nghiệp vụ ở backend. |
| Swagger/OpenAPI | Tra cứu API contract và thử request cơ bản. | Hỗ trợ đối chiếu endpoint, input, output trước khi viết test case. |
| Browser DevTools | Kiểm tra UI, network request, console error và responsive. | Dùng khi kiểm thử thủ công trên trình duyệt. |
| Database client | Kiểm tra dữ liệu test trong database. | Chỉ dùng ở môi trường phát triển/kiểm thử, không thay đổi dữ liệu production. |
| k6/JMeter | Định hướng kiểm thử hiệu năng. | Dùng khi cần đo tải API public discovery, appointment hoặc dashboard. |

### 3.6. Môi trường kiểm thử

| Thành phần | Giá trị | Ghi chú |
|---|---|---|
| Frontend | React 18, TypeScript, Vite | Cổng phát triển thường dùng: `5173`, có thể thay đổi theo cấu hình. |
| Backend | Node.js, NestJS, REST API | Cổng backend: TODO. |
| Database | PostgreSQL, Prisma ORM | Dùng database riêng cho môi trường test/dev, không dùng dữ liệu production. |
| Browser | Chromium/Chrome, Firefox, WebKit nếu dùng Playwright | Cần kiểm tra tối thiểu trên một trình duyệt chính; mở rộng cross-browser nếu đủ thời gian. |
| OS | TODO | Ghi hệ điều hành của máy chạy test khi thực thi. |
| Test data | Tài khoản Guest, Patient, Doctor, Administrator; bác sĩ active/approved; chuyên khoa; appointment; câu hỏi; phiên tư vấn | Dữ liệu test cần có trạng thái rõ ràng và không chứa thông tin sức khỏe thật. |
| Test date | TODO | Cập nhật ngày chạy test khi có thực thi. |
| Tester | TODO | Cập nhật người thực hiện kiểm thử khi ghi nhận kết quả. |

### 3.7. Nguyên tắc ghi nhận kết quả

Nguyên tắc ghi nhận kết quả kiểm thử:

- Không ghi pass/fail nếu test case chưa được thực thi.
- Trạng thái `Pass` chỉ được ghi khi actual result khớp expected result.
- Trạng thái `Fail` được ghi khi actual result khác expected result và cần có bug report hoặc mô tả lỗi đi kèm.
- Trạng thái `Blocked` được dùng khi không thể chạy test do thiếu môi trường, thiếu dữ liệu, thiếu quyền hoặc chức năng phụ thuộc chưa sẵn sàng.
- Trạng thái `Not Run` được dùng cho test case đã thiết kế nhưng chưa chạy.
- Evidence của kiểm thử có thể gồm screenshot, log terminal, API response, Playwright HTML report, trace, video hoặc ghi chú tái hiện lỗi.
- Với dữ liệu sức khỏe, evidence cần che hoặc loại bỏ thông tin nhạy cảm trước khi đưa vào báo cáo.

## 4. Đặc tả test case theo actor/use case

### 4.1. Test case nhóm Guest User/Public Discovery

Nhóm test case này kiểm tra các chức năng public dành cho Guest User, gồm xem trang chủ, xem chuyên khoa, tra cứu bác sĩ và điều hướng đăng nhập khi thực hiện thao tác cần xác thực.

**Bảng tổng hợp test case**

| Mã test case | Tên test case | Use case liên quan | Mức ưu tiên |
|---|---|---|---|
| TC-G-001 | Guest xem trang chủ | UC-G-01 | Cao |
| TC-G-002 | Guest xem danh sách chuyên khoa | UC-G-02 | Cao |
| TC-G-003 | Guest tìm kiếm bác sĩ theo keyword | UC-G-03 | Cao |
| TC-G-004 | Guest lọc bác sĩ theo chuyên khoa | UC-G-03 | Cao |
| TC-G-005 | Guest xem chi tiết bác sĩ | UC-G-04 | Cao |
| TC-G-006 | Không hiển thị bác sĩ inactive/unapproved | UC-G-05 | Cao |
| TC-G-007 | Guest bị chuyển login khi đặt lịch | UC-G-06 | Cao |
| TC-G-008 | Guest bị chuyển login khi gửi câu hỏi | UC-G-06 | Cao |

#### TC-G-001: Guest xem trang chủ

**Mô tả:** Kiểm tra Guest User truy cập được trang chủ và xem các nội dung public của hệ thống.

**Tiền điều kiện:** Hệ thống đang hoạt động. Người dùng chưa đăng nhập.

**Bước thực hiện:**

1. Mở trang chủ hệ thống.
2. Quan sát các khu vực thông tin chính.
3. Kiểm tra đường dẫn đến chuyên khoa, danh sách bác sĩ, đăng nhập và đăng ký.

**Kỹ thuật, công cụ:** Functional testing, UI testing. Công cụ: Playwright hoặc kiểm thử thủ công trên trình duyệt.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G001-01 | Guest truy cập trang chủ | Không có token đăng nhập | Trang chủ hiển thị nội dung public, không yêu cầu đăng nhập | TODO: Cập nhật sau khi chạy test |

#### TC-G-002: Guest xem danh sách chuyên khoa

**Mô tả:** Kiểm tra Guest User xem được danh sách chuyên khoa đang active.

**Tiền điều kiện:** Có dữ liệu chuyên khoa active trong hệ thống. Người dùng chưa đăng nhập.

**Bước thực hiện:**

1. Mở trang danh sách chuyên khoa.
2. Quan sát danh sách chuyên khoa.
3. Chọn một chuyên khoa để xem thông tin liên quan.

**Kỹ thuật, công cụ:** Black-box testing, API/UI testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G002-01 | Có chuyên khoa active | Specialty active | Danh sách hiển thị chuyên khoa active | TODO: Cập nhật sau khi chạy test |
| G002-02 | Có chuyên khoa inactive | Specialty inactive | Chuyên khoa inactive không hiển thị ở public area | TODO: Cập nhật sau khi chạy test |

#### TC-G-003: Guest tìm kiếm bác sĩ theo keyword

**Mô tả:** Kiểm tra Guest User tìm kiếm bác sĩ theo tên hoặc từ khóa trong hồ sơ công khai.

**Tiền điều kiện:** Có bác sĩ active/approved có dữ liệu khớp từ khóa. Người dùng chưa đăng nhập.

**Bước thực hiện:**

1. Mở trang danh sách bác sĩ.
2. Nhập keyword vào ô tìm kiếm.
3. Thực hiện tìm kiếm.
4. Kiểm tra danh sách kết quả.

**Kỹ thuật, công cụ:** Equivalence Partitioning, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G003-01 | Keyword khớp bác sĩ | Tên bác sĩ tồn tại | Hiển thị bác sĩ phù hợp keyword | TODO: Cập nhật sau khi chạy test |
| G003-02 | Keyword không tồn tại | `doctor-not-found` | Hiển thị danh sách rỗng hoặc thông báo không tìm thấy | TODO: Cập nhật sau khi chạy test |
| G003-03 | Keyword có ký tự đặc biệt | `<script>` | Không thực thi script, không lỗi hệ thống | TODO: Cập nhật sau khi chạy test |

#### TC-G-004: Guest lọc bác sĩ theo chuyên khoa

**Mô tả:** Kiểm tra Guest User lọc danh sách bác sĩ theo chuyên khoa được chọn.

**Tiền điều kiện:** Có chuyên khoa active và có bác sĩ active/approved thuộc chuyên khoa đó.

**Bước thực hiện:**

1. Mở trang danh sách bác sĩ.
2. Chọn một chuyên khoa.
3. Quan sát danh sách bác sĩ sau khi lọc.
4. Kiểm tra bác sĩ hiển thị thuộc chuyên khoa đã chọn.

**Kỹ thuật, công cụ:** Black-box testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G004-01 | Chuyên khoa có bác sĩ active/approved | `specialtyId` hợp lệ | Chỉ hiển thị bác sĩ thuộc chuyên khoa đã chọn | TODO: Cập nhật sau khi chạy test |
| G004-02 | Chuyên khoa không có bác sĩ | `specialtyId` hợp lệ nhưng không có bác sĩ | Hiển thị danh sách rỗng hoặc thông báo phù hợp | TODO: Cập nhật sau khi chạy test |

#### TC-G-005: Guest xem chi tiết bác sĩ

**Mô tả:** Kiểm tra Guest User xem được hồ sơ công khai của bác sĩ active/approved.

**Tiền điều kiện:** Có bác sĩ active/approved và có hồ sơ công khai.

**Bước thực hiện:**

1. Mở trang danh sách bác sĩ.
2. Chọn một bác sĩ.
3. Mở trang chi tiết bác sĩ.
4. Kiểm tra thông tin hiển thị.

**Kỹ thuật, công cụ:** Functional testing, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G005-01 | Doctor active/approved | `doctorId` hợp lệ | Hiển thị hồ sơ công khai của bác sĩ | TODO: Cập nhật sau khi chạy test |
| G005-02 | Doctor không tồn tại | `doctorId` không hợp lệ | Hiển thị lỗi phù hợp hoặc thông báo không tìm thấy | TODO: Cập nhật sau khi chạy test |
| G005-03 | Dữ liệu private | Email nội bộ, dữ liệu bệnh nhân, token | Không hiển thị dữ liệu private | TODO: Cập nhật sau khi chạy test |

#### TC-G-006: Không hiển thị bác sĩ inactive/unapproved

**Mô tả:** Kiểm tra khu vực public chỉ hiển thị bác sĩ đang active và đã được duyệt.

**Tiền điều kiện:** Có dữ liệu bác sĩ active/approved, inactive hoặc unapproved.

**Bước thực hiện:**

1. Mở trang danh sách bác sĩ public.
2. Quan sát danh sách hiển thị.
3. Tìm bác sĩ inactive hoặc unapproved trong danh sách.

**Kỹ thuật, công cụ:** Decision Table, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G006-01 | Doctor active/approved | `status=active`, `approved=true` | Bác sĩ được hiển thị | TODO: Cập nhật sau khi chạy test |
| G006-02 | Doctor inactive | `status=inactive` | Bác sĩ không hiển thị ở public area | TODO: Cập nhật sau khi chạy test |
| G006-03 | Doctor chưa duyệt | `approved=false` | Bác sĩ không hiển thị ở public area | TODO: Cập nhật sau khi chạy test |

#### TC-G-007: Guest bị chuyển login khi đặt lịch

**Mô tả:** Kiểm tra Guest User không được đặt lịch tư vấn khi chưa đăng nhập.

**Tiền điều kiện:** Người dùng chưa đăng nhập. Có bác sĩ active/approved.

**Bước thực hiện:**

1. Mở trang chi tiết bác sĩ.
2. Chọn chức năng đặt lịch tư vấn.
3. Quan sát điều hướng hoặc thông báo của hệ thống.

**Kỹ thuật, công cụ:** Authorization testing, E2E testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G007-01 | Guest chọn đặt lịch trên UI | Không có token | Chuyển đến trang đăng nhập/đăng ký, không tạo appointment | TODO: Cập nhật sau khi chạy test |
| G007-02 | Guest gọi API đặt lịch | Request không có token | Từ chối xác thực, không tạo appointment | TODO: Cập nhật sau khi chạy test |

#### TC-G-008: Guest bị chuyển login khi gửi câu hỏi

**Mô tả:** Kiểm tra Guest User không được gửi câu hỏi sức khỏe khi chưa đăng nhập.

**Tiền điều kiện:** Người dùng chưa đăng nhập. Chức năng gửi câu hỏi có hiển thị trên giao diện hoặc có API tương ứng.

**Bước thực hiện:**

1. Chọn chức năng gửi câu hỏi sức khỏe.
2. Quan sát điều hướng hoặc thông báo.
3. Nếu kiểm thử API, gửi request tạo câu hỏi không kèm token.

**Kỹ thuật, công cụ:** Authorization testing, Negative testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| G008-01 | Guest chọn gửi câu hỏi trên UI | Không có token | Chuyển đến trang đăng nhập/đăng ký | TODO: Cập nhật sau khi chạy test |
| G008-02 | Guest gọi API gửi câu hỏi | Request không có token | Từ chối xác thực, không tạo câu hỏi | TODO: Cập nhật sau khi chạy test |

### 4.2. Test case nhóm Authentication & Authorization

Nhóm test case này kiểm tra đăng ký, đăng nhập, đăng xuất, truy cập tài nguyên bảo vệ và phân quyền theo vai trò. Trọng tâm là bảo đảm Patient, Doctor và Administrator chỉ được truy cập đúng chức năng và đúng dữ liệu thuộc phạm vi của mình.

**Bảng tổng hợp test case**

| Mã test case | Tên test case | Actor | Mức ưu tiên |
|---|---|---|---|
| TC-AUTH-001 | Patient đăng ký hợp lệ | Patient | Cao |
| TC-AUTH-002 | Đăng ký thiếu thông tin hoặc sai email | Patient | Cao |
| TC-AUTH-003 | Đăng nhập hợp lệ | Patient/Doctor/Admin | Cao |
| TC-AUTH-004 | Đăng nhập sai mật khẩu | Patient/Doctor/Admin | Cao |
| TC-AUTH-005 | Đăng xuất | Patient/Doctor/Admin | Trung bình |
| TC-AUTH-006 | Protected API không token bị từ chối | Patient/Doctor/Admin | Cao |
| TC-AUTH-007 | Patient không truy cập chức năng Doctor/Admin | Patient | Cao |
| TC-AUTH-008 | Doctor không truy cập dữ liệu Patient khác | Doctor | Cao |
| TC-AUTH-009 | Admin truy cập admin module | Administrator | Cao |

#### TC-AUTH-001: Patient đăng ký hợp lệ

**Mô tả:** Kiểm tra người dùng đăng ký tài khoản Patient với dữ liệu hợp lệ.

**Tiền điều kiện:** Email đăng ký chưa tồn tại. Hệ thống cho phép Patient tự đăng ký.

**Bước thực hiện:**

1. Mở trang đăng ký.
2. Nhập họ tên, email, số điện thoại, mật khẩu và xác nhận mật khẩu hợp lệ.
3. Gửi form đăng ký.
4. Kiểm tra thông báo hoặc điều hướng sau đăng ký.

**Kỹ thuật, công cụ:** Equivalence Partitioning, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH001-01 | Dữ liệu hợp lệ | Email mới, mật khẩu hợp lệ | Tạo tài khoản Patient thành công hoặc chuyển sang bước đăng nhập theo thiết kế | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-002: Đăng ký thiếu thông tin hoặc sai email

**Mô tả:** Kiểm tra validation khi đăng ký thiếu trường bắt buộc hoặc email sai định dạng.

**Tiền điều kiện:** Trang/API đăng ký hoạt động.

**Bước thực hiện:**

1. Mở trang đăng ký.
2. Nhập dữ liệu thiếu trường hoặc sai định dạng.
3. Gửi form đăng ký.
4. Kiểm tra thông báo validation.

**Kỹ thuật, công cụ:** Equivalence Partitioning, Boundary Value Analysis. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH002-01 | Thiếu email | Email rỗng | Hiển thị lỗi validation, không tạo tài khoản | TODO: Cập nhật sau khi chạy test |
| AUTH002-02 | Email sai định dạng | `abc-email` | Hiển thị lỗi email không hợp lệ | TODO: Cập nhật sau khi chạy test |
| AUTH002-03 | Mật khẩu quá ngắn | Mật khẩu dưới giới hạn | Hiển thị lỗi validation mật khẩu | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-003: Đăng nhập hợp lệ

**Mô tả:** Kiểm tra người dùng đăng nhập thành công bằng tài khoản hợp lệ.

**Tiền điều kiện:** Có tài khoản Patient, Doctor hoặc Administrator hợp lệ.

**Bước thực hiện:**

1. Mở trang đăng nhập.
2. Nhập email và mật khẩu hợp lệ.
3. Gửi form đăng nhập.
4. Kiểm tra người dùng được điều hướng đúng theo vai trò.

**Kỹ thuật, công cụ:** Functional testing, Role-based access matrix testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH003-01 | Patient đăng nhập | Tài khoản Patient hợp lệ | Đăng nhập thành công, vào khu vực Patient | TODO: Cập nhật sau khi chạy test |
| AUTH003-02 | Doctor đăng nhập | Tài khoản Doctor hợp lệ | Đăng nhập thành công, vào khu vực Doctor | TODO: Cập nhật sau khi chạy test |
| AUTH003-03 | Admin đăng nhập | Tài khoản Admin hợp lệ | Đăng nhập thành công, vào khu vực Admin | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-004: Đăng nhập sai mật khẩu

**Mô tả:** Kiểm tra hệ thống từ chối đăng nhập khi mật khẩu không đúng.

**Tiền điều kiện:** Có tài khoản tồn tại.

**Bước thực hiện:**

1. Mở trang đăng nhập.
2. Nhập email hợp lệ.
3. Nhập mật khẩu sai.
4. Gửi form đăng nhập.
5. Kiểm tra thông báo lỗi.

**Kỹ thuật, công cụ:** Negative testing, Security testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH004-01 | Sai mật khẩu | Email tồn tại, password sai | Đăng nhập thất bại, không cấp token/session | TODO: Cập nhật sau khi chạy test |
| AUTH004-02 | Email không tồn tại | Email chưa đăng ký | Đăng nhập thất bại, không tiết lộ thông tin nội bộ | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-005: Đăng xuất

**Mô tả:** Kiểm tra người dùng đăng xuất khỏi hệ thống.

**Tiền điều kiện:** Người dùng đã đăng nhập.

**Bước thực hiện:**

1. Đăng nhập bằng tài khoản hợp lệ.
2. Chọn chức năng đăng xuất.
3. Kiểm tra người dùng được đưa về trạng thái chưa đăng nhập.
4. Thử truy cập lại trang protected.

**Kỹ thuật, công cụ:** Functional testing, E2E testing. Công cụ: Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH005-01 | User đăng xuất | Session/token đang hoạt động | Đăng xuất thành công, không truy cập được trang protected nếu chưa đăng nhập lại | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-006: Protected API không token bị từ chối

**Mô tả:** Kiểm tra các API hoặc trang protected không cho phép truy cập khi thiếu token.

**Tiền điều kiện:** Có endpoint/trang yêu cầu xác thực.

**Bước thực hiện:**

1. Không đăng nhập hoặc xóa token.
2. Truy cập trang/API protected.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Authorization testing, Negative testing. Công cụ: Postman, Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH006-01 | Truy cập không token | Protected endpoint/page | Bị từ chối truy cập hoặc chuyển về đăng nhập | TODO: Cập nhật sau khi chạy test |
| AUTH006-02 | Token sai định dạng | Token không hợp lệ | Bị từ chối xác thực | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-007: Patient không truy cập chức năng Doctor/Admin

**Mô tả:** Kiểm tra Patient không được truy cập các chức năng dành cho Doctor hoặc Administrator.

**Tiền điều kiện:** Patient đã đăng nhập. Có trang/API Doctor/Admin protected.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Truy cập trang quản lý lịch của bác sĩ hoặc admin module.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Role-based access matrix testing, Decision Table. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH007-01 | Patient truy cập Doctor module | Token Patient | Từ chối truy cập | TODO: Cập nhật sau khi chạy test |
| AUTH007-02 | Patient truy cập Admin module | Token Patient | Từ chối truy cập | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-008: Doctor không truy cập dữ liệu Patient khác

**Mô tả:** Kiểm tra Doctor không được xem dữ liệu sức khỏe hoặc hồ sơ của Patient ngoài phạm vi phụ trách.

**Tiền điều kiện:** Doctor đã đăng nhập. Có Patient không thuộc lịch/câu hỏi/phiên tư vấn của Doctor.

**Bước thực hiện:**

1. Đăng nhập bằng Doctor.
2. Thử truy cập hồ sơ hoặc dữ liệu tư vấn của Patient không liên quan.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Data privacy testing, Authorization testing. Công cụ: Postman, Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH008-01 | Doctor truy cập Patient ngoài phạm vi | Token Doctor, `patientId` không thuộc phạm vi | Từ chối truy cập hoặc không trả dữ liệu nhạy cảm | TODO: Cập nhật sau khi chạy test |
| AUTH008-02 | Doctor truy cập Patient thuộc lịch/câu hỏi phụ trách | Token Doctor, `patientId` hợp lệ theo phạm vi | Cho phép xem phần dữ liệu cần thiết theo nghiệp vụ | TODO: Cập nhật sau khi chạy test |

#### TC-AUTH-009: Admin truy cập admin module

**Mô tả:** Kiểm tra Administrator truy cập được module quản trị.

**Tiền điều kiện:** Có tài khoản Administrator hợp lệ.

**Bước thực hiện:**

1. Đăng nhập bằng Administrator.
2. Mở admin module.
3. Kiểm tra các menu quản lý chính.

**Kỹ thuật, công cụ:** Functional testing, Authorization testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| AUTH009-01 | Admin truy cập dashboard quản trị | Token Admin | Cho phép truy cập admin module | TODO: Cập nhật sau khi chạy test |
| AUTH009-02 | User không phải Admin truy cập admin module | Token Patient/Doctor | Từ chối truy cập | TODO: Cập nhật sau khi chạy test |

### 4.3. Test case nhóm Patient/Doctor Profile

Nhóm test case này kiểm tra hồ sơ bệnh nhân, hồ sơ bác sĩ và các ràng buộc phân quyền liên quan đến dữ liệu cá nhân. Đây là nhóm chức năng có liên quan trực tiếp đến dữ liệu sức khỏe nên cần chú trọng ownership và privacy.

**Bảng tổng hợp test case**

| Mã test case | Tên test case | Actor | Mức ưu tiên |
|---|---|---|---|
| TC-PRO-001 | Patient xem hồ sơ cá nhân | Patient | Cao |
| TC-PRO-002 | Patient cập nhật hồ sơ hợp lệ | Patient | Cao |
| TC-PRO-003 | Patient không xem hồ sơ Patient khác | Patient | Cao |
| TC-PRO-004 | Doctor xem hồ sơ chuyên môn của mình | Doctor | Cao |
| TC-PRO-005 | Doctor cập nhật hồ sơ chuyên môn | Doctor | Cao |
| TC-PRO-006 | Guest chỉ xem hồ sơ public của Doctor | Guest User | Cao |
| TC-PRO-007 | Admin quản lý trạng thái bác sĩ | Administrator | Trung bình |

#### TC-PRO-001: Patient xem hồ sơ cá nhân

**Mô tả:** Kiểm tra Patient xem được hồ sơ cá nhân và hồ sơ sức khỏe của chính mình.

**Tiền điều kiện:** Patient đã đăng nhập và có hồ sơ cá nhân.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Mở trang hồ sơ bệnh nhân.
3. Kiểm tra thông tin hiển thị.

**Kỹ thuật, công cụ:** Functional testing, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO001-01 | Patient xem hồ sơ của mình | Token Patient | Hiển thị hồ sơ cá nhân của Patient đang đăng nhập | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-002: Patient cập nhật hồ sơ hợp lệ

**Mô tả:** Kiểm tra Patient cập nhật thông tin cá nhân hoặc thông tin sức khỏe hợp lệ.

**Tiền điều kiện:** Patient đã đăng nhập.

**Bước thực hiện:**

1. Mở trang hồ sơ bệnh nhân.
2. Nhập thông tin hợp lệ.
3. Lưu thay đổi.
4. Tải lại trang và kiểm tra dữ liệu đã cập nhật.

**Kỹ thuật, công cụ:** Functional testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO002-01 | Dữ liệu hồ sơ hợp lệ | Họ tên, ngày sinh, số điện thoại, tiền sử bệnh hợp lệ | Cập nhật hồ sơ thành công | TODO: Cập nhật sau khi chạy test |
| PRO002-02 | Số điện thoại sai định dạng | Số điện thoại không hợp lệ | Hiển thị lỗi validation, không lưu dữ liệu sai | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-003: Patient không xem hồ sơ Patient khác

**Mô tả:** Kiểm tra Patient không được truy cập hồ sơ của Patient khác.

**Tiền điều kiện:** Có Patient A và Patient B. Patient A đã đăng nhập.

**Bước thực hiện:**

1. Đăng nhập bằng Patient A.
2. Thử truy cập hồ sơ Patient B bằng URL/API nếu biết ID.
3. Kiểm tra phản hồi.

**Kỹ thuật, công cụ:** Authorization testing, Data privacy testing. Công cụ: Postman, Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO003-01 | Patient A truy cập hồ sơ Patient B | Token Patient A, `patientId` của Patient B | Từ chối truy cập hoặc không trả dữ liệu | TODO: Cập nhật sau khi chạy test |
| PRO003-02 | Patient A truy cập hồ sơ của mình | Token Patient A | Cho phép xem hồ sơ | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-004: Doctor xem hồ sơ chuyên môn của mình

**Mô tả:** Kiểm tra Doctor xem được hồ sơ chuyên môn của chính mình.

**Tiền điều kiện:** Doctor đã đăng nhập và có hồ sơ bác sĩ.

**Bước thực hiện:**

1. Đăng nhập bằng Doctor.
2. Mở trang hồ sơ bác sĩ.
3. Kiểm tra thông tin chuyên môn hiển thị.

**Kỹ thuật, công cụ:** Functional testing, Authorization testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO004-01 | Doctor xem hồ sơ của mình | Token Doctor | Hiển thị hồ sơ chuyên môn của Doctor đang đăng nhập | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-005: Doctor cập nhật hồ sơ chuyên môn

**Mô tả:** Kiểm tra Doctor cập nhật thông tin chuyên môn hợp lệ.

**Tiền điều kiện:** Doctor đã đăng nhập.

**Bước thực hiện:**

1. Mở trang hồ sơ bác sĩ.
2. Cập nhật chuyên môn, kinh nghiệm hoặc mô tả dịch vụ.
3. Lưu thay đổi.
4. Kiểm tra dữ liệu sau khi cập nhật.

**Kỹ thuật, công cụ:** Functional testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO005-01 | Thông tin chuyên môn hợp lệ | Chuyên môn, kinh nghiệm, mô tả hợp lệ | Cập nhật hồ sơ bác sĩ thành công | TODO: Cập nhật sau khi chạy test |
| PRO005-02 | Dữ liệu bắt buộc bị bỏ trống | Tên hiển thị/chuyên môn rỗng nếu bắt buộc | Hiển thị lỗi validation | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-006: Guest chỉ xem hồ sơ public của Doctor

**Mô tả:** Kiểm tra Guest User chỉ xem được thông tin công khai của Doctor, không xem dữ liệu private.

**Tiền điều kiện:** Có Doctor active/approved với hồ sơ public.

**Bước thực hiện:**

1. Không đăng nhập.
2. Mở trang chi tiết bác sĩ.
3. Kiểm tra các trường thông tin hiển thị.
4. Đối chiếu không có dữ liệu private.

**Kỹ thuật, công cụ:** Data privacy testing, UI/API testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO006-01 | Guest xem doctor public | `doctorId` active/approved | Hiển thị thông tin công khai | TODO: Cập nhật sau khi chạy test |
| PRO006-02 | Trường private | Email nội bộ, dữ liệu hệ thống, token | Không hiển thị hoặc trả về trường private | TODO: Cập nhật sau khi chạy test |

#### TC-PRO-007: Admin quản lý trạng thái bác sĩ

**Mô tả:** Kiểm tra Administrator thay đổi trạng thái hoạt động hoặc trạng thái duyệt của bác sĩ nếu hệ thống hỗ trợ.

**Tiền điều kiện:** Administrator đã đăng nhập. Có tài khoản Doctor trong hệ thống.

**Bước thực hiện:**

1. Đăng nhập bằng Administrator.
2. Mở trang quản lý bác sĩ.
3. Chọn một bác sĩ.
4. Cập nhật trạng thái active/approved.
5. Kiểm tra ảnh hưởng ở khu vực public.

**Kỹ thuật, công cụ:** Functional testing, Decision Table. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| PRO007-01 | Admin duyệt bác sĩ | `approved=true`, `active=true` | Bác sĩ có thể hiển thị ở public nếu đủ điều kiện | TODO: Cập nhật sau khi chạy test |
| PRO007-02 | Admin vô hiệu hóa bác sĩ | `active=false` | Bác sĩ không hiển thị ở public area | TODO: Cập nhật sau khi chạy test |

### 4.4. Test case nhóm Specialty & Doctor Discovery

Nhóm test case này kiểm tra quản lý chuyên khoa và khả năng tra cứu bác sĩ theo chuyên khoa. Trọng tâm là bảo đảm dữ liệu chuyên khoa và bác sĩ hiển thị ở public area đúng trạng thái active/approved.

**Bảng tổng hợp test case**

| Mã test case | Tên test case | Actor | Mức ưu tiên |
|---|---|---|---|
| TC-SPE-001 | Admin tạo chuyên khoa | Administrator | Cao |
| TC-SPE-002 | Admin cập nhật chuyên khoa | Administrator | Trung bình |
| TC-SPE-003 | Admin vô hiệu hóa chuyên khoa | Administrator | Cao |
| TC-SPE-004 | Guest/Patient chỉ thấy chuyên khoa active | Guest User/Patient | Cao |
| TC-SPE-005 | Bác sĩ được gắn chuyên khoa | Administrator/Doctor | Cao |
| TC-SPE-006 | Tìm kiếm bác sĩ theo chuyên khoa | Guest User/Patient | Cao |
| TC-SPE-007 | Không hiển thị bác sĩ không active/approved | Guest User/Patient | Cao |

#### TC-SPE-001: Admin tạo chuyên khoa

**Mô tả:** Kiểm tra Administrator tạo chuyên khoa mới với dữ liệu hợp lệ.

**Tiền điều kiện:** Administrator đã đăng nhập. Tên chuyên khoa chưa tồn tại hoặc được phép tạo theo rule.

**Bước thực hiện:**

1. Mở trang quản lý chuyên khoa.
2. Chọn tạo chuyên khoa.
3. Nhập tên, mô tả và trạng thái.
4. Lưu chuyên khoa.

**Kỹ thuật, công cụ:** Functional testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE001-01 | Dữ liệu hợp lệ | Tên chuyên khoa mới, mô tả hợp lệ | Tạo chuyên khoa thành công | TODO: Cập nhật sau khi chạy test |
| SPE001-02 | Thiếu tên chuyên khoa | Tên rỗng | Hiển thị lỗi validation, không tạo chuyên khoa | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-002: Admin cập nhật chuyên khoa

**Mô tả:** Kiểm tra Administrator cập nhật thông tin chuyên khoa.

**Tiền điều kiện:** Administrator đã đăng nhập. Có chuyên khoa tồn tại.

**Bước thực hiện:**

1. Mở trang quản lý chuyên khoa.
2. Chọn một chuyên khoa.
3. Cập nhật tên hoặc mô tả.
4. Lưu thay đổi.
5. Kiểm tra dữ liệu sau cập nhật.

**Kỹ thuật, công cụ:** Functional testing, Regression testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE002-01 | Cập nhật dữ liệu hợp lệ | Tên/mô tả mới | Chuyên khoa được cập nhật thành công | TODO: Cập nhật sau khi chạy test |
| SPE002-02 | Chuyên khoa không tồn tại | `specialtyId` không hợp lệ | Trả lỗi phù hợp, không cập nhật dữ liệu | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-003: Admin vô hiệu hóa chuyên khoa

**Mô tả:** Kiểm tra Administrator vô hiệu hóa chuyên khoa và chuyên khoa đó không còn xuất hiện ở khu vực public.

**Tiền điều kiện:** Administrator đã đăng nhập. Có chuyên khoa active.

**Bước thực hiện:**

1. Mở trang quản lý chuyên khoa.
2. Chọn chuyên khoa active.
3. Chuyển trạng thái sang inactive.
4. Mở danh sách chuyên khoa public để kiểm tra.

**Kỹ thuật, công cụ:** State transition testing, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE003-01 | Chuyên khoa active chuyển inactive | `active=false` | Chuyên khoa không hiển thị ở public area | TODO: Cập nhật sau khi chạy test |
| SPE003-02 | Chuyên khoa inactive được kích hoạt lại | `active=true` | Chuyên khoa hiển thị lại nếu đủ điều kiện | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-004: Guest/Patient chỉ thấy chuyên khoa active

**Mô tả:** Kiểm tra danh sách chuyên khoa public chỉ hiển thị chuyên khoa active.

**Tiền điều kiện:** Có chuyên khoa active và inactive.

**Bước thực hiện:**

1. Truy cập danh sách chuyên khoa bằng Guest hoặc Patient.
2. Quan sát danh sách hiển thị.
3. Kiểm tra chuyên khoa inactive không xuất hiện.

**Kỹ thuật, công cụ:** Black-box testing, Decision Table. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE004-01 | Chuyên khoa active | `active=true` | Hiển thị trong danh sách public | TODO: Cập nhật sau khi chạy test |
| SPE004-02 | Chuyên khoa inactive | `active=false` | Không hiển thị trong danh sách public | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-005: Bác sĩ được gắn chuyên khoa

**Mô tả:** Kiểm tra bác sĩ được gắn với một hoặc nhiều chuyên khoa theo dữ liệu quản trị.

**Tiền điều kiện:** Có Doctor active/approved và có Specialty active.

**Bước thực hiện:**

1. Đăng nhập bằng Administrator hoặc Doctor nếu hệ thống cho phép cập nhật hồ sơ chuyên môn.
2. Gắn bác sĩ với chuyên khoa.
3. Lưu thay đổi.
4. Kiểm tra chuyên khoa hiển thị trong hồ sơ bác sĩ.

**Kỹ thuật, công cụ:** Functional testing, Integration testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE005-01 | Gắn chuyên khoa hợp lệ | Doctor active/approved, Specialty active | Bác sĩ được liên kết với chuyên khoa | TODO: Cập nhật sau khi chạy test |
| SPE005-02 | Gắn chuyên khoa không tồn tại | `specialtyId` không hợp lệ | Từ chối cập nhật, không tạo liên kết sai | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-006: Tìm kiếm bác sĩ theo chuyên khoa

**Mô tả:** Kiểm tra Guest/Patient tìm thấy bác sĩ khi lọc theo chuyên khoa.

**Tiền điều kiện:** Có bác sĩ active/approved thuộc chuyên khoa active.

**Bước thực hiện:**

1. Mở danh sách bác sĩ.
2. Chọn chuyên khoa.
3. Quan sát kết quả tìm kiếm.
4. Kiểm tra bác sĩ trong kết quả thuộc chuyên khoa đã chọn.

**Kỹ thuật, công cụ:** Black-box testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE006-01 | Chuyên khoa có bác sĩ phù hợp | `specialtyId` active | Hiển thị bác sĩ thuộc chuyên khoa | TODO: Cập nhật sau khi chạy test |
| SPE006-02 | Chuyên khoa không có bác sĩ phù hợp | `specialtyId` active nhưng không có doctor | Hiển thị danh sách rỗng hoặc thông báo phù hợp | TODO: Cập nhật sau khi chạy test |

#### TC-SPE-007: Không hiển thị bác sĩ không active/approved

**Mô tả:** Kiểm tra bác sĩ không active hoặc chưa được duyệt không xuất hiện trong kết quả discovery, kể cả khi được gắn chuyên khoa.

**Tiền điều kiện:** Có bác sĩ inactive/unapproved được gắn với chuyên khoa active.

**Bước thực hiện:**

1. Mở trang danh sách bác sĩ.
2. Lọc theo chuyên khoa có bác sĩ inactive/unapproved.
3. Quan sát danh sách kết quả.
4. Kiểm tra bác sĩ không đủ điều kiện không hiển thị.

**Kỹ thuật, công cụ:** Decision Table, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| SPE007-01 | Doctor active/approved thuộc chuyên khoa | `active=true`, `approved=true` | Bác sĩ hiển thị trong kết quả | TODO: Cập nhật sau khi chạy test |
| SPE007-02 | Doctor inactive thuộc chuyên khoa | `active=false` | Bác sĩ không hiển thị | TODO: Cập nhật sau khi chạy test |
| SPE007-03 | Doctor chưa duyệt thuộc chuyên khoa | `approved=false` | Bác sĩ không hiển thị | TODO: Cập nhật sau khi chạy test |

### 4.5. Test case nhóm Health Question

Nhóm test case này kiểm tra luồng Patient gửi câu hỏi sức khỏe, Doctor tiếp nhận và phản hồi câu hỏi, Patient xem phản hồi, đồng thời kiểm tra các ràng buộc phân quyền và trạng thái của câu hỏi.

**Bảng tổng hợp test case**

| Test ID | Tên test case | Loại test | Actor | Ưu tiên |
|---|---|---|---|---|
| TC-HQ-001 | Patient gửi câu hỏi hợp lệ | Positive | Patient | Cao |
| TC-HQ-002 | Guest không được gửi câu hỏi | Negative | Guest User | Cao |
| TC-HQ-003 | Nội dung câu hỏi rỗng bị từ chối | Negative | Patient | Cao |
| TC-HQ-004 | Câu hỏi mới có trạng thái PENDING | Positive | Patient | Cao |
| TC-HQ-005 | Patient xem danh sách câu hỏi của mình | Positive | Patient | Cao |
| TC-HQ-006 | Patient không xem câu hỏi Patient khác | Negative | Patient | Cao |
| TC-HQ-007 | Doctor xem câu hỏi được phân công hoặc có thể xử lý | Positive | Doctor | Cao |
| TC-HQ-008 | Doctor phản hồi câu hỏi | Positive | Doctor | Cao |
| TC-HQ-009 | Doctor không phản hồi câu hỏi không thuộc phạm vi | Negative | Doctor | Cao |
| TC-HQ-010 | Câu hỏi chuyển PENDING sang ANSWERED | Positive | Doctor | Cao |
| TC-HQ-011 | Patient xem phản hồi của bác sĩ | Positive | Patient | Cao |
| TC-HQ-012 | Admin kiểm duyệt câu hỏi/phản hồi nếu có | Positive/Negative | Administrator | Trung bình |

#### TC-HQ-001: Patient gửi câu hỏi hợp lệ

**Mô tả:** Kiểm tra Patient gửi câu hỏi sức khỏe với tiêu đề và nội dung hợp lệ.

**Tiền điều kiện:** Patient đã đăng nhập. Hệ thống có chức năng gửi câu hỏi.

**Bước thực hiện:**

1. Đăng nhập bằng tài khoản Patient.
2. Mở trang gửi câu hỏi sức khỏe.
3. Nhập tiêu đề và nội dung câu hỏi hợp lệ.
4. Chọn bác sĩ nếu luồng cho phép.
5. Gửi câu hỏi.

**Kỹ thuật, công cụ:** Functional testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ001-01 | Câu hỏi hợp lệ, không chọn bác sĩ cụ thể | `title`, `content` hợp lệ, `doctorId` rỗng | Câu hỏi được tạo và gắn với Patient hiện tại | TODO: Cập nhật sau khi chạy test |
| HQ001-02 | Câu hỏi hợp lệ, chọn bác sĩ | `title`, `content`, `doctorId` hợp lệ | Câu hỏi được tạo và gắn phạm vi xử lý phù hợp | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-002: Guest không được gửi câu hỏi

**Mô tả:** Kiểm tra hệ thống từ chối Guest User gửi câu hỏi sức khỏe.

**Tiền điều kiện:** Người dùng chưa đăng nhập.

**Bước thực hiện:**

1. Truy cập chức năng gửi câu hỏi khi chưa đăng nhập.
2. Chọn gửi câu hỏi hoặc gọi API tạo câu hỏi không có token.
3. Quan sát phản hồi của hệ thống.

**Kỹ thuật, công cụ:** Authorization testing, Negative testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ002-01 | Guest gửi câu hỏi từ UI | Không có token | Chuyển đến đăng nhập/đăng ký, không tạo câu hỏi | TODO: Cập nhật sau khi chạy test |
| HQ002-02 | Guest gọi API tạo câu hỏi | Request không có token | Từ chối xác thực, không tạo câu hỏi | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-003: Nội dung câu hỏi rỗng bị từ chối

**Mô tả:** Kiểm tra validation khi Patient gửi câu hỏi thiếu tiêu đề hoặc nội dung.

**Tiền điều kiện:** Patient đã đăng nhập.

**Bước thực hiện:**

1. Mở form gửi câu hỏi.
2. Bỏ trống tiêu đề hoặc nội dung.
3. Gửi form.
4. Kiểm tra thông báo validation.

**Kỹ thuật, công cụ:** Equivalence Partitioning, Boundary Value Analysis. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ003-01 | Thiếu tiêu đề | `title=""` | Hiển thị lỗi validation, không tạo câu hỏi | TODO: Cập nhật sau khi chạy test |
| HQ003-02 | Thiếu nội dung | `content=""` | Hiển thị lỗi validation, không tạo câu hỏi | TODO: Cập nhật sau khi chạy test |
| HQ003-03 | Nội dung chỉ có khoảng trắng | `content="   "` | Từ chối dữ liệu không hợp lệ | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-004: Câu hỏi mới có trạng thái PENDING

**Mô tả:** Kiểm tra trạng thái ban đầu của câu hỏi sau khi Patient gửi thành công.

**Tiền điều kiện:** Patient đã đăng nhập. Dữ liệu câu hỏi hợp lệ.

**Bước thực hiện:**

1. Patient gửi câu hỏi hợp lệ.
2. Mở danh sách câu hỏi của Patient hoặc xem response API.
3. Kiểm tra trạng thái của câu hỏi mới.

**Kỹ thuật, công cụ:** State transition testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ004-01 | Câu hỏi mới tạo | Question hợp lệ | Trạng thái ban đầu là `PENDING` hoặc trạng thái chờ xử lý tương ứng theo thiết kế | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-005: Patient xem danh sách câu hỏi của mình

**Mô tả:** Kiểm tra Patient xem được danh sách câu hỏi do chính mình gửi.

**Tiền điều kiện:** Patient đã đăng nhập và có ít nhất một câu hỏi.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Mở trang danh sách câu hỏi của tôi.
3. Quan sát danh sách câu hỏi.
4. Kiểm tra các câu hỏi hiển thị thuộc Patient hiện tại.

**Kỹ thuật, công cụ:** Functional testing, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ005-01 | Patient có câu hỏi | Token Patient A | Chỉ hiển thị câu hỏi của Patient A | TODO: Cập nhật sau khi chạy test |
| HQ005-02 | Patient chưa có câu hỏi | Token Patient không có dữ liệu | Hiển thị danh sách rỗng hoặc thông báo phù hợp | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-006: Patient không xem câu hỏi Patient khác

**Mô tả:** Kiểm tra Patient không được truy cập câu hỏi của Patient khác.

**Tiền điều kiện:** Có Patient A, Patient B. Patient B có câu hỏi. Patient A đã đăng nhập.

**Bước thực hiện:**

1. Đăng nhập bằng Patient A.
2. Thử truy cập câu hỏi của Patient B bằng URL hoặc API nếu biết ID.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Authorization testing, Data privacy testing. Công cụ: Postman, Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ006-01 | Patient A truy cập câu hỏi Patient B | Token Patient A, questionId của Patient B | Từ chối truy cập hoặc không trả dữ liệu | TODO: Cập nhật sau khi chạy test |
| HQ006-02 | Patient B truy cập câu hỏi của mình | Token Patient B, questionId của Patient B | Cho phép xem câu hỏi | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-007: Doctor xem câu hỏi được phân công hoặc có thể xử lý

**Mô tả:** Kiểm tra Doctor xem được các câu hỏi thuộc phạm vi xử lý của mình.

**Tiền điều kiện:** Doctor đã đăng nhập. Có câu hỏi được phân công cho Doctor hoặc câu hỏi mở theo rule.

**Bước thực hiện:**

1. Đăng nhập bằng Doctor.
2. Mở trang danh sách câu hỏi cần phản hồi.
3. Kiểm tra danh sách câu hỏi hiển thị.

**Kỹ thuật, công cụ:** Authorization testing, Decision Table. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ007-01 | Câu hỏi gán cho Doctor hiện tại | `doctorId=Doctor A` | Hiển thị trong danh sách Doctor A | TODO: Cập nhật sau khi chạy test |
| HQ007-02 | Câu hỏi mở có thể xử lý | `doctorId=null`, status phù hợp | Hiển thị nếu rule cho phép Doctor xử lý | TODO: Cập nhật sau khi chạy test |
| HQ007-03 | Câu hỏi gán Doctor khác | `doctorId=Doctor B` | Không hiển thị trong danh sách Doctor A | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-008: Doctor phản hồi câu hỏi

**Mô tả:** Kiểm tra Doctor gửi phản hồi cho câu hỏi thuộc phạm vi xử lý.

**Tiền điều kiện:** Doctor đã đăng nhập. Có câu hỏi đang chờ phản hồi thuộc phạm vi của Doctor.

**Bước thực hiện:**

1. Mở danh sách câu hỏi cần phản hồi.
2. Chọn một câu hỏi hợp lệ.
3. Nhập nội dung phản hồi.
4. Gửi phản hồi.
5. Kiểm tra phản hồi được lưu trong chi tiết câu hỏi.

**Kỹ thuật, công cụ:** Functional testing, Equivalence Partitioning. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ008-01 | Phản hồi hợp lệ | Nội dung phản hồi hợp lệ | Lưu phản hồi và gắn với Doctor hiện tại | TODO: Cập nhật sau khi chạy test |
| HQ008-02 | Phản hồi rỗng | `content=""` | Hiển thị lỗi validation, không lưu phản hồi | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-009: Doctor không phản hồi câu hỏi không thuộc phạm vi

**Mô tả:** Kiểm tra Doctor không được phản hồi câu hỏi ngoài phạm vi được phân công hoặc xử lý.

**Tiền điều kiện:** Doctor A đã đăng nhập. Có câu hỏi thuộc Doctor B hoặc không thuộc phạm vi Doctor A.

**Bước thực hiện:**

1. Đăng nhập bằng Doctor A.
2. Thử mở hoặc gửi phản hồi cho câu hỏi ngoài phạm vi.
3. Quan sát phản hồi hệ thống.
4. Kiểm tra không có phản hồi mới được lưu.

**Kỹ thuật, công cụ:** Authorization testing, Negative testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ009-01 | Doctor A phản hồi câu hỏi của Doctor B | Token Doctor A, questionId gán Doctor B | Từ chối thao tác, không lưu phản hồi | TODO: Cập nhật sau khi chạy test |
| HQ009-02 | Doctor A phản hồi câu hỏi đã đóng | Status `CLOSED` nếu có | Từ chối nếu trạng thái không cho phép phản hồi | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-010: Câu hỏi chuyển PENDING sang ANSWERED

**Mô tả:** Kiểm tra trạng thái câu hỏi chuyển đúng sau khi Doctor phản hồi thành công.

**Tiền điều kiện:** Có câu hỏi trạng thái `PENDING`. Doctor có quyền phản hồi.

**Bước thực hiện:**

1. Doctor phản hồi câu hỏi `PENDING`.
2. Mở lại chi tiết câu hỏi.
3. Kiểm tra trạng thái sau phản hồi.

**Kỹ thuật, công cụ:** State transition testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ010-01 | Doctor phản hồi câu hỏi PENDING | From `PENDING`, action trả lời | Trạng thái chuyển sang `ANSWERED` hoặc trạng thái đã phản hồi theo thiết kế | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-011: Patient xem phản hồi của bác sĩ

**Mô tả:** Kiểm tra Patient xem được phản hồi của bác sĩ cho câu hỏi do mình gửi.

**Tiền điều kiện:** Patient đã đăng nhập. Câu hỏi của Patient đã có phản hồi.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Mở danh sách câu hỏi của tôi.
3. Chọn câu hỏi đã được phản hồi.
4. Kiểm tra nội dung phản hồi.

**Kỹ thuật, công cụ:** Functional testing, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ011-01 | Patient xem phản hồi câu hỏi của mình | Token Patient, question đã ANSWERED | Hiển thị phản hồi của bác sĩ | TODO: Cập nhật sau khi chạy test |
| HQ011-02 | Patient xem câu hỏi chưa có phản hồi | Question `PENDING` | Hiển thị trạng thái chờ phản hồi | TODO: Cập nhật sau khi chạy test |

#### TC-HQ-012: Admin kiểm duyệt câu hỏi/phản hồi nếu có

**Mô tả:** Kiểm tra Administrator kiểm duyệt câu hỏi hoặc phản hồi trong trường hợp hệ thống có quy trình kiểm duyệt.

**Tiền điều kiện:** Administrator đã đăng nhập. Có câu hỏi/phản hồi cần kiểm duyệt hoặc dữ liệu mẫu cho chức năng kiểm duyệt.

**Bước thực hiện:**

1. Đăng nhập bằng Administrator.
2. Mở trang kiểm duyệt nội dung.
3. Chọn câu hỏi hoặc phản hồi cần xử lý.
4. Thực hiện duyệt, ẩn hoặc từ chối theo chức năng được hỗ trợ.
5. Kiểm tra trạng thái nội dung sau thao tác.

**Kỹ thuật, công cụ:** Functional testing, Decision Table, Authorization testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| HQ012-01 | Admin duyệt nội dung | Nội dung đang chờ kiểm duyệt | Nội dung chuyển sang trạng thái được duyệt nếu chức năng hỗ trợ | TODO: Cập nhật sau khi chạy test |
| HQ012-02 | Admin ẩn nội dung | Nội dung có dấu hiệu vi phạm | Nội dung không hiển thị cho người dùng thường nếu chức năng hỗ trợ | TODO: Cập nhật sau khi chạy test |
| HQ012-03 | User không phải Admin kiểm duyệt | Token Patient/Doctor | Từ chối truy cập chức năng kiểm duyệt | TODO: Cập nhật sau khi chạy test |

### 4.6. Test case nhóm Appointment Management

Nhóm test case này kiểm tra luồng đặt lịch, xem lịch, hủy lịch, bác sĩ quản lý lịch hẹn và quản trị lịch hẹn. Trọng tâm là validation dữ liệu, kiểm tra trùng lịch, trạng thái appointment và notification nếu hệ thống có cấu hình thông báo.

**Bảng tổng hợp test case**

| Test ID | Tên test case | Loại test | Actor | Ưu tiên |
|---|---|---|---|---|
| TC-APT-001 | Patient đặt lịch hợp lệ | Positive | Patient | Cao |
| TC-APT-002 | Guest không được đặt lịch | Negative | Guest User | Cao |
| TC-APT-003 | Đặt lịch thiếu doctor/time/reason bị từ chối | Negative | Patient | Cao |
| TC-APT-004 | Đặt lịch với doctor inactive/unapproved bị từ chối | Negative | Patient | Cao |
| TC-APT-005 | Không cho đặt lịch trùng giờ bác sĩ | Negative | Patient | Cao |
| TC-APT-006 | Không cho Patient đặt hai lịch trùng giờ nếu rule có | Negative | Patient | Trung bình |
| TC-APT-007 | Appointment mới có trạng thái hợp lệ | Positive | Patient | Cao |
| TC-APT-008 | Patient xem lịch hẹn của mình | Positive | Patient | Cao |
| TC-APT-009 | Patient hủy lịch nếu hợp lệ | Positive/Negative | Patient | Cao |
| TC-APT-010 | Doctor xem lịch hẹn được đặt với mình | Positive | Doctor | Cao |
| TC-APT-011 | Doctor xác nhận lịch nếu có | Positive | Doctor | Trung bình |
| TC-APT-012 | Admin xem/quản lý lịch hẹn | Positive | Administrator | Cao |
| TC-APT-013 | Notification được tạo/gửi khi đặt lịch nếu có | Positive/Negative | System | Trung bình |

#### TC-APT-001: Patient đặt lịch hợp lệ

**Mô tả:** Kiểm tra Patient đặt lịch tư vấn với bác sĩ active/approved ở khung giờ còn trống.

**Tiền điều kiện:** Patient đã đăng nhập. Doctor active/approved. Khung giờ chưa có lịch xung đột.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Mở trang chi tiết bác sĩ hoặc trang đặt lịch.
3. Chọn bác sĩ, thời gian và nhập lý do tư vấn.
4. Gửi yêu cầu đặt lịch.
5. Kiểm tra lịch hẹn được tạo trong danh sách của Patient.

**Kỹ thuật, công cụ:** E2E testing, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT001-01 | Slot trống, dữ liệu hợp lệ | `doctorId`, `scheduledAt`, `reason` hợp lệ | Tạo appointment thành công | TODO: Cập nhật sau khi chạy test |

#### TC-APT-002: Guest không được đặt lịch

**Mô tả:** Kiểm tra hệ thống từ chối Guest User đặt lịch tư vấn.

**Tiền điều kiện:** Người dùng chưa đăng nhập. Có bác sĩ public.

**Bước thực hiện:**

1. Mở trang chi tiết bác sĩ.
2. Chọn đặt lịch.
3. Nếu kiểm thử API, gửi request tạo appointment không có token.
4. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Authorization testing, Negative testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT002-01 | Guest đặt lịch trên UI | Không có token | Chuyển đến đăng nhập/đăng ký, không tạo appointment | TODO: Cập nhật sau khi chạy test |
| APT002-02 | Guest gọi API đặt lịch | Request không có token | Từ chối xác thực | TODO: Cập nhật sau khi chạy test |

#### TC-APT-003: Đặt lịch thiếu doctor/time/reason bị từ chối

**Mô tả:** Kiểm tra validation khi đặt lịch thiếu thông tin bắt buộc.

**Tiền điều kiện:** Patient đã đăng nhập.

**Bước thực hiện:**

1. Mở form đặt lịch.
2. Bỏ trống bác sĩ, thời gian hoặc lý do.
3. Gửi form.
4. Kiểm tra thông báo validation.

**Kỹ thuật, công cụ:** Equivalence Partitioning, Boundary Value Analysis. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT003-01 | Thiếu bác sĩ | Không có `doctorId` | Từ chối tạo lịch, hiển thị lỗi validation | TODO: Cập nhật sau khi chạy test |
| APT003-02 | Thiếu thời gian | Không có `scheduledAt` | Từ chối tạo lịch, hiển thị lỗi validation | TODO: Cập nhật sau khi chạy test |
| APT003-03 | Thiếu lý do | `reason=""` | Từ chối tạo lịch, hiển thị lỗi validation | TODO: Cập nhật sau khi chạy test |

#### TC-APT-004: Đặt lịch với doctor inactive/unapproved bị từ chối

**Mô tả:** Kiểm tra Patient không thể đặt lịch với bác sĩ không hoạt động hoặc chưa được duyệt.

**Tiền điều kiện:** Patient đã đăng nhập. Có bác sĩ inactive/unapproved trong dữ liệu kiểm thử.

**Bước thực hiện:**

1. Gửi yêu cầu đặt lịch với doctor inactive hoặc unapproved.
2. Quan sát phản hồi.
3. Kiểm tra appointment không được tạo.

**Kỹ thuật, công cụ:** Decision Table, Negative testing. Công cụ: Postman, Playwright.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT004-01 | Doctor inactive | `doctorId` inactive | Từ chối đặt lịch | TODO: Cập nhật sau khi chạy test |
| APT004-02 | Doctor chưa duyệt | `doctorId` unapproved | Từ chối đặt lịch | TODO: Cập nhật sau khi chạy test |

#### TC-APT-005: Không cho đặt lịch trùng giờ bác sĩ

**Mô tả:** Kiểm tra hệ thống không cho tạo hai appointment trùng hoặc chồng lấn cùng bác sĩ.

**Tiền điều kiện:** Patient đã đăng nhập. Bác sĩ đã có appointment trong khung giờ cần kiểm thử.

**Bước thực hiện:**

1. Chọn bác sĩ đã có lịch ở một khung giờ.
2. Đặt lịch mới trùng hoặc chồng lấn với khung giờ đó.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Decision Table, State transition testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT005-01 | Trùng giờ bắt đầu | Same `doctorId`, same `scheduledAt` | Từ chối đặt lịch trùng | TODO: Cập nhật sau khi chạy test |
| APT005-02 | Chồng lấn thời lượng | Lịch mới nằm trong khoảng lịch đã có | Từ chối đặt lịch chồng lấn | TODO: Cập nhật sau khi chạy test |
| APT005-03 | Slot khác không chồng lấn | Same doctor, khác time slot | Cho phép đặt nếu dữ liệu hợp lệ | TODO: Cập nhật sau khi chạy test |

#### TC-APT-006: Không cho Patient đặt hai lịch trùng giờ nếu rule có

**Mô tả:** Kiểm tra Patient không đặt hai lịch cùng thời điểm nếu quy tắc nghiệp vụ áp dụng.

**Tiền điều kiện:** Patient đã có appointment trong một khung giờ. Hệ thống có rule kiểm tra trùng lịch theo Patient.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Tạo lịch mới với cùng thời gian appointment hiện có.
3. Quan sát phản hồi.

**Kỹ thuật, công cụ:** Decision Table, Negative testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT006-01 | Patient đặt lịch trùng giờ với lịch đang có | Same patient, same `scheduledAt` | Từ chối nếu rule không cho Patient đặt trùng | TODO: Cập nhật sau khi chạy test |
| APT006-02 | Patient đặt lịch khác giờ | Same patient, different time slot | Cho phép nếu dữ liệu hợp lệ | TODO: Cập nhật sau khi chạy test |

#### TC-APT-007: Appointment mới có trạng thái hợp lệ

**Mô tả:** Kiểm tra appointment sau khi tạo thành công có trạng thái ban đầu hợp lệ.

**Tiền điều kiện:** Patient đặt lịch thành công.

**Bước thực hiện:**

1. Patient tạo appointment hợp lệ.
2. Mở danh sách lịch hẹn hoặc kiểm tra response.
3. Kiểm tra trạng thái appointment mới.

**Kỹ thuật, công cụ:** State transition testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT007-01 | Appointment mới tạo | Appointment hợp lệ | Trạng thái thuộc nhóm trạng thái ban đầu hợp lệ như `PENDING`/`PENDING_CONFIRMATION` theo thiết kế | TODO: Cập nhật sau khi chạy test |

#### TC-APT-008: Patient xem lịch hẹn của mình

**Mô tả:** Kiểm tra Patient xem danh sách lịch hẹn thuộc tài khoản của mình.

**Tiền điều kiện:** Patient đã đăng nhập và có appointment.

**Bước thực hiện:**

1. Đăng nhập bằng Patient.
2. Mở trang lịch hẹn của tôi.
3. Kiểm tra danh sách lịch hẹn hiển thị.

**Kỹ thuật, công cụ:** Functional testing, Data privacy testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT008-01 | Patient A có lịch hẹn | Token Patient A | Chỉ hiển thị appointment của Patient A | TODO: Cập nhật sau khi chạy test |
| APT008-02 | Patient A không có lịch | Token Patient không có appointment | Hiển thị danh sách rỗng hoặc thông báo phù hợp | TODO: Cập nhật sau khi chạy test |

#### TC-APT-009: Patient hủy lịch nếu hợp lệ

**Mô tả:** Kiểm tra Patient hủy appointment khi trạng thái và thời gian còn cho phép.

**Tiền điều kiện:** Patient đã đăng nhập. Appointment thuộc Patient và ở trạng thái có thể hủy.

**Bước thực hiện:**

1. Mở danh sách lịch hẹn của Patient.
2. Chọn appointment có thể hủy.
3. Thực hiện hủy lịch.
4. Kiểm tra trạng thái sau hủy.

**Kỹ thuật, công cụ:** State transition testing, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT009-01 | Appointment còn được hủy | Appointment thuộc Patient, trạng thái hợp lệ | Hủy lịch thành công, trạng thái chuyển sang `CANCELLED` hoặc tương ứng | TODO: Cập nhật sau khi chạy test |
| APT009-02 | Appointment đã hoàn tất | Status `COMPLETED` | Không cho hủy lịch đã hoàn tất | TODO: Cập nhật sau khi chạy test |
| APT009-03 | Appointment thuộc Patient khác | Token Patient A, appointment Patient B | Từ chối hủy | TODO: Cập nhật sau khi chạy test |

#### TC-APT-010: Doctor xem lịch hẹn được đặt với mình

**Mô tả:** Kiểm tra Doctor xem được các appointment được đặt với mình.

**Tiền điều kiện:** Doctor đã đăng nhập. Có appointment thuộc Doctor.

**Bước thực hiện:**

1. Đăng nhập bằng Doctor.
2. Mở trang quản lý lịch tư vấn.
3. Quan sát danh sách lịch hẹn.

**Kỹ thuật, công cụ:** Authorization testing, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT010-01 | Appointment thuộc Doctor A | Token Doctor A | Hiển thị trong lịch của Doctor A | TODO: Cập nhật sau khi chạy test |
| APT010-02 | Appointment thuộc Doctor B | Token Doctor A | Không hiển thị trong lịch của Doctor A | TODO: Cập nhật sau khi chạy test |

#### TC-APT-011: Doctor xác nhận lịch nếu có

**Mô tả:** Kiểm tra Doctor xác nhận appointment trong trường hợp hệ thống có bước xác nhận lịch.

**Tiền điều kiện:** Doctor đã đăng nhập. Appointment thuộc Doctor và ở trạng thái chờ xác nhận.

**Bước thực hiện:**

1. Mở trang quản lý lịch tư vấn của Doctor.
2. Chọn appointment chờ xác nhận.
3. Thực hiện xác nhận lịch.
4. Kiểm tra trạng thái appointment.

**Kỹ thuật, công cụ:** State transition testing, Functional testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT011-01 | Lịch chờ xác nhận thuộc Doctor | Status chờ xác nhận | Trạng thái chuyển sang `CONFIRMED` hoặc tương ứng | TODO: Cập nhật sau khi chạy test |
| APT011-02 | Doctor xác nhận lịch của Doctor khác | Token Doctor A, appointment Doctor B | Từ chối thao tác | TODO: Cập nhật sau khi chạy test |

#### TC-APT-012: Admin xem/quản lý lịch hẹn

**Mô tả:** Kiểm tra Administrator xem và quản lý lịch hẹn trong hệ thống.

**Tiền điều kiện:** Administrator đã đăng nhập. Có dữ liệu appointment.

**Bước thực hiện:**

1. Đăng nhập bằng Administrator.
2. Mở trang quản lý lịch hẹn.
3. Lọc theo bác sĩ, bệnh nhân hoặc trạng thái nếu có.
4. Thực hiện cập nhật trạng thái nếu chức năng hỗ trợ.

**Kỹ thuật, công cụ:** Functional testing, Authorization testing. Công cụ: Playwright, Postman.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT012-01 | Admin xem danh sách lịch hẹn | Token Admin | Hiển thị danh sách appointment theo quyền quản trị | TODO: Cập nhật sau khi chạy test |
| APT012-02 | Admin lọc lịch hẹn | Filter theo bác sĩ/trạng thái | Trả về dữ liệu phù hợp filter | TODO: Cập nhật sau khi chạy test |
| APT012-03 | User không phải Admin truy cập quản lý lịch | Token Patient/Doctor | Từ chối truy cập admin appointment module | TODO: Cập nhật sau khi chạy test |

#### TC-APT-013: Notification được tạo/gửi khi đặt lịch nếu có

**Mô tả:** Kiểm tra hệ thống tạo hoặc gửi notification sau khi Patient đặt lịch thành công nếu chức năng notification được cấu hình.

**Tiền điều kiện:** Patient đặt lịch thành công. Notification Service được cấu hình ở mức MVP/mock hoặc provider thử nghiệm.

**Bước thực hiện:**

1. Patient đặt lịch hợp lệ.
2. Kiểm tra notification của Patient và Doctor nếu có.
3. Kiểm tra trạng thái gửi thông báo hoặc log gửi thông báo nếu hệ thống hỗ trợ.

**Kỹ thuật, công cụ:** Integration testing, Negative testing. Công cụ: Playwright, Postman, database client ở môi trường test.

**Bảng test data:**

| ID | Điều kiện | Test value | Expected result | Kết quả |
|---|---|---|---|---|
| APT013-01 | Notification được cấu hình | Đặt lịch thành công | Tạo/gửi thông báo nhắc lịch hoặc thông báo lịch mới theo thiết kế | TODO: Cập nhật sau khi chạy test |
| APT013-02 | Notification Service lỗi hoặc chưa bật | Đặt lịch thành công, notification unavailable | Nghiệp vụ đặt lịch không bị ghi sai; lỗi thông báo được xử lý theo thiết kế | TODO: Cập nhật sau khi chạy test |

### 4.7. Checklist rà soát

Checklist rà soát được xây dựng dựa trên SRS_Master_Checklist.md, Test_Strategy_and_Traceability.md, API_Contract_v1.md, Auth_Authorization_Matrix.md và Privacy_and_Data_Handling.md. Mục tiêu của phần này là hỗ trợ nhóm kiểm tra lại mức độ đầy đủ của yêu cầu, thiết kế, API và bảo mật trước khi hoàn thiện báo cáo cuối. Cột "Kết quả" sử dụng các giá trị placeholder `Pass`, `Fail`, `N/A`; khi chưa có biên bản rà soát chính thức hoặc bằng chứng thực thi, báo cáo để ở trạng thái `N/A` hoặc ghi chú cần kiểm tra thêm.

#### 4.7.1. Checklist rà soát yêu cầu

| ID | Nội dung rà soát | Mục tiêu | Kết quả | Ghi chú |
|---|---|---|---|---|
| REQ-01 | Actor của hệ thống được định nghĩa rõ ràng | Bảo đảm Guest User, Patient, Doctor, Administrator và người vận hành hệ thống có vai trò phân biệt | Pass | Đã mô tả trong Chương I và Chương II theo SRS/auth matrix. |
| REQ-02 | Use case có tiền điều kiện và hậu điều kiện | Bảo đảm UC01-UC04 có điều kiện bắt đầu/kết thúc rõ để thiết kế testcase | Pass | Đã bổ sung trong phần phân tích use case. |
| REQ-03 | Luồng chính và luồng phụ rõ ràng | Bảo đảm luồng Guest discovery, booking, Q&A, consultation có thể kiểm thử được | Pass | Cần rà soát lại lần cuối khi chốt báo cáo. |
| REQ-04 | Input/output nghiệp vụ được mô tả rõ | Bảo đảm các trường như `doctorId`, `scheduledAt`, `reason`, `title`, `content`, `summary` có kỳ vọng rõ | Pass | Đã đối chiếu với API contract và source code hiện tại trong các testcase. |
| REQ-05 | Trạng thái nghiệp vụ quan trọng được xác định | Làm rõ appointment status, question status và consultation session status | Pass | Bao gồm `PENDING_CONFIRMATION`, `PENDING`, `ANSWERED`, `ONGOING`, `COMPLETED`. |
| REQ-06 | Appointment duplicate slot được kiểm soát | Bảo đảm không cho bác sĩ/bệnh nhân đặt lịch trùng hoặc chồng lấn | Pass | SRS checklist và source code hiện tại có conflict prevention; cần test evidence ở giai đoạn thực thi. |
| REQ-07 | Question status transition rõ ràng | Bảo đảm câu hỏi chuyển từ `PENDING` sang `ANSWERED` khi Doctor phản hồi | Pass | Đã đưa vào UC03 và testcase 2.3.7. |
| REQ-08 | Error messages rõ ràng cho người dùng | Bảo đảm lỗi validation, unauthorized, forbidden, conflict, not found được phản hồi dễ hiểu | N/A | Cần rà soát UI/API response thực tế khi chạy kiểm thử. |

#### 4.7.2. Checklist rà soát thiết kế

| ID | Nội dung rà soát | Mục tiêu | Kết quả | Ghi chú |
|---|---|---|---|---|
| DES-01 | Thiết kế có phân tách actor và vai trò truy cập | Bảo đảm nghiệp vụ và giao diện không trộn quyền Patient/Doctor/Admin | Pass | Đã dựa trên Auth_Authorization_Matrix.md. |
| DES-02 | Thiết kế kiến trúc có phân lớp rõ | Bảo đảm client, server, database và tích hợp ngoài được trình bày nhất quán | Pass | Đã mô tả ở Chương II - 3.1. |
| DES-03 | Form/UI có input và output rõ | Bảo đảm các màn hình tra cứu, đặt lịch, gửi câu hỏi, tư vấn có dữ liệu vào/ra kiểm thử được | Pass | Ảnh giao diện còn là TODO placeholder, chưa có screenshot thật. |
| DES-04 | Luồng đặt lịch có xử lý duplicate slot | Bảo đảm thiết kế không cho lịch hẹn trùng bác sĩ hoặc bệnh nhân | Pass | Cần xác nhận bằng testcase execution sau. |
| DES-05 | Luồng Q&A có trạng thái và phạm vi xử lý | Bảo đảm Doctor chỉ phản hồi câu hỏi được gán hoặc câu hỏi mở theo rule | Pass | Source code hiện tại có rule từ chối câu hỏi gán cho bác sĩ khác. |
| DES-06 | Consultation session giới hạn đúng Patient/Doctor | Bảo đảm chỉ đúng Patient và Doctor của appointment được join/start/end/view result | Pass | Đã mô tả trong UC04 và testcase 2.4. |
| DES-07 | Fallback chat/video được thể hiện trong thiết kế | Bảo đảm hệ thống có hướng xử lý khi video mock/provider không khả dụng | Pass | Source code hiện tại hỗ trợ fallback `VIDEO` sang `CHAT`. |
| DES-08 | Thiết kế ghi nhận dữ liệu sức khỏe theo nguyên tắc tối thiểu | Hạn chế hiển thị/log dữ liệu nhạy cảm không cần thiết | N/A | Cần rà soát log thực tế và response trong giai đoạn kiểm thử. |

#### 4.7.3. Checklist rà soát API

| ID | Nội dung rà soát | Mục tiêu | Kết quả | Ghi chú |
|---|---|---|---|---|
| API-01 | API public discovery không yêu cầu authentication | Bảo đảm Guest User xem home, chuyên khoa, danh sách/chi tiết bác sĩ công khai | Pass | Theo API contract: `/public/home`, `/public/specialties`, `/public/doctors`. |
| API-02 | Protected API yêu cầu authentication | Bảo đảm booking, questions, appointments, consultations không cho Guest truy cập | Pass | Theo Auth_Authorization_Matrix.md và controllers hiện tại. |
| API-03 | Role-based access được định nghĩa cho API | Bảo đảm endpoint Patient/Doctor/Admin có role phù hợp | Pass | Auth matrix quy định RBAC; source code sử dụng `RolesGuard` ở các controller chính. |
| API-04 | API validate required fields | Bảo đảm `doctorId`, `scheduledAt`, `reason`, `title`, `content`, `summary` không rỗng/sai định dạng | Pass | DTO hiện tại có class-validator; cần test execution để xác nhận runtime. |
| API-05 | Patient không xem dữ liệu của Patient khác | Bảo đảm `/appointments/mine`, `/questions/mine`, `/consultations/mine` chỉ trả dữ liệu chủ sở hữu | Pass | Theo policy và source code hiện tại; cần privacy regression test sau. |
| API-06 | Doctor không truy cập appointment/question trái phạm vi | Bảo đảm Doctor chỉ quản lý lịch và câu hỏi thuộc phạm vi xử lý | Pass | Đã đưa vào testcase UC02, UC03, UC04. |
| API-07 | API trả lỗi nhất quán | Bảo đảm lỗi validation/unauthorized/forbidden/not found/conflict có format rõ | N/A | API_Contract_v1.md có convention; cần kiểm tra response thực tế. |
| API-08 | API consultation hỗ trợ REST và realtime channel | Bảo đảm REST messages và WebSocket `/consultations` có thể kiểm thử tích hợp | Pass | Theo API contract và source gateway hiện tại. |

#### 4.7.4. Checklist rà soát bảo mật

| ID | Nội dung rà soát | Mục tiêu | Kết quả | Ghi chú |
|---|---|---|---|---|
| SEC-01 | Password không được lưu dạng plain text | Bảo đảm mật khẩu được hash trước khi lưu | Pass | Source/doc hiện tại dùng bcryptjs; không ghi secret thật trong báo cáo. |
| SEC-02 | Token/secret không xuất hiện trong response hoặc báo cáo | Tránh lộ JWT secret, refresh token, reset token hoặc cấu hình nhạy cảm | Pass | Báo cáo chỉ liệt kê tên biến env, không ghi secret thật. |
| SEC-03 | Protected API yêu cầu JWT hợp lệ | Ngăn Guest gọi booking, question, consultation, rating, admin APIs | Pass | Theo auth matrix và controllers hiện tại. |
| SEC-04 | RBAC được áp dụng theo vai trò | Bảo đảm Patient, Doctor, Admin không dùng chức năng ngoài quyền | Pass | Dựa trên RolesGuard và role matrix. |
| SEC-05 | Patient không truy cập dữ liệu sức khỏe của Patient khác | Bảo vệ privacy của appointment, question, consultation result, prescription | Pass | Đã đưa vào testcase; cần execution evidence trước khi ghi kết quả test. |
| SEC-06 | Doctor không truy cập appointment/question không được phép | Bảo đảm Doctor chỉ xử lý dữ liệu thuộc phạm vi hoặc câu hỏi mở theo rule | Pass | Đã đưa vào UC02-UC04 và checklist API. |
| SEC-07 | Sensitive health data không bị log thô | Tránh log medical history, consultation summary, prescription content không cần thiết | N/A | Privacy guide có nguyên tắc và sanitizer; cần kiểm tra log thực tế ở final phase. |
| SEC-08 | Audit được áp dụng cho hành động nhạy cảm | Ghi nhận thay đổi appointment, trả lời câu hỏi, cập nhật summary/prescription, admin actions | Pass | SRS checklist và coverage evidence có audit coverage; cần bằng chứng execution nếu báo cáo kết quả. |

## 5. Hiện thực automation test và ghi nhận lỗi

Theo Test_Strategy_and_Traceability.md, hoạt động kiểm thử của hệ thống được định hướng theo test pyramid gồm unit test, integration test và API/E2E test theo các use case trong SRS. Trong phạm vi báo cáo cuối, nhóm định hướng minh họa kiểm thử tự động bằng Playwright E2E trên frontend repository `OnlineHealthConsultation-Web`, dựa trên các testcase thủ công đã đặc tả ở mục 2.1 đến 2.4. Vì báo cáo hiện đang được viết từ backend repository, phần này chỉ trình bày kế hoạch hiện thực, cấu trúc, cách chạy và cách ghi nhận bằng chứng; chưa tạo Playwright config, chưa thêm test file và chưa chạy test trong repository backend.

Các hình minh họa kết quả kiểm thử tự động sẽ được bổ sung sau khi nhóm triển khai và chạy bộ kiểm thử Playwright trên frontend repository.

### 5.1. Mục tiêu kiểm thử tự động

Mục tiêu của kiểm thử tự động là chuyển một số testcase quan trọng thành các kịch bản E2E có thể chạy lặp lại trên giao diện người dùng. Bộ kiểm thử dự kiến tập trung vào các thao tác đại diện cho người dùng thật: Guest User tìm kiếm bác sĩ, Patient đặt lịch tư vấn và gửi câu hỏi sức khỏe, Doctor phản hồi câu hỏi, Doctor bắt đầu và hoàn tất phiên tư vấn. Thông qua đó, nhóm có thể kiểm tra sự liên kết giữa frontend, API backend, xác thực/phân quyền, trạng thái nghiệp vụ và thông báo lỗi trên giao diện.

Kiểm thử tự động không thay thế toàn bộ kiểm thử thủ công, mà đóng vai trò minh họa và hồi quy cho các luồng quan trọng trong báo cáo. Những testcase có dữ liệu phức tạp, cần kiểm tra sâu về database hoặc race condition vẫn có thể được bổ sung bằng API/integration test ở giai đoạn sau nếu có điều kiện.

### 5.2. Phạm vi kiểm thử tự động

Phạm vi tự động hóa dự kiến bao gồm bốn use case chính đã được phân tích trong báo cáo:

- UC01: Guest User tra cứu bác sĩ và chuyển đổi sang đăng nhập/đăng ký.
- UC02: Patient đặt lịch hẹn tư vấn.
- UC03: Patient gửi câu hỏi sức khỏe và Doctor phản hồi câu hỏi.
- UC04: Doctor thực hiện phiên tư vấn trực tuyến và hoàn tất appointment.

Các testcase được chọn ưu tiên là các testcase có khả năng thể hiện rõ hành vi giao diện, điều hướng, xác thực, phân quyền và thay đổi trạng thái nghiệp vụ. Các testcase cần dữ liệu backend ổn định như bác sĩ đã được duyệt, tài khoản Patient/Doctor, khung giờ còn trống và dữ liệu appointment/question sẽ được chuẩn bị trước khi chạy Playwright.

### 5.3. Kỹ thuật kiểm thử

Các kỹ thuật kiểm thử dự kiến sử dụng gồm:

- Black-box testing: kiểm tra hành vi hệ thống qua giao diện và kết quả hiển thị, không phụ thuộc vào mã nguồn frontend.
- Equivalence Partitioning: chia dữ liệu kiểm thử thành nhóm hợp lệ và không hợp lệ, ví dụ keyword tìm kiếm, khung giờ đặt lịch, nội dung câu hỏi.
- Boundary Value Analysis: áp dụng cho thời gian đặt lịch, trạng thái slot, độ dài nội dung câu hỏi và thời điểm tham gia tư vấn nếu có điều kiện kiểm thử.
- Decision Table Testing: áp dụng cho tổ hợp trạng thái đăng nhập, vai trò người dùng, quyền truy cập và trạng thái nghiệp vụ.
- End-to-End testing: kiểm tra luồng hoàn chỉnh từ thao tác UI đến phản hồi của hệ thống.
- API testing: chỉ dùng như bằng chứng hỗ trợ nếu frontend cần chuẩn bị dữ liệu hoặc xác nhận trạng thái sau khi thao tác UI.

### 5.4. Công cụ sử dụng

| Công cụ | Trạng thái | Mục đích sử dụng | Ghi chú |
|---|---|---|---|
| Playwright | Dự kiến triển khai ở frontend repository | E2E automation cho các luồng người dùng chính | Công cụ chính của phần minh họa kiểm thử tự động; không sử dụng Cypress cho báo cáo này. |
| Playwright HTML Report | Dự kiến | Lưu bằng chứng kết quả chạy test dưới dạng HTML report | Sẽ bổ sung hình minh họa sau khi chạy test thật. |
| Playwright Trace/Screenshot | Dự kiến | Ghi nhận ảnh màn hình và trace khi test pass/fail | Dùng làm bằng chứng cho các luồng đặt lịch và phản hồi câu hỏi. |
| API backend | Hỗ trợ | Cung cấp dữ liệu và xử lý nghiệp vụ cho frontend E2E | Dùng API thật theo backend hiện có; API testing riêng chỉ là bằng chứng bổ trợ nếu cần. |
| Postman hoặc API script | Dự kiến nếu cần | Chuẩn bị dữ liệu hoặc xác nhận trạng thái sau E2E | Không phải hướng chính của báo cáo. |

### 5.5. Cấu trúc kiểm thử tự động dự kiến trên frontend

Khi triển khai trong frontend repository, nhóm dự kiến tổ chức Playwright theo cấu trúc dễ truy vết với các use case trong báo cáo:

```text
OnlineHealthConsultation-Web/
├── playwright.config.ts
├── tests/
│   ├── e2e/
│   │   ├── uc01-guest-discovery.spec.ts
│   │   ├── uc02-patient-appointment.spec.ts
│   │   ├── uc03-health-question.spec.ts
│   │   └── uc04-consultation-session.spec.ts
│   ├── fixtures/
│   │   └── auth.fixture.ts
│   └── utils/
│       ├── test-data.ts
│       └── api-helper.ts
└── playwright-report/
```

Các test file sẽ ưu tiên mô phỏng thao tác người dùng trên giao diện: nhập từ khóa, click nút đặt lịch, đăng nhập bằng tài khoản Patient/Doctor, nhập form đặt lịch, gửi câu hỏi, phản hồi câu hỏi và hoàn tất phiên tư vấn. Dữ liệu kiểm thử có thể được chuẩn bị bằng seed backend, tài khoản test cố định hoặc API helper, tùy trạng thái môi trường khi triển khai frontend.

<!-- TODO: Insert Figure: Playwright test code for selected testcases -->

Hình 55: Playwright test code for selected testcases

### 5.6. Danh sách testcase dự kiến tự động hóa

| Test ID | Usecase | Nội dung kiểm thử tự động dự kiến | Công cụ | Trạng thái | Ghi chú |
|---|---|---|---|---|---|
| AUTO-UC01-01 | UC01 | Guest User searches doctor by keyword | Playwright E2E | Planned / Evidence Pending | Kiểm tra ô tìm kiếm, danh sách bác sĩ và kết quả hiển thị. |
| AUTO-UC01-02 | UC01 | Guest User is redirected to login when choosing appointment booking | Playwright E2E | Planned / Evidence Pending | Kiểm tra điều hướng đăng nhập/đăng ký khi Guest chọn đặt lịch. |
| AUTO-UC02-01 | UC02 | Patient books appointment with available slot | Playwright E2E | Planned / Evidence Pending | Cần tài khoản Patient, bác sĩ active/approved và slot còn trống. |
| AUTO-UC02-02 | UC02 | Duplicate appointment slot is rejected | Playwright E2E | Planned / Evidence Pending | Có thể cần chuẩn bị dữ liệu slot đã đặt trước khi chạy test. |
| AUTO-UC03-01 | UC03 | Patient submits health question | Playwright E2E | Planned / Evidence Pending | Kiểm tra form gửi câu hỏi và thông báo thành công. |
| AUTO-UC03-02 | UC03 | Doctor replies to health question | Playwright E2E | Planned / Evidence Pending | Cần tài khoản Doctor và câu hỏi đang ở trạng thái chờ phản hồi. |
| AUTO-UC04-01 | UC04 | Doctor starts consultation session | Playwright E2E | Planned / Evidence Pending | Cần appointment hợp lệ trong khung thời gian cho phép. |
| AUTO-UC04-02 | UC04 | Doctor completes consultation and appointment becomes COMPLETED | Playwright E2E | Planned / Evidence Pending | Kiểm tra thao tác kết thúc phiên và trạng thái hoàn tất trên UI/API. |

### 5.7. Cách chạy kiểm thử tự động dự kiến

Khi frontend repository đã có Playwright config và test file, nhóm dự kiến chạy test bằng lệnh:

```bash
npx playwright test
```

Hoặc nếu frontend `package.json` có script riêng:

```bash
npm run test:e2e
```

Để xem báo cáo HTML sau khi chạy:

```bash
npx playwright show-report
```

Trước khi chạy, cần bảo đảm backend API, database và frontend dev server đã hoạt động đúng môi trường. Ví dụ quy trình dự kiến:

```text
1. Khởi động database và backend API.
2. Chuẩn bị dữ liệu test: Patient, Doctor, Specialty, Appointment/Question nếu cần.
3. Khởi động frontend hoặc cấu hình Playwright tự khởi động frontend.
4. Chạy Playwright E2E test.
5. Lưu terminal output, screenshot, trace và HTML report.
```

<!-- TODO: Insert Figure: Playwright test execution result in terminal -->

Hình 56: Playwright test execution result in terminal

<!-- TODO: Insert Figure: Playwright HTML report -->

Hình 57: Playwright HTML report

<!-- TODO: Insert Figure: Screenshot of automated test evidence for appointment booking -->

Hình 58: Screenshot of automated test evidence for appointment booking

<!-- TODO: Insert Figure: Screenshot of automated test evidence for health question reply -->

Hình 59: Screenshot of automated test evidence for health question reply

### 5.8. Cách ghi nhận kết quả kiểm thử

Khi thực thi Playwright, mỗi lần chạy cần ghi nhận tối thiểu các thông tin sau: ngày chạy test, branch/commit nếu có, môi trường chạy, trình duyệt dùng để test, lệnh chạy, số testcase pass/fail/skipped, ảnh terminal output, HTML report, screenshot hoặc trace nếu có. Kết quả chỉ được đưa vào báo cáo khi có bằng chứng kèm theo, chẳng hạn ảnh Playwright terminal, Playwright HTML report hoặc screenshot của test evidence.

Đối với mỗi testcase, nhóm ghi nhận theo trạng thái:

- Pass: kết quả thực tế khớp expected result và có evidence.
- Fail: kết quả thực tế khác expected result, có bug report tương ứng.
- Blocked: chưa thể chạy do thiếu dữ liệu, thiếu môi trường hoặc dependency.
- Not Run: đã đặc tả nhưng chưa thực thi.

Trong báo cáo hiện tại, các testcase tự động hóa đang ở trạng thái `Planned / Evidence Pending`. Nhóm chưa triển khai và chưa chạy Playwright trong backend repository này, do đó không trình bày tỷ lệ pass/fail.

### 5.9. Kết quả kiểm thử dự kiến

Kết quả kiểm thử trong bảng sẽ được cập nhật sau khi nhóm thực hiện chạy kiểm thử thủ công và kiểm thử tự động trên frontend. Tại thời điểm viết báo cáo trong backend repository, nhóm chưa chèn số liệu pass/fail thực tế để tránh ghi nhận kết quả không có minh chứng.

Các chỉ số trong phần này hiện được để ở trạng thái placeholder như `Chưa thực hiện`, `Chờ minh chứng` hoặc `Sẽ cập nhật sau khi chạy Playwright`. Khi có kết quả thực tế, nhóm sẽ cập nhật số lượng testcase đã chạy, số testcase pass/fail/skipped, ảnh chụp terminal, Playwright HTML report và minh chứng UI tương ứng.

### 5.10. Bảng tổng hợp kết quả kiểm thử

| Test Suite | Total Test Cases | Passed | Failed | Skipped | Status | Ghi chú |
|---|---|---|---|---|---|---|
| UC01 - Guest User tra cứu bác sĩ | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chờ minh chứng | Sẽ cập nhật sau khi chạy kiểm thử thủ công và Playwright trên frontend. |
| UC02 - Patient đặt lịch hẹn tư vấn | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chờ minh chứng | Cần minh chứng UI flow đặt lịch và trường hợp trùng slot nếu có. |
| UC03 - Doctor phản hồi câu hỏi sức khỏe | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chờ minh chứng | Cần minh chứng Patient gửi câu hỏi và Doctor phản hồi. |
| UC04 - Doctor thực hiện phiên tư vấn trực tuyến | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chờ minh chứng | Cần minh chứng start/end consultation và trạng thái hoàn tất. |
| Playwright E2E Automation | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Chưa thực hiện | Planned / Evidence Pending | Sẽ cập nhật sau khi chạy Playwright trong frontend repository. |

<!-- TODO: Insert Figure: Tổng hợp kết quả chạy Playwright -->

Hình 60: Tổng hợp kết quả chạy Playwright

<!-- TODO: Insert Figure: Playwright HTML report overview -->

Hình 61: Playwright HTML report overview

### 5.11. Bảng ghi nhận lỗi

| Bug ID | Module | Mô tả lỗi | Steps to Reproduce | Actual Result | Expected Result | Severity | Status |
|---|---|---|---|---|---|---|---|
| BUG-TPL-001 | Discovery | Chưa ghi nhận lỗi thực tế | Sẽ cập nhật nếu phát hiện lỗi khi kiểm thử UC01 | Chưa thực hiện | Chưa thực hiện | N/A | Template |
| BUG-TPL-002 | Appointment | Chưa ghi nhận lỗi thực tế | Sẽ cập nhật nếu phát hiện lỗi khi kiểm thử UC02 | Chưa thực hiện | Chưa thực hiện | N/A | Template |
| BUG-TPL-003 | Question | Chưa ghi nhận lỗi thực tế | Sẽ cập nhật nếu phát hiện lỗi khi kiểm thử UC03 | Chưa thực hiện | Chưa thực hiện | N/A | Template |
| BUG-TPL-004 | Consultation | Chưa ghi nhận lỗi thực tế | Sẽ cập nhật nếu phát hiện lỗi khi kiểm thử UC04 | Chưa thực hiện | Chưa thực hiện | N/A | Template |
| BUG-TPL-005 | Auth/Authorization | Chưa ghi nhận lỗi thực tế | Sẽ cập nhật nếu phát hiện lỗi xác thực/phân quyền | Chưa thực hiện | Chưa thực hiện | N/A | Template |

<!-- TODO: Insert Figure: Bug evidence screenshot if defects are found -->

Hình 62: Bug evidence screenshot if defects are found

### 5.12. Minh chứng kiểm thử cần bổ sung

| STT | Minh chứng cần bổ sung | Trạng thái | Ghi chú |
|---|---|---|---|
| 1 | Screenshot Playwright terminal result | Chờ minh chứng | Bổ sung sau khi chạy Playwright. |
| 2 | Screenshot Playwright HTML report | Chờ minh chứng | Bổ sung tổng quan số test pass/fail/skipped. |
| 3 | Screenshot selected passing test case | Chờ minh chứng | Chỉ bổ sung khi testcase đã chạy và pass thật. |
| 4 | Screenshot failed test case if any | Chờ minh chứng | Chỉ bổ sung nếu có testcase fail thật. |
| 5 | Screenshot bug evidence if any | Chờ minh chứng | Chỉ bổ sung nếu có defect được ghi nhận. |
| 6 | Screenshot UI flow for appointment booking | Chờ minh chứng | Minh chứng cho UC02. |
| 7 | Screenshot UI flow for health question/reply | Chờ minh chứng | Minh chứng cho UC03. |

### 5.13. Cách ghi nhận lỗi

Khi phát hiện lỗi, nhóm ghi nhận theo một bug report thống nhất để dễ truy vết về use case, testcase và module. Một lỗi cần có mô tả ngắn gọn, actual result, expected result, mức độ nghiêm trọng và trạng thái xử lý. Severity được đề xuất gồm Critical, High, Medium, Low. Status được đề xuất gồm Open, In Progress, Fixed, Retest, Closed hoặc Won't Fix.

| Bug ID | Module | Mô tả lỗi | Actual Result | Expected Result | Severity | Status |
|---|---|---|---|---|---|---|
| BUG-001 | Discovery | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |
| BUG-002 | Appointment | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |
| BUG-003 | Question | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |
| BUG-004 | Consultation | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |
| BUG-005 | Auth/Authorization | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |
| BUG-006 | Privacy/Logging | Chưa ghi nhận lỗi thực tế | N/A | N/A | N/A | Not Reported |

Các dòng trên là placeholder để chuẩn hóa cách ghi nhận lỗi, không phải danh sách lỗi đã phát hiện. Khi có lỗi thật, nhóm cần thay thế placeholder bằng thông tin thực tế và liên kết Bug ID với Test ID tương ứng.

# CHƯƠNG IV. KẾT LUẬN

## 1. Nhận xét về điểm mạnh, điểm yếu trong cách tiến hành đồ án

Đề tài "Xây dựng và kiểm thử nền tảng tư vấn sức khỏe trực tuyến" được thực hiện theo định hướng kết hợp giữa phân tích, thiết kế hệ thống và đảm bảo chất lượng phần mềm. Báo cáo tập trung vào bốn use case chính được lựa chọn từ SRS, gồm Guest User tra cứu bác sĩ và chuyển đổi sang đăng ký/đăng nhập, Patient đặt lịch hẹn tư vấn, Doctor phản hồi câu hỏi sức khỏe và Doctor thực hiện phiên tư vấn trực tuyến. Đây là các luồng nghiệp vụ đại diện cho quá trình người dùng tiếp cận hệ thống, tương tác với bác sĩ, đặt lịch và thực hiện tư vấn trong phạm vi MVP.

Trong quá trình thực hiện, nhóm đã sử dụng SRS và các tài liệu hỗ trợ như requirement baseline, API contract, architecture overview, database design, authorization matrix, privacy guide và test strategy để xây dựng nội dung báo cáo. Cách tiếp cận này giúp báo cáo không chỉ mô tả chức năng phần mềm mà còn thể hiện được mối liên hệ giữa yêu cầu, thiết kế, kiểm thử và bằng chứng chất lượng. Tuy nhiên, do báo cáo hiện đang được hoàn thiện từ backend repository, một số minh chứng liên quan đến giao diện frontend, hình ảnh sơ đồ và kết quả Playwright E2E vẫn cần được bổ sung trong giai đoạn hoàn thiện cuối.

### 1.1. Điểm mạnh

Trước hết, báo cáo bám theo tài liệu SRS và các tài liệu trong hệ thống tài liệu dự án. Các nội dung chính như actor, phạm vi MVP, luồng nghiệp vụ, yêu cầu chức năng, yêu cầu phi chức năng, phân quyền, bảo mật dữ liệu sức khỏe, API và cơ sở dữ liệu đều được trình bày dựa trên nguồn tài liệu có sẵn. Điều này giúp hạn chế việc mô tả cảm tính và tạo cơ sở cho hoạt động kiểm thử có thể truy vết.

Thứ hai, báo cáo đã xác định rõ các actor và business flow chính của nền tảng tư vấn sức khỏe trực tuyến. Các actor như Guest User, Patient, Doctor và Administrator được phân biệt theo quyền và phạm vi thao tác. Từ đó, nhóm lựa chọn bốn use case trọng tâm để phân tích sâu, phù hợp với mục tiêu kiểm thử trong báo cáo.

Thứ ba, đối với từng use case, báo cáo đã mô tả mục tiêu, actor, tiền điều kiện, luồng sự kiện chính, luồng phụ và hậu điều kiện. Cách đặc tả này giúp các luồng nghiệp vụ trở nên rõ ràng, có khả năng kiểm thử và thuận lợi cho việc thiết kế testcase. Các tình huống như Guest User bị chuyển hướng đăng nhập, Patient đặt lịch hẹn, Doctor trả lời câu hỏi và Doctor thực hiện phiên tư vấn đều được trình bày theo hướng có thể xác định expected result.

Thứ tư, báo cáo đã định nghĩa các yêu cầu chất lượng phần mềm theo ba nhóm môi trường: môi trường nghiệp vụ, môi trường vận hành và môi trường phát triển. Các nội dung như bảo mật, quyền riêng tư dữ liệu sức khỏe, phân quyền, kiểm tra dữ liệu nhập, xử lý lỗi, hiệu năng ở mức MVP, tính khả dụng, modular design, logging và khả năng kiểm thử được xem xét như những tiêu chí quan trọng chứ không chỉ là phần bổ sung.

Thứ năm, nhóm đã xây dựng checklist rà soát yêu cầu, thiết kế, API và bảo mật. Checklist này hỗ trợ việc kiểm tra lại tính đầy đủ của báo cáo, đặc biệt là các nội dung như actor, precondition/postcondition, input/output, RBAC, API validation, protected API, ownership của Patient/Doctor, bảo vệ mật khẩu và hạn chế log dữ liệu nhạy cảm.

Thứ sáu, báo cáo đã đặc tả testcase theo từng use case, bao gồm cả positive test và negative test. Các testcase được trình bày theo mô tả, tiền điều kiện, bước thực hiện, kỹ thuật/công cụ và bảng phân hoạch miền giá trị. Điều này giúp hoạt động kiểm thử có cấu trúc hơn, đồng thời tạo nền tảng để triển khai kiểm thử tự động sau này.

Cuối cùng, báo cáo đã xây dựng định hướng minh họa kiểm thử tự động bằng Playwright cho frontend. Phần này chưa ghi nhận kết quả thực thi, nhưng đã xác định rõ phạm vi tự động hóa, công cụ, cấu trúc test dự kiến, danh sách testcase ưu tiên, cách chạy, cách ghi nhận kết quả và cách ghi nhận lỗi. Đây là cơ sở để nhóm tiếp tục triển khai ở frontend repository trong giai đoạn sau.

### 1.2. Điểm yếu

Bên cạnh các điểm mạnh, báo cáo vẫn còn một số hạn chế cần được hoàn thiện. Trước hết, báo cáo chưa chèn đầy đủ hình ảnh giao diện UI, use case diagram, activity diagram, ERD và screenshot kết quả kiểm thử. Nhiều vị trí hiện vẫn đang được giữ dưới dạng TODO placeholder, vì nhóm chưa có đủ ảnh chụp giao diện và sơ đồ cuối cùng để đưa vào báo cáo.

Thứ hai, báo cáo chưa cập nhật kết quả pass/fail thực tế cho Playwright automation. Lý do là phần kiểm thử tự động sẽ được triển khai và chạy ở frontend repository, trong khi báo cáo hiện đang được viết từ backend repository. Vì vậy, các bảng kết quả kiểm thử tự động hiện chỉ ở trạng thái `Chưa thực hiện`, `Chờ minh chứng` hoặc `Planned / Evidence Pending`, nhằm tránh ghi nhận kết quả không có bằng chứng.

Thứ ba, phạm vi testcase hiện tập trung vào bốn use case trọng tâm. Một số chức năng mở rộng như profile management, prescription, rating, notification và admin dashboard đã được nhắc đến trong phạm vi hệ thống nhưng chưa được kiểm thử sâu trong phần testcase chính của báo cáo. Các chức năng này cần được bổ sung testcase nếu muốn đánh giá đầy đủ hơn toàn bộ hệ thống.

Thứ tư, performance testing và security testing hiện mới dừng ở mức tiêu chí, định hướng và checklist rà soát. Báo cáo chưa có bằng chứng kiểm thử hiệu năng bằng công cụ chuyên dụng, chưa có số liệu benchmark và chưa có kết quả kiểm thử bảo mật chuyên sâu theo OWASP API Security Top 10. Do đó, phần đánh giá chất lượng phi chức năng vẫn cần được bổ sung nếu thời gian và môi trường cho phép.

Thứ năm, video consultation trong phạm vi MVP có thể mới ở mức mock hoặc simulated nếu chưa tích hợp video provider thật. Vì vậy, các kiểm thử liên quan đến video cần được hiểu theo phạm vi MVP và cần được cập nhật lại nếu hệ thống tích hợp dịch vụ video thật trong giai đoạn sau.

## 2. Khả năng cải tiến cách tiến hành đồ án

Để hoàn thiện báo cáo và nâng cao chất lượng đồ án, nhóm cần tiếp tục bổ sung hình ảnh diagram và screenshot UI vào các vị trí TODO. Các hình như wireframe, use case diagram, activity diagram, kiến trúc hệ thống, ERD, giao diện chính và minh chứng kiểm thử sẽ giúp báo cáo trực quan hơn, đồng thời tăng tính thuyết phục khi trình bày kết quả.

Về kiểm thử tự động, nhóm cần triển khai Playwright E2E test trong frontend repository dựa trên các testcase đã đặc tả. Các luồng nên được ưu tiên gồm Guest User tìm kiếm bác sĩ, Guest User bị chuyển hướng đăng nhập khi đặt lịch, Patient đặt lịch hẹn, Patient gửi câu hỏi, Doctor phản hồi câu hỏi, Doctor bắt đầu phiên tư vấn và Doctor hoàn tất phiên tư vấn. Sau khi chạy Playwright, nhóm cần cập nhật bảng kết quả kiểm thử, số lượng testcase pass/fail/skipped, Playwright HTML report, ảnh terminal và các screenshot minh chứng vào báo cáo cuối kỳ.

Ngoài bốn use case trọng tâm, nhóm có thể mở rộng testcase cho profile management, prescriptions, ratings, notifications và admin dashboard. Đây là các chức năng quan trọng để đánh giá đầy đủ hơn vai trò Patient, Doctor và Administrator, đồng thời giúp kiểm tra các luồng quản trị và lịch sử tư vấn sau khi phiên tư vấn hoàn tất.

Về kiểm thử phi chức năng, nếu có thời gian, nhóm nên bổ sung kiểm thử hiệu năng bằng k6 hoặc JMeter cho các endpoint quan trọng như public doctor discovery, login, booking appointment, question answering và consultation history. Kết quả hiệu năng nên được trình bày bằng số liệu cụ thể như response time trung bình, p95, throughput và error rate trong điều kiện kiểm thử xác định.

Về bảo mật, nhóm nên bổ sung kiểm thử theo OWASP API Security Top 10, đặc biệt là các nhóm lỗi liên quan đến broken object level authorization, broken authentication, excessive data exposure, mass assignment, security misconfiguration và improper assets management. Các testcase bảo mật cần tập trung vào quyền sở hữu dữ liệu Patient/Doctor, dữ liệu sức khỏe, token, password, audit log và thông báo lỗi.

Về quy trình phát triển, nhóm có thể tích hợp CI/CD để chạy test tự động khi có thay đổi mã nguồn. Pipeline nên bao gồm kiểm tra định dạng, type-check, build, unit/integration test, Playwright E2E test và lưu report artifact. Nếu phù hợp, nhóm có thể bổ sung báo cáo test dạng HTML hoặc Allure để kết quả kiểm thử dễ theo dõi, dễ chèn vào báo cáo và dễ đối chiếu trong quá trình nghiệm thu.

Tóm lại, báo cáo hiện đã hình thành được nền tảng phân tích, thiết kế và đặc tả kiểm thử cho nền tảng tư vấn sức khỏe trực tuyến. Tuy nhiên, để đạt mức hoàn thiện cao hơn cho báo cáo cuối kỳ, nhóm cần tiếp tục bổ sung minh chứng trực quan, kết quả kiểm thử thực tế và các kiểm thử tự động/phi chức năng có bằng chứng rõ ràng.

# TÀI LIỆU THAM KHẢO

<!-- TODO: Chuẩn hóa danh mục tài liệu tham khảo ở phiên bản cuối. -->
